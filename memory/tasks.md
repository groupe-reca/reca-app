# Tâches — RECA Centre des opérations

Portée totale : 10 modules (doc 07 §8). Ordre de construction imposé par les dépendances du pipeline.

- [x] Auth — terminé
- [x] A. Leads — terminé
- [x] B. Soumissions/Quotes — terminé (commit 3ded2b1) — inclut fix RLS soft-delete
- [x] C. Clients — terminé (commit c6edf7b, 2026-07-10) — conversion soumission → client câblée
- [x] D. Contrats — terminé (commit 4c29ef9, 2026-07-10) — lié depuis la fiche client
- [x] E. Factures — terminé (commit 0876f18, 2026-07-10) — TPS 5%/TVQ 9.975% autofill, lien contrat optionnel
- [x] F. Paiements — terminé (commit 8480040, 2026-07-10) — enregistrement/annulation recalcule solde+statut facture ; pas d'action "Modifier"
- [x] H. Équipements — terminé (commit ec3c575, 2026-07-10)
- [x] I. Employés — terminé (commit 97a1c7c, 2026-07-10) — équipements assignés (table jonction employee_equipment) + promotion de rôle (employe ↔ administrateur) via public.users, RLS. Vue "Application employé" pas encore spécifiée (doc 06 la reporte).
- [x] G. Routes — terminé (commit af9f848) — le module le plus complexe (route_clients + route_assignments, liste de clients ordonnée, assignation employé+équipement+date)
- [x] J. Paramètres — terminé (2026-07-11, pas encore commité). `pages/SettingsPage.tsx` créé (section entreprise/taxes + section comptes). Route `/settings` câblée dans `src/routes/router.tsx` sous un nœud `RequireRole roles={['administrateur']}` (composant déjà présent dans `auth/components/RequireAuth.tsx`, jamais utilisé avant — premier module à s'en servir). Lien sidebar activé (`enabled: true`) et rendu conditionnel à `session.user.role === 'administrateur'` dans `src/layouts/Sidebar.tsx`. Vérifié : `tsc --noEmit` et `eslint` propres, dev server sert les nouveaux fichiers sans erreur de compilation. Vérifié en navigateur (Playwright + Chromium, `chromium-cli` n'existe pas comme paquet installable donc Playwright sert de driver équivalent) : connexion admin, page Paramètres rendue avec les vraies données live (formulaire entreprise/taxes pré-rempli, table des 2 comptes admin), aucune erreur console, lien sidebar visible pour un compte admin. Mutations de rôle/statut de compte non testées par clic pour éviter de modifier des comptes réels sur la base live. Les 10 modules du scope sont maintenant tous construits.

## Modernisation UI / Responsive
- [x] 2026-07-12 — Socle responsive (design tokens/durées, shell `layouts/`, primitives `components/ui/`) + pilote module Leads. Tiroir sidebar sous 1024px + fond Bleu Nuit (doc 05), table→cartes sous 768px (`TableColumn.primary`/`hiddenOnCard`), Modal en bottom-sheet sous 640px (animé via `motion`), Dropdown avec détection de collision de viewport, Tooltip avec fallback tactile, Toaster repositionné mobile, nouveaux hooks partagés `useBreakpoint`/`useBodyScrollLock`/`useFocusTrap`. Vérifié `tsc -b`+lint propres et en navigateur (Playwright, 375/768/1024/1280px) sur Leads+Dashboard, non-régression confirmée. Détail : `memory/plans.md`. Reste à faire (tâches futures séparées) : dérouler le même pattern sur les 8 autres modules.

## Navigation — pattern "page dédiée"
- [x] 2026-07-12 — Généralisation du pattern de création en page dédiée (module Contrats) à tout le site (`.input/tache1.md`). Les 7 modules restants (Leads, Quotes, Clients, Invoices, Equipment, Employees, Routes) créent maintenant via `/x/new` au lieu d'une modale sur la liste. Les 2 boutons de création croisée depuis une fiche détail (Lead→"Créer une soumission", Client→"Créer une facture") suivent le même principe, avec les query params `leadId`/`clientId` pour préselectionner. Le bouton "Transformer en client" (fiche Soumission) est aussi devenu une page (`/clients/new?convertQuoteId=...`), avec préremplissage du nom depuis le lead d'origine et liaison automatique après création. Chaque `XFormModal` (sauf `ClientFormModal`) est devenu édition seule (branche création retirée, code mort). Exception documentée conservée : `ClientSearchPicker` (recherche/création rapide de client à l'intérieur d'un formulaire en cours, ex. Contrats) reste une modale — c'est le pattern que Contrats lui-même utilise. Payments et Settings hors périmètre (pas de flow de création équivalent). Vérifié : `tsc -b` + `npm run lint` propres ; test de bout en bout réel (Playwright, compte admin, port 3011) sur les 7 boutons "Nouveau X" + les 3 flows croisés (Lead→Soumission avec mise à jour de statut, Soumission→Client avec préremplissage et liaison, Client→Facture avec client préselectionné) + régression (modale d'édition, modale inline `ClientSearchPicker`). Un bug de course a été trouvé et corrigé pendant la vérification (voir `memory/memory.md`). Toutes les données de test créées ont été supprimées après vérification (4 leads, 4 soumissions, 2 clients, 2 factures). Pas encore commité.

## Optimisation de la navigation
- [x] 2026-07-12 — Refonte navigation (branche `optimisation-de-la-navigation`, contenu de `.input/tache1.md` **réutilisé/écrasé** pour cette nouvelle tâche — ne pas confondre avec la tâche "page dédiée" du même nom de fichier ci-dessus). Implémenté : (1) sidebar verrouillée sans scroll, pagination Précédent/Suivant si les items dépassent la hauteur dispo (`useElementSize`, nouveau hook) ; (2) déconnexion déplacée du header vers une icône seule dans le bloc utilisateur du bas du menu (`useLogout` importé dans `Sidebar.tsx`, retiré d'`AppLayout.tsx`) ; (3) `Breadcrumb.tsx` fusionné avec l'ancien header en une seule barre du haut (hamburger + fil d'Ariane), bug corrigé (avant : tous les crumbs étaient cliquables y compris la page courante — maintenant seul le dernier est en texte simple) ; (4) système de modules activables/désactivables : colonne `settings.modules` (jsonb) + `ModulesTable.tsx` dans Paramètres + garde de route `RequireModule` (nouveau, `src/routes/RequireModule.tsx` — décision : cache le lien ET bloque l'URL directe, validé avec l'utilisateur) appliquée aux 9 modules (pas seulement les 4 désactivés par défaut, car l'admin peut désormais désactiver n'importe lequel) ; Routes/Équipements/Factures/Employés désactivés par défaut via seed de migration ; (5) "Centre des opérations" retiré du header du menu (logo seul) ; (6) nouveaux logos SVG posés (`logo-sombre.svg` sidebar, `logo-clair.svg` login) ; (7) colonne `users.nom` ajoutée + seed "Gabriel Cayer" pour `admin@groupereca.ca`, affiché dans le bloc utilisateur du menu et la table des comptes.
  `tsc -b` et `npm run lint` propres. Committé (commit `02086f2`) le 2026-07-12 avant de démarrer le sprint Contrats suivant. Les 2 migrations (`20260712180000_settings_modules`, `20260712180100_users_nom`) ont été appliquées par l'utilisateur le 2026-07-12 — vérifié indirectement (login fonctionnel, sondes REST 200 sur les 2 colonnes) pendant la vérification du sprint Contrats ci-dessous ; sidebar paginée/breadcrumb/toggle modules/rename admin non re-testés individuellement mais aucune régression observée. Détail complet du plan : `/root/.claude/plans/binary-coalescing-hejlsberg.md`.

## Refonte UX Contrats — interface "Desktop Application"
- [x] 2026-07-12 — Sprint UX du module Contrats (branche `refonte-ux-contrats`, `.input/tache1.md` réutilisé une 3e fois). Transforme `ContractCreatePage` (longue page qui défile) en interface à onglets façon SaaS desktop (Linear/Vercel/Stripe/Notion) : 5 onglets (Informations générales incl. `ClientSearchPicker`, Description des services, Seuils et heures d'intervention, Obligations et responsabilités, Modalités de paiement), un seul visible à la fois, défilement local uniquement, sidebar/header/breadcrumb/onglets/footer toujours fixes. Nouveaux composants génériques réutilisables dans `src/components/layout/` : `ModuleContainer`, `StickyPageHeader`, `PageTabs` (avec point rouge d'indication d'erreur par onglet — amélioration ajoutée au-delà de la demande brute, pour ne pas laisser "Créer" désactivé sans indice visible), `StickyPageFooter`, `PageLayout` (composition des 4 précédents). `ContractClausesFields.tsx` (1 fichier, 3 sections) éclaté en 3 composants dédiés + `TextareaField.tsx` partagé. Fix fondation partagée (tous modules, bas risque) : `AppLayout.tsx` `min-h-screen` → `h-screen overflow-hidden` pour éliminer le double-scroll (body + `<main>`) — ne change rien visuellement pour les autres modules. Logos +50% (`h-8→h-12` sidebar, `h-16→h-24` login), ratio inchangé. Bug de breadcrumb corrigé au passage (hors périmètre strict mais explicitement requis par le ticket §5) : les routes `contracts/*` étaient des routes sœurs à plat, donc "Contrats" n'apparaissait jamais dans le fil d'Ariane de "Nouveau contrat" — routes imbriquées sous un parent `contracts` avec `handle: { breadcrumb: 'Contrats' }` pour obtenir "Centre des opérations › Contrats › Nouveau contrat".
  `tsc -b` et `npm run lint` propres. Vérifié en navigateur (Playwright, compte admin, port 3011) : les 2 migrations nav-optim étaient déjà appliquées par l'utilisateur (sondes REST 200) donc vérification complète possible — aucun scroll de body nulle part dans l'app, 5 onglets basculent sans reflow de la coquille, point d'erreur testé sur un onglet non affiché, footer sans chevauchement sidebar (280px), tailles de logo confirmées visuellement, breadcrumb correct sur liste/nouveau/détail. Contrat de test créé de bout en bout (client inline + 10 clauses + 2 échéances 50/50 → 2 factures générées, statut Actif) puis toutes les données de test supprimées (2 factures, 1 contrat, 1 client) via l'API REST avec la clé `service_role`. Pas encore commité.

## Wizard de création de contrat (sprint007)
- [x] 2026-07-12/13 — Remplacement complet de `ContractCreatePage` (onglets) par un vrai Wizard linéaire verrouillé à 6 étapes (Client → Analyse de la propriété [Localiser/Délimiter/Valider] → Services → Obligations → Paiement → Validation), branche `wizard-creation-contrat` (créée depuis `refonte-ux-contrats`, commit `ea2d6c1`). Intègre Mapbox GL JS + Mapbox GL Draw + Turf.js (nouvelles dépendances) pour géocoder l'adresse du client, capturer une vue satellite, tracer des zones au polygone et calculer leur superficie GPS. Les clauses du contrat sont désormais générées automatiquement à partir des réponses Q&R de l'étape Obligations (`generateClauses()`), plus de texte libre édité par l'utilisateur. Détail complet (schéma SQL, composants, décisions) : `memory/plans.md` et `/root/.claude/plans/recupere-ton-prompt-dans-jazzy-gizmo.md`.
  `tsc -b`, `npm run lint` et `npm run build` propres. Vérifié en navigateur (Playwright, compte admin, port 3011) : les 6 étapes se déroulent correctement avec verrouillage/déverrouillage du bouton "Suivant" à chaque étape, retour en arrière fonctionnel, barre de progression ○/◐/✓ correcte, génération des clauses visible à l'étape Validation, presets de paiement fonctionnels. Comme convenu avec l'utilisateur (pas de token Mapbox disponible ce sprint), un **mode de secours manuel** a été ajouté à l'étape "Délimiter" (ajout d'une zone par nom + superficie en m² tapés à la main) quand `VITE_MAPBOX_TOKEN` est absent — ceci a permis de vérifier tout le reste du Wizard de bout en bout malgré l'absence de carte réelle ; ce mode disparaît automatiquement dès qu'un token est configuré (`isMapboxConfigured` devient `true`). Deux clients de test créés pendant la vérification (WizardTest Sprint007, ToastCheck Temp) supprimés via l'interface après usage.
  **2026-07-13 (suite)** : l'utilisateur a appliqué la migration (confirmé par sonde REST : `contracts.mode_paiement` et `contract_zones` existent en live) et fourni un token dans `.input/mapbox` — **mais ce token commence par `sk.` (token secret Mapbox), pas `pk.` (token public)**. Mapbox GL JS refuse strictement les tokens secrets côté navigateur et lève une exception synchrone dans son constructeur (`new mapboxgl.Map()`), ce qui faisait planter toute l'application (aucun `ErrorBoundary` sur la route) — corrigé : `useMapboxMap.ts` capture désormais cette exception (`try/catch` + `queueMicrotask` pour respecter la règle eslint `set-state-in-effect`) et expose un état `error`, propagé jusqu'à `MapCanvas`/`PropertyMap`/les sous-étapes Localiser/Délimiter via un nouveau prop `onError`/`mapUnavailable` — la carte affiche maintenant un message d'erreur clair et le mode de secours manuel (zones saisies à la main) prend le relais automatiquement, sans crash. **Bug de gating trouvé et corrigé pendant ce test** : la barre de progression affichait un ✓ sur "Obligations" dès l'étape 2, avant même d'y être passé, car son critère de complétion est trivialement vrai (valeurs par défaut valides) et le calcul de statut ne vérifiait pas que l'étape était réellement derrière l'étape courante — corrigé (`index < activeIndex && STEP_COMPLETION[id]` au lieu de `index < STEP_ORDER.length - 1`), voir `ContractWizard.tsx`. Vérifié en navigateur avec le token `sk.*` fourni : aucun crash, message d'erreur affiché, mode de secours fonctionnel de bout en bout jusqu'à l'étape Services, barre de progression correcte. Client de test (GracefulTest Fallback) supprimé après vérification.
  **2026-07-13 (clôture) — vérification complète réussie** : l'utilisateur a fourni un token public (`pk.*`) dans `.input/mapbox2`. Test réel de bout en bout avec la vraie carte : géocodage automatique de l'adresse client affiché sur une vue satellite réelle, tracé d'un polygone à la souris sur la carte, calcul de surface GPS via Turf.js (75.01 m² sur un test), capture satellite → upload Storage réel, contrat créé (statut Actif) avec 2 factures générées automatiquement (échéancier "Deux versements"), tout visible sur la fiche détail du contrat. **3 bugs réels trouvés et corrigés pendant cette vérification** (aucun n'était lié au choix du token, auraient existé avec n'importe quel token valide) :
  1. `PolygonEditor.tsx` : rappeler `draw.changeMode('draw_polygon')` de façon synchrone depuis le gestionnaire de l'événement `draw.create` lui-même fait re-rentrer Mapbox GL Draw dans son propre cycle interne (`onStop`/`fire`) → `RangeError: Maximum call stack size exceeded`. Corrigé en reportant l'appel via `setTimeout(..., 0)`.
  2. Même fichier : le nettoyage (`map.removeControl(draw)`) au démontage lève une exception si la carte parente (`PropertyMap`) a déjà été détruite avant que ce nettoyage ne s'exécute (ordre de démontage entre composants frères non garanti) — corrigé par un `try/catch` best-effort, sans impact puisque la carte entière disparaît de toute façon.
  3. `contractCreation.schema.ts` : `accumulationMaximaleCm` (champ "Accumulation maximale" de l'étape Obligations) passait silencieusement de `null` à `0` après un aller-retour entre étapes (remontage du champ non-contrôlé par React Hook Form) — `0` échouait la contrainte Zod `.positive()`, ce qui faisait échouer `handleSubmit` **silencieusement** (aucun toast, aucune erreur console, le bouton "Créer" semblait ne rien faire). Corrigé en deux temps : contrainte assouplie à `.min(0)` (0 devient une valeur valide) et `generateClauses.ts` traite `0` comme "non renseigné" (un plafond de 0 cm n'a pas de sens métier). La cause racine exacte du remontage à 0 n'a pas été entièrement élucidée (piste RHF/DOM sur les inputs `type=number` avec valeur par défaut `null`) mais n'a plus d'impact utilisateur.
  Correctif additionnel hors bug : `ContractDetailPage.tsx` affichait encore "pi²" à côté de `superficie`, une unité obsolète depuis que ce champ est maintenant calculé en m² par le Wizard — corrigé (label seulement, l'affichage complet des zones/carte/clauses/services sur la fiche détail reste un fast-follow hors scope, non oublié).
  Toutes les données de test créées pendant cette session de débogage intensive (11+ clients dupliqués, 2 contrats, 4 factures, zones associées) supprimées via l'interface après vérification — listes Contrats/Factures/Clients revérifiées vides de toute trace de test. `tsc -b` et `npm run lint` propres après chaque correctif. **Le Wizard est maintenant pleinement fonctionnel de bout en bout, migration et token réels confirmés.**
  **2026-07-13 (mergé)** : commité et mergé dans `main` via PR #1 (commit `16963be` puis merge `c4a96e5`), suivi d'un commit de documentation du flow dans `CLAUDE.md` (`2438861`). Branche `wizard-creation-contrat` toujours présente côté remote pour historique.

## Sprint 008 — Analyse de la propriété V1, carte immersive (branche `sprint008-analyse-propriete`)
- [x] 2026-07-13 — Amélioration de la sous-étape **Localiser** du Wizard Contrats (`.input/sprint008`) : la carte se centre/zoome désormais automatiquement sur le terrain (`map.fitBounds()`), un contour GeoJSON de démonstration (pas encore de données cadastrales — placeholder généré autour du centre géocodé, `buildDemoBoundary`) est tracé en rouge RECA (`PropertyBoundaryLayer.tsx`), tout ce qui est à l'extérieur est assombri (`PropertyMaskLayer.tsx`), avec un panneau d'information à gauche (30%, `PropertyInfoPanel.tsx`) + la carte à droite (70%). Nouveau contrôleur invisible `MapViewportController.tsx` (fitBounds + reveal + fonction "recentrer"). `MapCapture.tsx` renommé `CaptureButton.tsx` et étendu pour préparer (sans encore persister) zoom/centre/polygone au moment de la capture. Délimiter/Valider non touchés.
  **2 bugs réels trouvés et corrigés pendant la vérification navigateur** (voir `memory/memory.md` pour le détail technique à réutiliser pour tout futur layer Mapbox GL de ce module) : (1) `onReady={setRecenter}` passait une fonction directement à un `setState`, que React interprète comme un updater fonctionnel plutôt que la nouvelle valeur — le bouton "Recentrer" restait bloqué désactivé ; corrigé (`onReady={(fn) => setRecenter(() => fn)}`). (2) Les couches Mapbox (contour + masque) ne s'ajoutaient jamais : le code attendait un événement `load` qui avait déjà eu lieu avant que ces composants ne reçoivent `map` (contrat `MapCanvas`/`onMapReady`), et `isStyleLoaded()` peut renvoyer `false` juste après `load` (tuiles encore en chargement) — corrigé en ajoutant les sources/layers directement sans attendre un futur `load`. Un 3e problème (non un bug applicatif) : le masque en "polygone à trou" (anneau extérieur + trou GeoJSON standard) ne se rendait pas visuellement malgré une géométrie/état logique corrects (confirmé via `queryRenderedFeatures`) — remplacé par 4 rectangles simples entourant le contour (`buildMaskRects`), nettement plus robuste pour un layer `fill` Mapbox GL.
  Vérifié en navigateur (Playwright, compte admin, port 3011, token Mapbox public réel) : géocodage auto + fitBounds + contour rouge + masque assombri confirmés visuellement (captures comparées avant/après chaque correctif), bouton "Recentrer la carte" fonctionnel (y compris après zoom manuel), bouton Capturer toujours opérationnel (upload Storage réel inchangé), sous-étape Délimiter non régressée (tracé de polygone + calcul de surface toujours fonctionnels), test de bout en bout complet jusqu'à la création réelle d'un contrat (statut Actif, 2 factures générées). `tsc -b`, `npm run lint` propres à chaque étape. **Piège de outillage découvert pendant le débogage** : `page.locator('canvas').screenshot()` (Playwright) ne reflète pas fidèlement le contenu WebGL d'une carte Mapbox GL dans cet environnement (donnait un faux négatif "masque invisible" alors que `page.screenshot()` plein-page, au même instant, montrait le rendu correct) — toujours utiliser une capture plein-page (ou clip par coordonnées) pour vérifier visuellement une carte Mapbox, jamais `locator(canvas).screenshot()`.
  17 clients de test créés pendant cette session de débogage (dont 1 contrat + 2 factures + 1 zone pour le test de bout en bout final) tous supprimés après vérification (confirmé vide en base). Pas encore commité.

## Sprint 008.5 — Optimisation UX du Wizard, carte plein écran (branche `sprint008-analyse-propriete`, suite)
- [x] 2026-07-13 — Sprint purement UX/layout (`.input/sprint85`, aucune nouvelle fonctionnalité métier) sur l'étape "Analyse de la propriété" du Wizard Contrats : (1) en-tête de page du Wizard (titre "Nouveau contrat" + sous-titre) supprimé sur les 6 étapes — `WizardProgress` devient le premier élément sous le Breadcrumb (`WizardLayout.tsx` perd ses props `title`/`subtitle`) ; (2) la carte remplit désormais toute la hauteur disponible sans scroll (`PropertyMap.tsx` : `h-[480px]` fixe → `h-full`, chaîne `flex h-full`/`min-h-0` propagée dans `WizardStepProperty.tsx`/`PropertySubStepLocate.tsx`/`PropertySubStepDelineate.tsx`) ; (3) tous les boutons autrefois rendus dans la zone de carte (📸 Capturer, "Continuer vers le tracé"/"vers la validation") supprimés et regroupés dans un Footer unique via un mécanisme de remontée de navigation ("nav lifting") — décision validée avec l'utilisateur : étendu aux **3 sous-étapes** (Localiser/Délimiter/Valider), pas seulement Localiser (seul exemple concret du brief) ; (4) `WizardFooter.tsx` gagne un slot générique optionnel `action` (label/icon/onClick/disabled/isLoading), volontairement agnostique du métier pour être réutilisable par tout futur Wizard (Soumissions, Clients, Employés, Équipements) ; (5) `CaptureButton.tsx` (composant bouton, sprint008) transformé en hook `src/features/contracts/hooks/usePropertyCapture.ts` (même logique d'upload Storage réel) pour préparer une future capture automatique sans toucher à l'UI ; (6) `useMapboxMap.ts` gagne un `ResizeObserver` → `map.resize()`, nécessaire maintenant que la carte a une hauteur flexible (fenêtre redimensionnée = plus d'espace vide/débordement).
  **1 bug réel trouvé et corrigé pendant la vérification navigateur** (voir détail technique dans `memory/memory.md`) : boucle de rendu infinie ("Maximum update depth exceeded") causée par `goNext` (recréé à chaque rendu de `ContractWizard`) passé en dépendance d'un `useEffect` dans `WizardStepProperty` (sous-étape Valider) — chaque appel à `onNavChange` déclenchait un rendu, qui recréait `goNext`, qui redéclenchait l'effet, à l'infini. Corrigé en mémoïsant `goNext` via `useCallback`.
  Vérifié en navigateur (Playwright, compte admin, port 3011, token Mapbox public réel) : plus de titre "Nouveau contrat" nulle part, barre de progression bien sous le Breadcrumb sur les 6 étapes, carte occupant l'essentiel de la hauteur sans scroll vertical sur les 3 sous-étapes de Propriété, Footer affichant Annuler/Retour/Capturer/Suivant sur Localiser (Capturer absent sur Délimiter/Valider, comme attendu), navigation Suivant fonctionnelle à travers les 3 sous-étapes puis vers Services, resize de fenêtre (1440→1024→1920px) sans espace vide ni débordement sur la carte (masque/contour se réajustent correctement), régression : Services/Obligations/Paiement/Validation toujours fonctionnels sans header, test de bout en bout complet jusqu'à la création réelle d'un contrat (statut Actif, 2 factures). `tsc -b`/`npm run lint`/`npm run build` propres. 4 clients de test (+ 1 contrat/2 factures/1 zone pour le test final) créés pendant la vérification, tous supprimés après (confirmé vide en base, avec confirmation explicite de l'utilisateur). Pas encore commité (toujours sur la branche `sprint008-analyse-propriete`, sprint008 + sprint008.5 cumulés, aucun des deux commité).

## Tâche 3 — Cadrage carte + parité Localiser/Délimiter + noms de zone en liste déroulante (branche `sprint008-analyse-propriete`, suite)
- [x] 2026-07-13 — Retour utilisateur (`.input/tache3.md`) après sprint008/008.5 : (1) `MapViewportController.tsx` — `FIT_BOUNDS_PADDING` réduit de `60` à `20` (zoom plus serré autour du contour) ; (2) nouveau composant partagé `PropertyMapStage.tsx` (compose `PropertyMap`+`PropertyMaskLayer`+`PropertyBoundaryLayer`+`MapViewportController`+placeholder indisponible) réutilisé par **Localiser et Délimiter**, pour qu'elles affichent exactement la même vue (contour rouge + masque + même cadrage) — `WizardStepProperty.tsx` calcule désormais `boundary` une seule fois et le transmet aux deux sous-étapes (au lieu que Localiser le recalcule seule) ; (3) `PropertySubStepDelineate.tsx` entièrement réorganisé en 2 colonnes (panneau gauche/carte droite, identique à Localiser) — colonne gauche = nouveau `PropertyZonesPanel.tsx` (zones tracées + formulaire d'ajout) au lieu de l'ancienne mise en page verticale (Card d'instructions + carte + formulaires empilés) ; (4) nom de zone : champ texte libre → menu déroulant (`ZONE_NAME_OPTIONS` dans `constants/wizardOptions.ts` : Stationnement/Entrée/Trottoir/Autres, Stationnement par défaut) avec champ "Précisez le nom" additionnel (le menu reste visible, pas de remplacement) quand Autres est choisi — décision validée avec l'utilisateur.
  Vérifié en navigateur (Playwright, compte admin, port 3011, token Mapbox public réel) : Localiser visiblement plus zoomé qu'avant, Délimiter affichant exactement le même cadre/contour/masque que Localiser avec le panneau "Zones tracées" à gauche, menu déroulant fonctionnel (Stationnement par défaut, "Autres" révèle "Précisez le nom" sans faire disparaître le menu, zone ajoutée avec le bon nom résolu dans les 2 cas testés — tracé au clic + sélection dropdown simple), test de bout en bout complet jusqu'à la création réelle d'un contrat (statut Actif, 2 factures). `tsc -b`/`npm run lint`/`npm run build` propres, aucune erreur console. 2 clients de test (+1 contrat/2 factures/1 zone pour le test final) créés puis supprimés après vérification (confirmation utilisateur). Pas encore commité.

## Sprint 009 — Dessin des zones de déneigement & calcul des superficies (branche `sprint008-analyse-propriete`, suite)
- [x] 2026-07-13 — Implémentation complète (`.input/sprint009`) : déclenchement explicite du dessin (barre d'outils `ZoneToolbar` + Nouvelle zone/Modifier/Supprimer), 7 types de zone colorés (Entrée/Stationnement/Trottoir/Escaliers/Aire de manœuvre/Terrasse/Autre) via `ZoneNamingModal`+`ZoneTypeSelector`, édition des sommets d'une zone existante (recalcul de surface en direct, aucun bouton), zoom sur une zone, suppression réelle carte+liste (corrige un bug préexistant de désynchronisation), résumé par type (`ZoneAreaSummary`) + liste (`PolygonList`/`PolygonCard`, ce dernier aussi réutilisé par `SurfaceSummary`). Colonne `type` ajoutée à `contract_zones` (pas de nouvelle table `contract_areas`, décision validée avec l'utilisateur) via `supabase/migrations/20260713010000_contract_zones_type.sql`.
  `tsc -b`/`npm run lint`/`npm run build` propres. Vérifié en navigateur (Playwright, compte admin, port 3011, token Mapbox réel) : tous les critères d'acceptation UI confirmés (dessin non auto-actif, fenêtre de nommage + couleur par type sur carte et liste, Annuler retire le polygone, 3 zones de types différents avec total/sous-totaux corrects, glisser un sommet recalcule en direct, Supprimer retire carte+liste, Suivant gaté sur zéro zone). Détail technique complet (bug `userProperties: true` manquant sur Mapbox GL Draw, bug de désynchronisation Supprimer corrigé, thème Draw 7 couches) : `memory/memory.md`.
  **2026-07-13 (persistance confirmée)** : migration appliquée par l'utilisateur. Re-testé de bout en bout avec 2 contrats réels — 1 zone (`trottoir`) puis 5 zones couvrant les 5 autres types (Entrée/Stationnement/Escaliers/Aire de manœuvre/Terrasse) — sonde REST post-création confirme `type`/`label`/`surface_m2`/`ordre` corrects pour chaque ligne `contract_zones`, aucune erreur console. Données de test supprimées après vérification (2 clients, 2 contrats, 6 zones). **Sprint entièrement vérifié, y compris la persistance réelle.** Pas encore commité (accumulé avec sprint008/008.5/tâche3 sur la même branche).

## Système d'index de fichiers (méthode "Signa" / réduction de tokens)
- [x] 2026-07-13 — Création de `memory/file-index.md` (table de référence module → fichiers réels, pour les 11 dossiers `src/features/*` + section "Partagé/transverse" : `src/components/layout`, `src/components/ui`, `src/hooks`, `src/layouts`, `src/routes`, `src/lib`) suite à `.input/tache2.md` (note méthode "Signa" sur la réduction de coûts en tokens Claude Code). `CLAUDE.md` mis à jour : section 4 étendue (4e fichier `memory/` documenté + intégré au protocole début/fin de tâche), nouvelle section 6 "Règles pour limiter l'exploration du repo" (ne pas parcourir tout le repo si le module est déjà cartographié, consulter l'index en premier réflexe, exception ponctuelle si fichier absent de l'index mais mise à jour obligatoire ensuite). Aucun changement de code applicatif — tâche 100% documentation/mémoire. Point noté : le module "Équipements" a pour dossier réel `src/features/equipments/` (pluriel), pas `equipment` — à corriger si un futur index/doc utilise le mauvais nom.

## Tâches hors-module
- [x] 2026-07-11 — Ajout de la section "Système de mémoire" au `CLAUDE.md` racine (créé, n'existait pas avant).
- [x] 2026-07-11 — Renommage `mempry/` → `memory/` et initialisation des 3 fichiers avec le contexte réel du projet.
- [x] 2026-07-12 — Rework module Contrats : fiche de création complète (clauses éditables, recherche/création client inline, échéancier de paiement → génération de factures). Migration `20260712000000_contracts_clauses_and_schedule.sql` appliquée par l'utilisateur à la base live. Test de bout en bout réel confirmé : contrat créé (statut Actif), 2 modalités 50/50 → 2 factures générées automatiquement (574,88 $ chacune, statut Brouillon), client créé et bien lié. Les 3 critères de vérification de `.input/tache2.md` sont validés. Données de test nettoyées (client/contrat/factures du test final supprimés ; 2 clients "Jean Test"/"Jean Test2" d'un test précédent laissés à l'utilisateur). Pas encore commité. Détail complet du plan : `/root/.claude/plans/robust-fluttering-dahl.md`.
