# Tâches — RECA Centre des opérations

Portée totale : 10 modules (doc 07 §8). Ordre de construction imposé par les dépendances du pipeline.

- [x] Auth — terminé
- [x] A. Leads — terminé
- [x] B. Soumissions/Quotes — terminé (commit 3ded2b1) — inclut fix RLS soft-delete
- [x] C. Clients — terminé (commit c6edf7b, 2026-07-10) — conversion soumission → client câblée
- [x] D. Contrats — terminé (commit 4c29ef9, 2026-07-10) — lié depuis la fiche client
- [x] E. Factures — terminé (commit 0876f18, 2026-07-10) — TPS 5%/TVQ 9.975% autofill, lien contrat optionnel
- [x] F. Paiements — terminé (commit 8480040, 2026-07-10) — enregistrement/annulation recalcule solde+statut facture ; pas d'action "Modifier"
- [x] H. Équipements — terminé (commit ec3c575, 2026-07-10)
- [x] I. Employés — terminé (commit 97a1c7c, 2026-07-10) — équipements assignés (table jonction employee_equipment) + promotion de rôle (employe ↔ administrateur) via public.users, RLS. Vue "Application employé" pas encore spécifiée (doc 06 la reporte).
- [x] G. Routes — terminé (commit af9f848) — le module le plus complexe (route_clients + route_assignments, liste de clients ordonnée, assignation employé+équipement+date)
- [x] J. Paramètres — terminé (2026-07-11, pas encore commité). `pages/SettingsPage.tsx` créé (section entreprise/taxes + section comptes). Route `/settings` câblée dans `src/routes/router.tsx` sous un nœud `RequireRole roles={['administrateur']}` (composant déjà présent dans `auth/components/RequireAuth.tsx`, jamais utilisé avant — premier module à s'en servir). Lien sidebar activé (`enabled: true`) et rendu conditionnel à `session.user.role === 'administrateur'` dans `src/layouts/Sidebar.tsx`. Vérifié : `tsc --noEmit` et `eslint` propres, dev server sert les nouveaux fichiers sans erreur de compilation. Vérifié en navigateur (Playwright + Chromium, `chromium-cli` n'existe pas comme paquet installable donc Playwright sert de driver équivalent) : connexion admin, page Paramètres rendue avec les vraies données live (formulaire entreprise/taxes pré-rempli, table des 2 comptes admin), aucune erreur console, lien sidebar visible pour un compte admin. Mutations de rôle/statut de compte non testées par clic pour éviter de modifier des comptes réels sur la base live. Les 10 modules du scope sont maintenant tous construits.

## Modernisation UI / Responsive
- [x] 2026-07-12 — Socle responsive (design tokens/durées, shell `layouts/`, primitives `components/ui/`) + pilote module Leads. Tiroir sidebar sous 1024px + fond Bleu Nuit (doc 05), table→cartes sous 768px (`TableColumn.primary`/`hiddenOnCard`), Modal en bottom-sheet sous 640px (animé via `motion`), Dropdown avec détection de collision de viewport, Tooltip avec fallback tactile, Toaster repositionné mobile, nouveaux hooks partagés `useBreakpoint`/`useBodyScrollLock`/`useFocusTrap`. Vérifié `tsc -b`+lint propres et en navigateur (Playwright, 375/768/1024/1280px) sur Leads+Dashboard, non-régression confirmée. Détail : `memory/plans.md`. Reste à faire (tâches futures séparées) : dérouler le même pattern sur les 8 autres modules.

## Navigation — pattern "page dédiée"
- [x] 2026-07-12 — Généralisation du pattern de création en page dédiée (module Contrats) à tout le site (`.input/tache1.md`). Les 7 modules restants (Leads, Quotes, Clients, Invoices, Equipment, Employees, Routes) créent maintenant via `/x/new` au lieu d'une modale sur la liste. Les 2 boutons de création croisée depuis une fiche détail (Lead→"Créer une soumission", Client→"Créer une facture") suivent le même principe, avec les query params `leadId`/`clientId` pour préselectionner. Le bouton "Transformer en client" (fiche Soumission) est aussi devenu une page (`/clients/new?convertQuoteId=...`), avec préremplissage du nom depuis le lead d'origine et liaison automatique après création. Chaque `XFormModal` (sauf `ClientFormModal`) est devenu édition seule (branche création retirée, code mort). Exception documentée conservée : `ClientSearchPicker` (recherche/création rapide de client à l'intérieur d'un formulaire en cours, ex. Contrats) reste une modale — c'est le pattern que Contrats lui-même utilise. Payments et Settings hors périmètre (pas de flow de création équivalent). Vérifié : `tsc -b` + `npm run lint` propres ; test de bout en bout réel (Playwright, compte admin, port 3011) sur les 7 boutons "Nouveau X" + les 3 flows croisés (Lead→Soumission avec mise à jour de statut, Soumission→Client avec préremplissage et liaison, Client→Facture avec client préselectionné) + régression (modale d'édition, modale inline `ClientSearchPicker`). Un bug de course a été trouvé et corrigé pendant la vérification (voir `memory/memory.md`). Toutes les données de test créées ont été supprimées après vérification (4 leads, 4 soumissions, 2 clients, 2 factures). Pas encore commité.

## Optimisation de la navigation
- [x] 2026-07-12 — Refonte navigation (branche `optimisation-de-la-navigation`, contenu de `.input/tache1.md` **réutilisé/écrasé** pour cette nouvelle tâche — ne pas confondre avec la tâche "page dédiée" du même nom de fichier ci-dessus). Implémenté : (1) sidebar verrouillée sans scroll, pagination Précédent/Suivant si les items dépassent la hauteur dispo (`useElementSize`, nouveau hook) ; (2) déconnexion déplacée du header vers une icône seule dans le bloc utilisateur du bas du menu (`useLogout` importé dans `Sidebar.tsx`, retiré d'`AppLayout.tsx`) ; (3) `Breadcrumb.tsx` fusionné avec l'ancien header en une seule barre du haut (hamburger + fil d'Ariane), bug corrigé (avant : tous les crumbs étaient cliquables y compris la page courante — maintenant seul le dernier est en texte simple) ; (4) système de modules activables/désactivables : colonne `settings.modules` (jsonb) + `ModulesTable.tsx` dans Paramètres + garde de route `RequireModule` (nouveau, `src/routes/RequireModule.tsx` — décision : cache le lien ET bloque l'URL directe, validé avec l'utilisateur) appliquée aux 9 modules (pas seulement les 4 désactivés par défaut, car l'admin peut désormais désactiver n'importe lequel) ; Routes/Équipements/Factures/Employés désactivés par défaut via seed de migration ; (5) "Centre des opérations" retiré du header du menu (logo seul) ; (6) nouveaux logos SVG posés (`logo-sombre.svg` sidebar, `logo-clair.svg` login) ; (7) colonne `users.nom` ajoutée + seed "Gabriel Cayer" pour `admin@groupereca.ca`, affiché dans le bloc utilisateur du menu et la table des comptes.
  `tsc -b` et `npm run lint` propres. Committé (commit `02086f2`) le 2026-07-12 avant de démarrer le sprint Contrats suivant. Les 2 migrations (`20260712180000_settings_modules`, `20260712180100_users_nom`) ont été appliquées par l'utilisateur le 2026-07-12 — vérifié indirectement (login fonctionnel, sondes REST 200 sur les 2 colonnes) pendant la vérification du sprint Contrats ci-dessous ; sidebar paginée/breadcrumb/toggle modules/rename admin non re-testés individuellement mais aucune régression observée. Détail complet du plan : `/root/.claude/plans/binary-coalescing-hejlsberg.md`.

## Refonte UX Contrats — interface "Desktop Application"
- [x] 2026-07-12 — Sprint UX du module Contrats (branche `refonte-ux-contrats`, `.input/tache1.md` réutilisé une 3e fois). Transforme `ContractCreatePage` (longue page qui défile) en interface à onglets façon SaaS desktop (Linear/Vercel/Stripe/Notion) : 5 onglets (Informations générales incl. `ClientSearchPicker`, Description des services, Seuils et heures d'intervention, Obligations et responsabilités, Modalités de paiement), un seul visible à la fois, défilement local uniquement, sidebar/header/breadcrumb/onglets/footer toujours fixes. Nouveaux composants génériques réutilisables dans `src/components/layout/` : `ModuleContainer`, `StickyPageHeader`, `PageTabs` (avec point rouge d'indication d'erreur par onglet — amélioration ajoutée au-delà de la demande brute, pour ne pas laisser "Créer" désactivé sans indice visible), `StickyPageFooter`, `PageLayout` (composition des 4 précédents). `ContractClausesFields.tsx` (1 fichier, 3 sections) éclaté en 3 composants dédiés + `TextareaField.tsx` partagé. Fix fondation partagée (tous modules, bas risque) : `AppLayout.tsx` `min-h-screen` → `h-screen overflow-hidden` pour éliminer le double-scroll (body + `<main>`) — ne change rien visuellement pour les autres modules. Logos +50% (`h-8→h-12` sidebar, `h-16→h-24` login), ratio inchangé. Bug de breadcrumb corrigé au passage (hors périmètre strict mais explicitement requis par le ticket §5) : les routes `contracts/*` étaient des routes sœurs à plat, donc "Contrats" n'apparaissait jamais dans le fil d'Ariane de "Nouveau contrat" — routes imbriquées sous un parent `contracts` avec `handle: { breadcrumb: 'Contrats' }` pour obtenir "Centre des opérations › Contrats › Nouveau contrat".
  `tsc -b` et `npm run lint` propres. Vérifié en navigateur (Playwright, compte admin, port 3011) : les 2 migrations nav-optim étaient déjà appliquées par l'utilisateur (sondes REST 200) donc vérification complète possible — aucun scroll de body nulle part dans l'app, 5 onglets basculent sans reflow de la coquille, point d'erreur testé sur un onglet non affiché, footer sans chevauchement sidebar (280px), tailles de logo confirmées visuellement, breadcrumb correct sur liste/nouveau/détail. Contrat de test créé de bout en bout (client inline + 10 clauses + 2 échéances 50/50 → 2 factures générées, statut Actif) puis toutes les données de test supprimées (2 factures, 1 contrat, 1 client) via l'API REST avec la clé `service_role`. Pas encore commité.

## Wizard de création de contrat (sprint007)
- [x] 2026-07-12/13 — Remplacement complet de `ContractCreatePage` (onglets) par un vrai Wizard linéaire verrouillé à 6 étapes (Client → Analyse de la propriété [Localiser/Délimiter/Valider] → Services → Obligations → Paiement → Validation), branche `wizard-creation-contrat` (créée depuis `refonte-ux-contrats`, commit `ea2d6c1`). Intègre Mapbox GL JS + Mapbox GL Draw + Turf.js (nouvelles dépendances) pour géocoder l'adresse du client, capturer une vue satellite, tracer des zones au polygone et calculer leur superficie GPS. Les clauses du contrat sont désormais générées automatiquement à partir des réponses Q&R de l'étape Obligations (`generateClauses()`), plus de texte libre édité par l'utilisateur. Détail complet (schéma SQL, composants, décisions) : `memory/plans.md` et `/root/.claude/plans/recupere-ton-prompt-dans-jazzy-gizmo.md`.
  `tsc -b`, `npm run lint` et `npm run build` propres. Vérifié en navigateur (Playwright, compte admin, port 3011) : les 6 étapes se déroulent correctement avec verrouillage/déverrouillage du bouton "Suivant" à chaque étape, retour en arrière fonctionnel, barre de progression ○/◐/✓ correcte, génération des clauses visible à l'étape Validation, presets de paiement fonctionnels. Comme convenu avec l'utilisateur (pas de token Mapbox disponible ce sprint), un **mode de secours manuel** a été ajouté à l'étape "Délimiter" (ajout d'une zone par nom + superficie en m² tapés à la main) quand `VITE_MAPBOX_TOKEN` est absent — ceci a permis de vérifier tout le reste du Wizard de bout en bout malgré l'absence de carte réelle ; ce mode disparaît automatiquement dès qu'un token est configuré (`isMapboxConfigured` devient `true`). Deux clients de test créés pendant la vérification (WizardTest Sprint007, ToastCheck Temp) supprimés via l'interface après usage.
  **2026-07-13 (suite)** : l'utilisateur a appliqué la migration (confirmé par sonde REST : `contracts.mode_paiement` et `contract_zones` existent en live) et fourni un token dans `.input/mapbox` — **mais ce token commence par `sk.` (token secret Mapbox), pas `pk.` (token public)**. Mapbox GL JS refuse strictement les tokens secrets côté navigateur et lève une exception synchrone dans son constructeur (`new mapboxgl.Map()`), ce qui faisait planter toute l'application (aucun `ErrorBoundary` sur la route) — corrigé : `useMapboxMap.ts` capture désormais cette exception (`try/catch` + `queueMicrotask` pour respecter la règle eslint `set-state-in-effect`) et expose un état `error`, propagé jusqu'à `MapCanvas`/`PropertyMap`/les sous-étapes Localiser/Délimiter via un nouveau prop `onError`/`mapUnavailable` — la carte affiche maintenant un message d'erreur clair et le mode de secours manuel (zones saisies à la main) prend le relais automatiquement, sans crash. **Bug de gating trouvé et corrigé pendant ce test** : la barre de progression affichait un ✓ sur "Obligations" dès l'étape 2, avant même d'y être passé, car son critère de complétion est trivialement vrai (valeurs par défaut valides) et le calcul de statut ne vérifiait pas que l'étape était réellement derrière l'étape courante — corrigé (`index < activeIndex && STEP_COMPLETION[id]` au lieu de `index < STEP_ORDER.length - 1`), voir `ContractWizard.tsx`. Vérifié en navigateur avec le token `sk.*` fourni : aucun crash, message d'erreur affiché, mode de secours fonctionnel de bout en bout jusqu'à l'étape Services, barre de progression correcte. Client de test (GracefulTest Fallback) supprimé après vérification.
  **2026-07-13 (clôture) — vérification complète réussie** : l'utilisateur a fourni un token public (`pk.*`) dans `.input/mapbox2`. Test réel de bout en bout avec la vraie carte : géocodage automatique de l'adresse client affiché sur une vue satellite réelle, tracé d'un polygone à la souris sur la carte, calcul de surface GPS via Turf.js (75.01 m² sur un test), capture satellite → upload Storage réel, contrat créé (statut Actif) avec 2 factures générées automatiquement (échéancier "Deux versements"), tout visible sur la fiche détail du contrat. **3 bugs réels trouvés et corrigés pendant cette vérification** (aucun n'était lié au choix du token, auraient existé avec n'importe quel token valide) :
  1. `PolygonEditor.tsx` : rappeler `draw.changeMode('draw_polygon')` de façon synchrone depuis le gestionnaire de l'événement `draw.create` lui-même fait re-rentrer Mapbox GL Draw dans son propre cycle interne (`onStop`/`fire`) → `RangeError: Maximum call stack size exceeded`. Corrigé en reportant l'appel via `setTimeout(..., 0)`.
  2. Même fichier : le nettoyage (`map.removeControl(draw)`) au démontage lève une exception si la carte parente (`PropertyMap`) a déjà été détruite avant que ce nettoyage ne s'exécute (ordre de démontage entre composants frères non garanti) — corrigé par un `try/catch` best-effort, sans impact puisque la carte entière disparaît de toute façon.
  3. `contractCreation.schema.ts` : `accumulationMaximaleCm` (champ "Accumulation maximale" de l'étape Obligations) passait silencieusement de `null` à `0` après un aller-retour entre étapes (remontage du champ non-contrôlé par React Hook Form) — `0` échouait la contrainte Zod `.positive()`, ce qui faisait échouer `handleSubmit` **silencieusement** (aucun toast, aucune erreur console, le bouton "Créer" semblait ne rien faire). Corrigé en deux temps : contrainte assouplie à `.min(0)` (0 devient une valeur valide) et `generateClauses.ts` traite `0` comme "non renseigné" (un plafond de 0 cm n'a pas de sens métier). La cause racine exacte du remontage à 0 n'a pas été entièrement élucidée (piste RHF/DOM sur les inputs `type=number` avec valeur par défaut `null`) mais n'a plus d'impact utilisateur.
  Correctif additionnel hors bug : `ContractDetailPage.tsx` affichait encore "pi²" à côté de `superficie`, une unité obsolète depuis que ce champ est maintenant calculé en m² par le Wizard — corrigé (label seulement, l'affichage complet des zones/carte/clauses/services sur la fiche détail reste un fast-follow hors scope, non oublié).
  Toutes les données de test créées pendant cette session de débogage intensive (11+ clients dupliqués, 2 contrats, 4 factures, zones associées) supprimées via l'interface après vérification — listes Contrats/Factures/Clients revérifiées vides de toute trace de test. `tsc -b` et `npm run lint` propres après chaque correctif. **Le Wizard est maintenant pleinement fonctionnel de bout en bout, migration et token réels confirmés.**
  **2026-07-13 (mergé)** : commité et mergé dans `main` via PR #1 (commit `16963be` puis merge `c4a96e5`), suivi d'un commit de documentation du flow dans `CLAUDE.md` (`2438861`). Branche `wizard-creation-contrat` toujours présente côté remote pour historique.

## Sprint 008 — Analyse de la propriété V1, carte immersive (branche `sprint008-analyse-propriete`)
- [x] 2026-07-13 — Amélioration de la sous-étape **Localiser** du Wizard Contrats (`.input/sprint008`) : la carte se centre/zoome désormais automatiquement sur le terrain (`map.fitBounds()`), un contour GeoJSON de démonstration (pas encore de données cadastrales — placeholder généré autour du centre géocodé, `buildDemoBoundary`) est tracé en rouge RECA (`PropertyBoundaryLayer.tsx`), tout ce qui est à l'extérieur est assombri (`PropertyMaskLayer.tsx`), avec un panneau d'information à gauche (30%, `PropertyInfoPanel.tsx`) + la carte à droite (70%). Nouveau contrôleur invisible `MapViewportController.tsx` (fitBounds + reveal + fonction "recentrer"). `MapCapture.tsx` renommé `CaptureButton.tsx` et étendu pour préparer (sans encore persister) zoom/centre/polygone au moment de la capture. Délimiter/Valider non touchés.
  **2 bugs réels trouvés et corrigés pendant la vérification navigateur** (voir `memory/memory.md` pour le détail technique à réutiliser pour tout futur layer Mapbox GL de ce module) : (1) `onReady={setRecenter}` passait une fonction directement à un `setState`, que React interprète comme un updater fonctionnel plutôt que la nouvelle valeur — le bouton "Recentrer" restait bloqué désactivé ; corrigé (`onReady={(fn) => setRecenter(() => fn)}`). (2) Les couches Mapbox (contour + masque) ne s'ajoutaient jamais : le code attendait un événement `load` qui avait déjà eu lieu avant que ces composants ne reçoivent `map` (contrat `MapCanvas`/`onMapReady`), et `isStyleLoaded()` peut renvoyer `false` juste après `load` (tuiles encore en chargement) — corrigé en ajoutant les sources/layers directement sans attendre un futur `load`. Un 3e problème (non un bug applicatif) : le masque en "polygone à trou" (anneau extérieur + trou GeoJSON standard) ne se rendait pas visuellement malgré une géométrie/état logique corrects (confirmé via `queryRenderedFeatures`) — remplacé par 4 rectangles simples entourant le contour (`buildMaskRects`), nettement plus robuste pour un layer `fill` Mapbox GL.
  Vérifié en navigateur (Playwright, compte admin, port 3011, token Mapbox public réel) : géocodage auto + fitBounds + contour rouge + masque assombri confirmés visuellement (captures comparées avant/après chaque correctif), bouton "Recentrer la carte" fonctionnel (y compris après zoom manuel), bouton Capturer toujours opérationnel (upload Storage réel inchangé), sous-étape Délimiter non régressée (tracé de polygone + calcul de surface toujours fonctionnels), test de bout en bout complet jusqu'à la création réelle d'un contrat (statut Actif, 2 factures générées). `tsc -b`, `npm run lint` propres à chaque étape. **Piège de outillage découvert pendant le débogage** : `page.locator('canvas').screenshot()` (Playwright) ne reflète pas fidèlement le contenu WebGL d'une carte Mapbox GL dans cet environnement (donnait un faux négatif "masque invisible" alors que `page.screenshot()` plein-page, au même instant, montrait le rendu correct) — toujours utiliser une capture plein-page (ou clip par coordonnées) pour vérifier visuellement une carte Mapbox, jamais `locator(canvas).screenshot()`.
  17 clients de test créés pendant cette session de débogage (dont 1 contrat + 2 factures + 1 zone pour le test de bout en bout final) tous supprimés après vérification (confirmé vide en base). Pas encore commité.

## Sprint 008.5 — Optimisation UX du Wizard, carte plein écran (branche `sprint008-analyse-propriete`, suite)
- [x] 2026-07-13 — Sprint purement UX/layout (`.input/sprint85`, aucune nouvelle fonctionnalité métier) sur l'étape "Analyse de la propriété" du Wizard Contrats : (1) en-tête de page du Wizard (titre "Nouveau contrat" + sous-titre) supprimé sur les 6 étapes — `WizardProgress` devient le premier élément sous le Breadcrumb (`WizardLayout.tsx` perd ses props `title`/`subtitle`) ; (2) la carte remplit désormais toute la hauteur disponible sans scroll (`PropertyMap.tsx` : `h-[480px]` fixe → `h-full`, chaîne `flex h-full`/`min-h-0` propagée dans `WizardStepProperty.tsx`/`PropertySubStepLocate.tsx`/`PropertySubStepDelineate.tsx`) ; (3) tous les boutons autrefois rendus dans la zone de carte (📸 Capturer, "Continuer vers le tracé"/"vers la validation") supprimés et regroupés dans un Footer unique via un mécanisme de remontée de navigation ("nav lifting") — décision validée avec l'utilisateur : étendu aux **3 sous-étapes** (Localiser/Délimiter/Valider), pas seulement Localiser (seul exemple concret du brief) ; (4) `WizardFooter.tsx` gagne un slot générique optionnel `action` (label/icon/onClick/disabled/isLoading), volontairement agnostique du métier pour être réutilisable par tout futur Wizard (Soumissions, Clients, Employés, Équipements) ; (5) `CaptureButton.tsx` (composant bouton, sprint008) transformé en hook `src/features/contracts/hooks/usePropertyCapture.ts` (même logique d'upload Storage réel) pour préparer une future capture automatique sans toucher à l'UI ; (6) `useMapboxMap.ts` gagne un `ResizeObserver` → `map.resize()`, nécessaire maintenant que la carte a une hauteur flexible (fenêtre redimensionnée = plus d'espace vide/débordement).
  **1 bug réel trouvé et corrigé pendant la vérification navigateur** (voir détail technique dans `memory/memory.md`) : boucle de rendu infinie ("Maximum update depth exceeded") causée par `goNext` (recréé à chaque rendu de `ContractWizard`) passé en dépendance d'un `useEffect` dans `WizardStepProperty` (sous-étape Valider) — chaque appel à `onNavChange` déclenchait un rendu, qui recréait `goNext`, qui redéclenchait l'effet, à l'infini. Corrigé en mémoïsant `goNext` via `useCallback`.
  Vérifié en navigateur (Playwright, compte admin, port 3011, token Mapbox public réel) : plus de titre "Nouveau contrat" nulle part, barre de progression bien sous le Breadcrumb sur les 6 étapes, carte occupant l'essentiel de la hauteur sans scroll vertical sur les 3 sous-étapes de Propriété, Footer affichant Annuler/Retour/Capturer/Suivant sur Localiser (Capturer absent sur Délimiter/Valider, comme attendu), navigation Suivant fonctionnelle à travers les 3 sous-étapes puis vers Services, resize de fenêtre (1440→1024→1920px) sans espace vide ni débordement sur la carte (masque/contour se réajustent correctement), régression : Services/Obligations/Paiement/Validation toujours fonctionnels sans header, test de bout en bout complet jusqu'à la création réelle d'un contrat (statut Actif, 2 factures). `tsc -b`/`npm run lint`/`npm run build` propres. 4 clients de test (+ 1 contrat/2 factures/1 zone pour le test final) créés pendant la vérification, tous supprimés après (confirmé vide en base, avec confirmation explicite de l'utilisateur). Pas encore commité (toujours sur la branche `sprint008-analyse-propriete`, sprint008 + sprint008.5 cumulés, aucun des deux commité).

## Tâche 3 — Cadrage carte + parité Localiser/Délimiter + noms de zone en liste déroulante (branche `sprint008-analyse-propriete`, suite)
- [x] 2026-07-13 — Retour utilisateur (`.input/tache3.md`) après sprint008/008.5 : (1) `MapViewportController.tsx` — `FIT_BOUNDS_PADDING` réduit de `60` à `20` (zoom plus serré autour du contour) ; (2) nouveau composant partagé `PropertyMapStage.tsx` (compose `PropertyMap`+`PropertyMaskLayer`+`PropertyBoundaryLayer`+`MapViewportController`+placeholder indisponible) réutilisé par **Localiser et Délimiter**, pour qu'elles affichent exactement la même vue (contour rouge + masque + même cadrage) — `WizardStepProperty.tsx` calcule désormais `boundary` une seule fois et le transmet aux deux sous-étapes (au lieu que Localiser le recalcule seule) ; (3) `PropertySubStepDelineate.tsx` entièrement réorganisé en 2 colonnes (panneau gauche/carte droite, identique à Localiser) — colonne gauche = nouveau `PropertyZonesPanel.tsx` (zones tracées + formulaire d'ajout) au lieu de l'ancienne mise en page verticale (Card d'instructions + carte + formulaires empilés) ; (4) nom de zone : champ texte libre → menu déroulant (`ZONE_NAME_OPTIONS` dans `constants/wizardOptions.ts` : Stationnement/Entrée/Trottoir/Autres, Stationnement par défaut) avec champ "Précisez le nom" additionnel (le menu reste visible, pas de remplacement) quand Autres est choisi — décision validée avec l'utilisateur.
  Vérifié en navigateur (Playwright, compte admin, port 3011, token Mapbox public réel) : Localiser visiblement plus zoomé qu'avant, Délimiter affichant exactement le même cadre/contour/masque que Localiser avec le panneau "Zones tracées" à gauche, menu déroulant fonctionnel (Stationnement par défaut, "Autres" révèle "Précisez le nom" sans faire disparaître le menu, zone ajoutée avec le bon nom résolu dans les 2 cas testés — tracé au clic + sélection dropdown simple), test de bout en bout complet jusqu'à la création réelle d'un contrat (statut Actif, 2 factures). `tsc -b`/`npm run lint`/`npm run build` propres, aucune erreur console. 2 clients de test (+1 contrat/2 factures/1 zone pour le test final) créés puis supprimés après vérification (confirmation utilisateur). Pas encore commité.

## Sprint 009 — Dessin des zones de déneigement & calcul des superficies (branche `sprint008-analyse-propriete`, suite)
- [x] 2026-07-13 — Implémentation complète (`.input/sprint009`) : déclenchement explicite du dessin (barre d'outils `ZoneToolbar` + Nouvelle zone/Modifier/Supprimer), 7 types de zone colorés (Entrée/Stationnement/Trottoir/Escaliers/Aire de manœuvre/Terrasse/Autre) via `ZoneNamingModal`+`ZoneTypeSelector`, édition des sommets d'une zone existante (recalcul de surface en direct, aucun bouton), zoom sur une zone, suppression réelle carte+liste (corrige un bug préexistant de désynchronisation), résumé par type (`ZoneAreaSummary`) + liste (`PolygonList`/`PolygonCard`, ce dernier aussi réutilisé par `SurfaceSummary`). Colonne `type` ajoutée à `contract_zones` (pas de nouvelle table `contract_areas`, décision validée avec l'utilisateur) via `supabase/migrations/20260713010000_contract_zones_type.sql`.
  `tsc -b`/`npm run lint`/`npm run build` propres. Vérifié en navigateur (Playwright, compte admin, port 3011, token Mapbox réel) : tous les critères d'acceptation UI confirmés (dessin non auto-actif, fenêtre de nommage + couleur par type sur carte et liste, Annuler retire le polygone, 3 zones de types différents avec total/sous-totaux corrects, glisser un sommet recalcule en direct, Supprimer retire carte+liste, Suivant gaté sur zéro zone). Détail technique complet (bug `userProperties: true` manquant sur Mapbox GL Draw, bug de désynchronisation Supprimer corrigé, thème Draw 7 couches) : `memory/memory.md`.
  **2026-07-13 (persistance confirmée)** : migration appliquée par l'utilisateur. Re-testé de bout en bout avec 2 contrats réels — 1 zone (`trottoir`) puis 5 zones couvrant les 5 autres types (Entrée/Stationnement/Escaliers/Aire de manœuvre/Terrasse) — sonde REST post-création confirme `type`/`label`/`surface_m2`/`ordre` corrects pour chaque ligne `contract_zones`, aucune erreur console. Données de test supprimées après vérification (2 clients, 2 contrats, 6 zones). **Sprint entièrement vérifié, y compris la persistance réelle.** Pas encore commité (accumulé avec sprint008/008.5/tâche3 sur la même branche).

## Sprint 012 — Expérience mobile du module Contrats (branche `sprint012-mobile-contrats`, depuis `sprint008-analyse-propriete` committé)
- [~] 2026-07-13 — `.input/tache4.md`. Refonte mobile "app native" (pas juste responsive) : Bottom Navigation + Header compact app-wide, Wizard/carte/bottom sheets dédiés pour Contrats. Décisions validées : (1) Bottom Nav/Header app-wide mais chrome seulement (contenu des 9 autres modules inchangé) ; (2) tablette hérite du Desktop ce sprint (pas de 3e expérience dédiée) ; (3) branche committée d'abord (`75adafe`) puis nouvelle branche depuis `sprint008-analyse-propriete` (PAS `main` — piège rencontré et corrigé en tout début d'implémentation, voir `memory/memory.md`). Plan détaillé complet (5 phases A-E) : `/root/.claude/plans/recupere-ta-nouvelle-tache-harmonic-patterson.md`.
  **Phase A terminée et vérifiée** (infra device-tier + `BottomSheet` + bascule nav app-wide) : `useDeviceTier.ts`, `BottomSheet.tsx` (glisser/snap peek-half-full via `motion`, poignée dédiée pour ne pas intercepter le scroll interne), `viewport-fit=cover` + fix zoom iOS 16px, `AppLayout.tsx` devient un dispatcher (`DesktopAppShell.tsx`/`MobileAppShell.tsx`), `navItems.ts` (extrait de `Sidebar.tsx`, partagé avec le nouveau menu mobile), `MobileBottomNavigation.tsx` (5 onglets + feuille "Menu"), `MobileHeader.tsx` + `useMobileHeaderActions.ts` (contexte actions principales). `tsc -b`/`npm run lint` propres. Vérifié en navigateur (Playwright, compte admin, port 3011, viewports 390/768/1280px) : Bottom Nav+Header sur tous les modules à 390px (contenu inchangé), tiroir Sidebar intact à 768/1280px (aucune régression), glisser-déposer de la feuille "Menu" testé (peek→full), route `/contracts/new` masque bien le Bottom Nav (`handle.hideMobileNav`) et affiche le bouton retour. **1 correctif appliqué pendant la vérification** : les racines de module (Leads/Clients/Contrats/Factures) affichaient à tort un bouton retour (réutilisation naïve du calcul `parent` du `Breadcrumb` desktop, qui a toujours un "parent" synthétique "Centre des opérations") — corrigé pour n'afficher retour que si ≥2 crumbs réels déclarés par les routes (vraie page imbriquée détail/nouveau), pas sur un onglet racine (paradigme bottom-tab natif).
  **Phase B terminée et vérifiée** (pages Contrats mobiles Liste + Détail) : `ContractsListPage.tsx`/`ContractDetailPage.tsx` deviennent des dispatchers triviaux (`tier==='mobile' ? Mobile... : Desktop...`), contenu desktop déplacé tel quel vers `pages/desktop/Desktop{ContractsList,ContractDetail}Page.tsx` (aucun changement de comportement), nouveaux `pages/mobile/Mobile{ContractsList,ContractDetail}Page.tsx` + `components/mobile/MobileContractLayout.tsx` (cadre `p-4 gap-4` + câblage `useMobileHeaderActions`). Liste mobile réutilise `ContractTable`/`Table` tel quel (vue carte déjà correcte <768px) — seul changement réel : bouton "Nouveau contrat" déplacé du header de page vers `useMobileHeaderActions` (icône `+`, 48px). Détail mobile : actions (Modifier/Statut ×4/Supprimer) condensées dans un menu `⋮` (`Dropdown` existant) faute de place dans le Header compact, Cards reprises telles quelles (déjà mono-colonne <768px). `tsc -b`/`npm run lint` propres. Vérifié en navigateur (Playwright, compte admin, port 3011, 390px) avec un contrat réel créé via le Wizard desktop (toujours utilisé tel quel à ce stade, Phase C pas encore faite) : liste avec bouton "+" et carte contrat visibles, détail avec bouton retour + menu `⋮` fonctionnel (Modifier/4 statuts/Supprimer tous présents). Client/contrat de test supprimés après vérification.
  **Phase C terminée et vérifiée** (`MobileWizard` + 5 étapes simples) : logique du Wizard extraite de `ContractWizard.tsx` vers `useContractWizardState.ts` (hook neutre, aucun JSX — `ContractWizard.tsx` desktop devient un simple consommateur, comportement 100% inchangé) ; nouveaux `src/components/layout/mobile/{MobileStepLayout,MobileWizard,FloatingActionBar}.tsx` (indicateur "Étape X/6" + barre de progression fine, transition horizontale via `motion` `AnimatePresence` 200ms, barre d'action flottante reprenant le type `WizardFooterAction`) ; `MobileContractWizard.tsx` (nouveau, `components/wizard/mobile/`) réutilise les 5 étapes simples telles quelles (`WizardStepClient/Services/Obligations/Payment/Validation`) — l'étape Property utilise temporairement `WizardStepProperty` desktop (placeholder visuel, Phase D la remplace). `ContractWizardPage.tsx` devient un dispatcher. `tsc -b`/`npm run lint`/`npm run build` propres.
  **1 piège eslint rencontré** : le pattern React classique "ajuster un state à partir d'un changement de prop" (détecter le sens du glissement entre étapes) via une `ref` mutée pendant le rendu est bloqué par la règle `react-hooks/refs` de ce projet (plus stricte que le `set-state-in-effect` déjà connu — interdit toute lecture/écriture de ref pendant le rendu, pas seulement un `setState` dans un effet) — corrigé en utilisant deux `useState` (valeur courante + valeur précédente) au lieu d'une ref, seule variante de ce pattern autorisée par la règle.
  Vérifié en navigateur (Playwright, compte admin, port 3011, 390px et 1280px) : les 6 étapes mobiles (transition glissée, indicateur, gating identique au desktop) parcourues de bout en bout jusqu'à la création réelle d'un contrat (avec une zone tracée sur la carte, malgré le layout desktop temporaire de l'étape Property) ; Retour de la barre d'action fonctionnel (glissement inverse) ; Wizard desktop re-testé à 1280px pour confirmer l'extraction `useContractWizardState` n'a rien cassé. Données de test supprimées après chaque vérification.
  **Phase D terminée et vérifiée** (étape Property mobile — carte plein écran + bottom sheets + outils flottants, la plus complexe) : extraction de deux hooks neutres supplémentaires (même principe que `useContractWizardState`) pour que Desktop/Mobile partagent la logique déjà débogée en sprint009/008 sans la dupliquer : `useDelineateState.ts` (dessin/nommage/édition/suppression/zoom de zone, extrait de `PropertySubStepDelineate.tsx`) et `usePropertyStepState.ts` (orchestration des 3 sous-étapes, extrait de `WizardStepProperty.tsx`) — les deux composants Desktop deviennent de simples consommateurs, JSX/comportement inchangés. `PropertyInfoPanel.tsx`/`PropertyZonesPanel.tsx` éclatés en contenu pur (`PropertyInfoContent.tsx`/`PropertyZonesContent.tsx`, sans le `Card`) + wrapper `Card` (Desktop, inchangé) — réutilisés par les nouveaux `PropertyInfoSheet.tsx`/`PropertyZonesSheet.tsx` (Mobile, dans un `BottomSheet`). Nouveaux composants mobile-only : `ZoneToolbarFloating.tsx` (pilule flottante 4 boutons : Tracer/Modifier/Supprimer/Recentrer, "Terminer" du brief = Suivant du footer, pas un 5e bouton), `ZoneDetailSheet.tsx` (tap une zone → nom/surface réels + Services/Notes/Photos/Priorité en placeholder "Bientôt disponible", décision de portée : pas d'extension du modèle de données `contract_zones` ce sprint), `MobilePropertySubStepLocate/Delineate.tsx` + `MobileWizardStepProperty.tsx` (branchés dans `MobileContractWizard.tsx`, rendu plein bord SANS `MobileStepLayout` — pas de padding ni d'indicateur d'étape par-dessus la carte, "aucune distraction" du brief). `ZoneNamingModal.tsx` réutilisé tel quel (déjà le bon pattern via `Modal`, pas de `BottomSheet`).
  **3 bugs réels trouvés et corrigés pendant la vérification navigateur** (détail technique complet dans `memory/memory.md`, à connaître pour tout futur `BottomSheet`) : (1) le portail `fixed` de `BottomSheet` se positionnait par-dessus la `FloatingActionBar` (barre d'action flottante), rendant "Capturer"/"Suivant" incliquables — corrigé avec `MobileFooterInsetContext` (nouveau, `src/components/layout/mobile/`) qui fait mesurer sa hauteur réelle par `MobileWizard.tsx` (`ResizeObserver`) et la transmet aux `BottomSheet` descendants pour qu'ils s'ancrent au-dessus, jamais par-dessus. (2) le conteneur "plein hauteur + transform" de la feuille (technique nécessaire pour l'animation de snap) interceptait quand même les clics dans sa zone visuellement "hors écran" — corrigé en ajoutant un conteneur de clip `overflow:hidden` autour, avec `pointer-events-none` sur ce conteneur et `pointer-events-auto` seulement sur la feuille elle-même. (3) le pattern `useDragControls`+`dragListener={false}`+`dragControls.start()` sur la poignée (pourtant l'approche standard documentée par Framer Motion) s'est avéré non fiable en test réel : un 2e glissement juste après un premier snap ne déclenchait plus jamais `onDragEnd` (cause exacte non élucidée malgré investigation approfondie) — remplacé par `drag="y"` standard sur toute la feuille + `onPointerDownCapture`+`stopPropagation()` sur le contenu scrollable interne pour ne pas intercepter son scroll (seule la poignée, non couverte par ce stop, déclenche réellement le glissement) — bien plus robuste, confirmé par des glissements répétés réussis en test réel.
  Vérifié en navigateur (Playwright, compte admin, port 3011, 390px, token Mapbox réel) : carte plein écran sans aucun panneau desktop visible, `PropertyInfoSheet`/`PropertyZonesSheet` fonctionnels (peek↔half↔full glissés avec succès, y compris glissements consécutifs), tracé de zone → fenêtre de nommage → zone dans la feuille, tap zone → `ZoneDetailSheet` avec nom/surface réels + 4 placeholders visibles, barre d'outils flottante (Nouvelle zone/Modifier/Supprimer/Recentrer) fonctionnelle y compris le mode édition (poignées de sommet visibles), création réelle de plusieurs contrats de bout en bout depuis un viewport mobile jusqu'à `ContractDetailPage` (mobile). `tsc -b`/`npm run lint`/`npm run build` propres à chaque itération.
  **Phase E terminée et vérifiée** (passe de régression finale) : smoke test mobile (375/390/414px) sur les 6 modules non touchés par ce sprint (Soumissions/Paiements/Routes/Équipements/Employés/Paramètres) — Header compact + Bottom Nav corrects partout, contenu de page inchangé. Re-vérification tablette (768px)/desktop (1280px) : tiroir Sidebar intact, Leads/Clients non régressés. `tsc -b`/`npm run lint`/`npm run build` propres. Toutes les données de test créées pendant les Phases A-E (une vingtaine de clients "Sprint012 Phase*"/"Debug*" + contrats/zones associés) supprimées via l'API REST après chaque vérification, base revérifiée vide de ces entités.
  **Sprint entièrement implémenté et vérifié (Phases A à E). Pas encore commité** (reste sur la branche `sprint012-mobile-contrats`, base = `sprint008-analyse-propriete` déjà committé séparément).

## Sprint 014 — Wizard Contrats V2 : 5 étapes, conformité Québec, brouillon (branche `sprint014-wizard-contrat-v2`, depuis `sprint013-design-system`)
- [x] 2026-07-14 — `.input/sprint014.md`. Wizard Contrats refondu de 6 à 5 étapes (Client & Propriété / Analyse & Zones / Services & Prix / Modalités & Obligations / Révision) + champs légaux (`mode_conclusion`, `plafond_saisonnier_cm`, `depot_neige`+`permis_municipal_obtenu`, `heure_premier_passage` réexposé) + 4 nouveaux blocs de clauses générées (Annulation/Prix/Exécution/Assurance, `generateClauses.ts` étendu — **à faire relire par un juriste**) + brouillon disponible dès l'étape 1 avec reprise complète éditable (`?draftId=`) + photos de propriété (`contract_photos`) + panneau "Récapitulatif du contrat" permanent (desktop). Décision utilisateur confirmée : reprise de brouillon = éditable complète (pas la V1 légère). Détail complet des 7 phases (A-G) et décisions d'architecture (upsert, soft-delete zones/photos, schéma brouillon allégé) : `memory/plans.md`.
  `tsc -b`/`npm run lint`/`npm run build` propres. Vérifié en navigateur (Playwright, compte admin, port 3011, 1280px+390px) de bout en bout avec un client réel jusqu'à la tentative de "Créer" (échoue proprement par toast — les 3 migrations ne sont pas encore appliquées, comportement attendu et confirmé non-bloquant/non-crashant). Non-régression confirmée sur un contrat existant et sur le module carte (tracé/capture/édition de zone inchangés). **Migrations à appliquer par l'utilisateur avant que la création réelle/le brouillon/la reprise soient testables de bout en bout.** Aucune donnée de test créée.

## Tâche 5 — Refonte Wizard Contrats (4 étapes, propriété optionnelle) + fiche Client + paramètres par défaut (branche `sprint014-wizard-contrat-v2`, suite)
- [x] 2026-07-15 — `.input/tache5.md`. Deux volets :
  1. **Module Clients** (`ClientForm.tsx`) : sélecteur Résidentiel/Commercial (défaut Résidentiel) remplace l'ancien champ libre "Type de client" — Commercial révèle Entreprise (1er champ) puis Nom/Prénom ; Téléphone/Courriel deviennent les 2 derniers champs ; Adresse passe à un nouveau composant générique `src/components/ui/AddressAutocomplete.tsx` (suggestions Mapbox à la frappe, façon Google Maps — net nouveau, aucun pattern existant à réutiliser) ; Latitude/Longitude/Notes retirés de l'UI mais gardés en base (toujours dans `ClientFormValues`/`defaultValues`, jamais rendus — round-trip silencieux en édition, remplis automatiquement par la sélection d'une suggestion d'adresse en création). `ClientSearchPicker.tsx` (réutilisé tel quel par le Wizard Contrats) affiche désormais un badge Résidentiel/Commercial à droite de "Client", l'adresse au format complet et le téléphone formaté (nouveaux `formatAddress`/`formatPhone` dans `src/lib/format.ts`).
  2. **Wizard Contrats** passe de 5 à 4 étapes (`STEP_ORDER` dans `useContractWizardState.ts` : `client/property/terms/review`, l'étape "Services" est supprimée) :
     - "Analyse & Zones" devient **optionnelle** : `AddressPreviewCard.tsx` (étape "Client & Propriété") est restylée en "Adresse Validée" (titre+icône verts, adresse complète en noir) et gagne un bouton "Outil de mesure" (icône règle) qui appelle `openPropertyAnalysis()` — sans clic, `goNext()`/`goBack()` sautent purement l'index `property` dans `STEP_ORDER` (nouveau helper `nextSkippingProperty`), jamais atteint par la navigation séquentielle. `contractCreationSchema` n'exige donc plus `zones.min(1)` pour "Créer".
     - `ContractBasicInfoFields.tsx` et `WizardStepServices.tsx` **supprimés** — tous leurs champs (`type`, `saison`, `dateSignature`, `dateDebut`, `dateFin`, `renouvellement`, `services`) sont soit dérivés (`type` ← `client.typeClient`), soit retirés (`dateSignature`/`renouvellement`, signature électronique = phase future), soit déplacés vers les **paramètres par défaut du Wizard** (voir plus bas). L'étape "Modalités & Obligations" est renommée **"Modalités de paiement"** (choix utilisateur confirmé) et perd tout le bloc Obligations visible (seuil/heure/dépôt de neige déplacés en paramètres ; accumulation max./plafond saisonnier/balises/entrée libre/animaux/portail/autres particularités **retirés complètement**, `ObligationsAnswers` réduit à 4 champs) — ne garde que Prix (déplacé depuis Services), Échéancier (option "Mensuel" retirée de `paymentPresets.ts`/`WizardStepTerms.tsx`), Mode de paiement, Modalités de paiement, Notes (déplacé depuis l'étape 1).
  3. **Nouveaux "paramètres par défaut du Wizard"** (`ContractWizardDefaults` : saison, dates début/fin, services actifs, seuil d'intervention, heure limite, dépôt de neige, **mode de conclusion**) — stockés dans une nouvelle colonne jsonb `settings.contract_wizard_defaults` (même pattern que `settings.modules`), lus/écrits depuis le module Contrats (`contractWizardSettings.service.ts`, pas depuis `features/settings/` — évite de faire fuiter des enums Contrats dans le module Settings générique). Nouvelle page admin-only `ContractWizardSettingsPage.tsx` (route `contracts/parametres`, `RequireRole roles={['administrateur']}` imbriqué sous `RequireModule moduleKey="contracts"`), ouverte via une icône ⚙️ en haut à droite de la liste Contrats (desktop + mobile, visible admin seulement). `contracts.service.ts` lit ces défauts à chaque sauvegarde/finalisation (`fetchContractWizardSettings`) plutôt qu'une seule fois côté formulaire, pour qu'un changement de défaut s'applique immédiatement aux prochains contrats.
  4. **Décision utilisateur sur "Mode de conclusion"** (clarifiée via question explicite, risque légal identifié) : retiré du Wizard mais **conservé comme paramètre par défaut** (pas totalement supprimé) — car il pilote la clause d'annulation générée (délais de rétractation OPC différents pour une vente itinérante/à distance) ; le supprimer sans alternative aurait figé la clause à "en personne" pour tout contrat, même conclu autrement.
  5. **Migration pendante éditée en place** (`20260714000000_contracts_legal_fields.sql`, sprint014, jamais appliquée) : colonne `plafond_saisonnier_cm` retirée avant même d'avoir existé en live (plus simple que l'ajouter puis la droper) ; `mode_conclusion`/`depot_neige`/`permis_municipal_obtenu` restent (toujours nécessaires, juste settings-driven). Nouvelle migration `20260715000000_settings_contract_wizard_defaults.sql` (colonne + seed des 8 valeurs par défaut demandées).
  `tsc -b`/`npm run lint`/`npm run build` propres. **Vérification initiale en navigateur** (Playwright, compte admin, port 3011, migrations pas encore appliquées) : fiche client, `ClientSearchPicker`, icône ⚙️ → page Paramètres (état d'erreur propre, pas de spinner infini), Wizard étape 1 "Adresse Validée" + "Outil de mesure", saut "Analyse & Zones" confirmé, étape Paiement sans bloc Obligations. **Commité (`db67964`) et poussé sur `origin/sprint014-wizard-contrat-v2`** le 2026-07-15 (regroupe sprint014 + tâche 5, jamais commités séparément — 61 fichiers). PR ouvrable : `github.com/groupe-reca/reca-app/pull/new/sprint014-wizard-contrat-v2`.
  **Clôture (2026-07-15, migrations appliquées par l'utilisateur)** : les 4 migrations (sprint014 ×3 + `contract_wizard_defaults`) confirmées appliquées par sonde REST directe (`mode_conclusion`/`depot_neige`/`permis_municipal_obtenu`/`clause_annulation` sur `contracts`, table `contract_photos`, `settings.assurance_police_no`, `settings.contract_wizard_defaults` avec les 8 valeurs seedées). **Test end-to-end réel complet** (Playwright, compte admin, port 3011, avec autorisation explicite de l'utilisateur pour créer des données de test réelles — une action de soumission de formulaire réel a été bloquée une première fois par le classificateur de sécurité auto-mode, résolu en demandant confirmation via `AskUserQuestion`) :
  1. Page Paramètres du Wizard charge les vraies valeurs seedées (Saison 2026-2027, 01/11/2026→01/05/2027, Déneigement coché, 5cm, 07:00 AM, Sur le terrain du client, En personne).
  2. Client créé (Résidentiel, adresse via suggestion Mapbox réelle, ville/code postal auto-remplis) → contrat créé via le Wizard **en sautant "Analyse & Zones"** (bouton "Outil de mesure" jamais cliqué) : étape Révision confirme "Aucune zone tracée", Services "Déneigement", Modalités d'exécution 5cm/07:00/sur le terrain — tous dérivés des paramètres par défaut, aucun saisi. Contrat créé (statut Actif), 1 facture générée, fiche détail confirme Type "Résidentiel" (dérivé du client), Mode de conclusion "En personne", Début/Fin 2026-11-01/2027-05-01 — tout correctement écrit depuis les défauts.
  3. Brouillon : "Enregistrer le brouillon" dès l'étape 1 fonctionne (badge Résidentiel confirmé visible dans `ClientSearchPicker`), contrat `en_attente` créé en base, **"Reprendre le brouillon"** depuis la fiche détail navigue vers `?draftId=` et réhydrate correctement le client/l'adresse.
  Toutes les données de test (1 client + 1 contrat + 1 facture pour le test de création ; 1 client + 1 contrat brouillon pour le test de reprise) supprimées via l'UI (icône corbeille, soft-delete) après vérification — confirmé vide en base par sonde REST (`deleted_at` renseigné, absent des listes actives). **Le Wizard V2 (sprint014 + tâche 5) est maintenant pleinement fonctionnel et vérifié de bout en bout en conditions réelles, migrations comprises.**

## Tâche 6 — Bug autocomplétion d'adresse fiche Client (branche `tache6-fix-adresse-autocomplete`, depuis `sprint014-wizard-contrat-v2`)
- [x] 2026-07-15 — `.input/tache6.md`. Sélectionner une suggestion d'adresse Mapbox dans `AddressAutocomplete.tsx` (composant générique, `src/components/ui/`, introduit tâche 5) déclenchait une 2e recherche au lieu de se figer : `onChange(suggestion.adresse)` dans `handleSelect` remonte dans la prop `value`, ce qui redéclenchait le `useEffect` de recherche (dépendance `[value]`) sur ce texte comme s'il s'agissait d'une frappe utilisateur, rouvrant le dropdown. Corrigé avec un ref `justSelectedRef` : mis à `true` dans `handleSelect` avant `onChange`, lu en tout premier dans le `useEffect` (skip + reset à `false` si vrai, saute le fetch) — la recherche ne se redéclenche que sur une vraie frappe utilisateur.
  `tsc -b`/`npm run lint` propres. Vérifié en navigateur (Playwright, compte admin, port 3011, token Mapbox réel) depuis le Wizard Contrats → "Ajouter un nouveau client" : sélection d'une suggestion remplit adresse/ville/code postal, dropdown fermé, 0 nouvelle requête réseau (confirmé sur 1.5s) ; re-test frappe manuelle ultérieure redéclenche normalement une vraie recherche (non-régression). Aucune donnée de test créée (formulaire jamais soumis). Détail complet : `memory/plans.md`. Pas encore commité (branche `tache6-fix-adresse-autocomplete`, créée pour cette tâche sur demande explicite de l'utilisateur).

## Tâche 12 — Cadrage carte (zoom/pan) préservé entre Localiser et Délimiter (branche `optimisation-outil-de-mesure`, depuis `tache6-fix-adresse-autocomplete`)
- [x] 2026-07-17 — Demande initiale de l'utilisateur ("tracer plus que 4 points") s'est révélée être un non-bug après investigation (Mapbox GL Draw en mode `draw_polygon` standard, aucune limite codée nulle part — confirmé par un agent Explore dédié) : l'utilisateur a confirmé lui-même que ça fonctionnait déjà. Vrai bug rapporté ensuite : zoomer/déplacer manuellement la carte satellite à l'étape "Localiser" avant de cliquer "Capturer" puis "Suivant" ne préservait pas ce cadrage à l'étape "Délimiter" — la carte revenait systématiquement au cadrage automatique par défaut (`fitBounds` sur le contour de démonstration).
  **Cause** : chaque sous-étape (Localiser/Délimiter, desktop et mobile) monte sa **propre** instance Mapbox GL (`PropertyMap.tsx`, toujours créée au même centre géocodé + `zoom=19.5` codé en dur) puis `MapViewportController.tsx` relance `fitBounds()` **inconditionnellement** à chaque montage. `usePropertyCapture.capture()` calculait déjà `viewport: {center, zoom}` au moment de la capture mais ce résultat était jeté (`onCaptured` ne remontait que le `storagePath`).
  **Fix** : le viewport capturé est désormais persisté dans `usePropertyStepState.ts` (nouveau state `viewport` + handler combiné `handleCaptured(path, viewport)`) et propagé en `initialViewport` à `PropertyMapStage.tsx`, qui l'utilise comme centre/zoom de création de la carte (`PropertyMap.tsx` gagne un prop `zoom` optionnel) et désactive le `fitBounds` automatique de `MapViewportController.tsx` (nouveau prop `autoFit`, révèle immédiatement le contour/masque sans attendre un `moveend` qui n'aura pas lieu). Le bouton "Recentrer la carte" reste toujours branché sur `fit()` peu importe `autoFit` — non affecté. Changement dupliqué à l'identique dans les variantes mobiles (`MobilePropertySubStepLocate.tsx`/`MobilePropertySubStepDelineate.tsx`/`MobileWizardStepProperty.tsx`), qui ne partagent pas de hook commun pour Locate (duplication délibérée déjà actée en sprint012). Aucune migration/schema requise (état de session UI, non persisté en base).
  `tsc -b`/`npm run lint` propres. Vérifié en navigateur (Playwright, compte admin, port 3011, token Mapbox réel, 1280px) : premier chargement de Localiser toujours auto-cadré (non-régression) ; zoom arrière + pan manuel puis "Capturer" puis "Suivant" → Délimiter s'ouvre exactement sur le cadrage custom (captures avant/après comparées, cadrage identique au pixel près) ; bouton "Recentrer la carte" toujours fonctionnel (retour au cadrage automatique du contour) ; aucune erreur console sur tout le parcours. 2 clients de test (ViewportTest Verify/NoCapture) créés puis supprimés via l'UI (icône corbeille + confirmation navigateur) après vérification, confirmé absents de la liste Clients. Committé (`045ae8d`).

- [x] 2026-07-17 (suite, même branche) — Après relecture attentive, l'utilisateur a identifié précisément le symptôme réel derrière son impression initiale de "limite à 4 points" (tâche précédente, refermée comme non-bug trop vite) : pendant le tracé actif d'un polygone, les points-sommets **disparaissent visuellement après 2-3 clics** alors que les segments/le remplissage continuent de s'afficher normalement, la forme complète (avec tous les sommets visibles) ne réapparaissant qu'une fois le tracé terminé. Confirmé en lisant `node_modules/@mapbox/mapbox-gl-draw/src/modes/draw_polygon.js` : c'est un comportement **documenté de la librairie elle-même** (pas un bug applicatif) — `toDisplayFeatures` du mode `draw_polygon` standard n'affiche jamais que 2 sommets pendant le tracé (le tout premier + l'avant-dernier commis), jamais tous les sommets intermédiaires.
  **Fix** : nouveau mode `draw_polygon` custom (`src/features/contracts/utils/drawPolygonAllVertices.ts`, `createDrawPolygonModeWithAllVertices()`) qui étend le mode standard (spread de `MapboxDraw.modes.draw_polygon`, comportement d'origine 100% intact) en ajoutant un point décoratif à CHAQUE sommet déjà commis. Point important pour la sécurité du fix : ces points décoratifs portent un `meta` custom (`'reca-historical-vertex'`, jamais `'vertex'`/`'feature'`) précisément pour ne PAS être hit-testés par les sélecteurs internes de Draw (`isVertex`/`clickOnVertex`) — sinon cliquer près d'un ancien sommet aurait terminé le polygone prématurément à cet endroit au lieu d'ajouter un nouveau point, ce qui aurait **recréé exactement le symptôme "impossible de tracer plus de N points"** de la tâche précédente. Branché dans `PolygonEditor.tsx` (option `modes: { ...MapboxDraw.modes, draw_polygon: createDrawPolygonModeWithAllVertices() }`) + 2 nouvelles couches de style dans `zoneDrawStyles.ts` (`gl-draw-historical-vertex-outer/inner`, même rendu visuel blanc+orange que les sommets standards).
  `tsc -b`/`npm run lint` propres. Vérifié en navigateur (Playwright, compte admin, port 3011, token Mapbox réel) : tracé d'un hexagone (6 points) — un point visible à CHAQUE sommet posé, capturé après le 3e et le 6e point (avant), confirmé par captures d'écran ; terminé au clavier (Entrée), forme correcte, surface calculée (405.10 m²) ; tracé d'un pentagone (5 points) → zone ajoutée avec succès (320.71 m²) ; non-régression du mode Modifier (`direct_select`, code non touché) : sélection de la zone, glissement d'un sommet, surface recalculée en direct (320.71 → 350.36 m²), aucune erreur console sur tout le parcours. 2 clients de test (VertexTest Verify/EditFlow) créés puis supprimés via l'UI après vérification, confirmé absents de la liste Clients. Pas encore commité.

## Tâche 1 (nom de fichier réutilisé une 5e fois) — Détection automatique du stationnement par IA (Gemini), branche `optimisation-outil-de-mesure`, suite
- [x] 2026-07-17 — `.input/tache1.md`. Ajout d'un bouton "Détection automatique" dans la barre d'outils de la sous-étape Délimiter (desktop `ZoneToolbar.tsx` + mobile `ZoneToolbarFloating.tsx`) : envoie l'image satellite déjà capturée à Gemini via une **nouvelle Edge Function Supabase** (`supabase/functions/analyze-satellite-image/`, première du repo — aucun précédent à `supabase/functions/`), reçoit des bounding boxes suggérées, les convertit en polygones éditables ajoutés directement sur la carte (mêmes outils Modifier/Supprimer que le tracé manuel, qui reste inchangé). Portée resserrée avec l'utilisateur par rapport au module fourni dans `.input/tache1.md` : **uniquement la détection de stationnement** (`type: 'stationnement'` fixe, pas de table de correspondance avec les 6 autres types de zone) et **aucune détection d'obstacles** (champ `obstacles` du module fourni abandonné) — seuls `qualite_image`/`raison_qualite` sont conservés (nécessaires pour masquer la suggestion si l'image est insuffisante). Aucune migration DB (`contract_zones.type = 'stationnement'` existe déjà depuis sprint009).
  Détail technique : `usePropertyStepState`/`useDelineateState` (déjà partagés Desktop/Mobile) reçoivent le `viewport` capturé (tâche 12) pour réaligner la caméra (`map.jumpTo`) avant de déprojeter les bounding boxes Gemini (normalisées 0-1000) via `map.unproject()` — point technique le plus délicat : diviser par `window.devicePixelRatio` avant `unproject()` (canvas en pixels *device*, `unproject()` attend des pixels *CSS*), sinon le polygone suggéré serait mal positionné sur écran Retina. `PolygonEditor.tsx` gagne `addSuggestedZone()` (ajoute directement une géométrie confirmée à Mapbox GL Draw, sans passer par la fenêtre de nommage manuelle — type/label déjà connus).
  `tsc -b`/`npm run lint`/`npm run build` propres.
  **Vérification en 2 temps** (l'utilisateur a déployé l'Edge Function + le secret `GEMINI_API_KEY` après une première passe) :
  1. Première passe (avant déploiement) : bouton visible/désactivé avant capture puis activé après, clic échoue proprement (Edge Function inexistante) sans crash, non-régression du tracé manuel confirmée.
  2. **3 bugs réels trouvés et corrigés pendant la vérification post-déploiement** (voir `memory/memory.md` pour le détail technique) : (1) CORS manquant sur l'Edge Function (le navigateur bloquait le préflight `OPTIONS`, aucune requête n'atteignait le code applicatif) — corrigé avec des `corsHeaders` explicites + handler `OPTIONS`. (2) `gemini-2.5-flash` renvoie 404 "no longer available to new users" malgré son apparition dans `models.list` — remplacé par l'alias `gemini-flash-latest` (confirmé fonctionnel par curl direct). (3) **Bug de state React** : ajouter plusieurs zones suggérées en boucle via `onAddZone` (une fermeture figée sur l'ancien tableau `zones` à chaque appel) faisait perdre toutes les zones sauf la dernière — 3 polygones bien tracés sur la carte mais 1 seule zone dans le formulaire. Corrigé avec un nouveau callback `onAddZones` (ajout groupé, une seule mise à jour d'état), threadé à travers `usePropertyStepState`→`WizardStepProperty`/`MobileWizardStepProperty`→`PropertySubStepDelineate`/`MobilePropertySubStepDelineate`→`useDelineateState`.
  Après ces 3 correctifs (2 redéploiements utilisateur requis entre-temps), **vérification complète réussie en conditions réelles** (Playwright, compte admin, port 3011, vraie clé Gemini) : détection de 3 zones de stationnement réelles sur une image satellite réelle (adresse "960 Rue Labelle"), polygones correctement positionnés visuellement sur la carte (captures avant/après), les 3 zones listées avec leurs surfaces individuelles ET le total dans le panneau "Zones tracées", zones portées correctement jusqu'à l'étape Révision (Récapitulatif du contrat), contrat créé avec succès (statut Actif, redirection vers la page de confirmation), zéro erreur console sur tout le parcours. 4 clients de test (+ 1 contrat/2 factures pour le test de création finale) créés pendant cette session de vérification, tous supprimés via l'UI après coup (dont 1 client + 1 facture orpheline oubliés lors d'un premier passage de nettoyage, retrouvés et supprimés lors d'une repasse de vérification finale — listes Clients/Contrats/Factures revérifiées vides de toute trace de test par capture d'écran).
  **Tâche entièrement implémentée et vérifiée de bout en bout, y compris avec la vraie clé Gemini déployée par l'utilisateur.** Pas encore commité.

- [x] 2026-07-17 (suite, même jour) — Retour utilisateur : le prompt Gemini devait (1) chercher d'abord les limites du terrain de la maison **au centre de l'image seulement** (ignorer les propriétés voisines) pour savoir où chercher, puis chercher le stationnement à l'intérieur de ce terrain repéré ; (2) tracer un point à **chaque coin réel** du stationnement même si sa forme n'est pas carrée/rectangulaire (L, trapèze, coin coupé...), pas juste une bounding box englobante. Changement de prompt seul insuffisant pour le point (2) — a nécessité de remplacer le format de réponse `bounding_box: [ymin,xmin,ymax,xmax]` par `contour: [y,x][]` (liste ordonnée de points, min 3) dans 4 fichiers : `supabase/functions/analyze-satellite-image/index.ts` (prompt + `GEMINI_RESPONSE_SCHEMA` + schéma Zod), `src/features/contracts/types/satelliteAnalysis.types.ts`, `src/features/contracts/utils/satelliteZoneProjection.ts` (`boundingBoxToPolygon` → `contourToPolygon`, mappe chaque point du contour au lieu de reconstruire un rectangle à partir de 2 coins), `src/features/contracts/hooks/useDelineateState.ts` (point d'appel). `tsc -b`/`npm run lint` propres. **Nécessite un nouveau `supabase functions deploy analyze-satellite-image` par l'utilisateur avant de pouvoir revérifier avec la vraie clé Gemini** (même contrainte que le reste de cette tâche — ce sandbox ne peut pas déployer). Pas encore vérifié en conditions réelles avec ce nouveau format, pas encore commité.
  **2026-07-17 (suite) — tentative de déploiement** : l'utilisateur a configuré le CLI Supabase (`npx supabase functions deploy analyze-satellite-image`) dans son propre terminal (pas de `SUPABASE_ACCESS_TOKEN` propagé jusqu'au sandbox, donc la commande échoue toujours de ce côté-ci avec `LegacyPlatformAuthRequiredError` — normal, projet bien lié via `supabase/.temp/linked-project.json` mais authentification absente ici). L'utilisateur a ensuite confirmé avoir redéployé lui-même. Une vérification Playwright de bout en bout a été lancée pour confirmer visuellement (contours non-rectangulaires + terrain voisin ignoré) mais **interrompue par une limite de session Claude avant d'avoir pu conclure** — à refaire à la prochaine session avant de considérer ce point comme vérifié.
- [x] 2026-07-17 (suite, 2e retour prompt le même jour) — Nouveau retour utilisateur : reformuler `SATELLITE_ANALYSIS_SYSTEM_PROMPT` pour refléter fidèlement « Tu es un estimateur en déneigement au Québec. Détecte uniquement les surfaces qui devront être déneigées par le tracteur. Exclus la maison, les pelouses, les arbres, les trottoirs publics et la rue. Retourne un polygone précis suivant les limites de la surface asphaltée ou bétonnée. » Prompt réécrit dans `supabase/functions/analyze-satellite-image/index.ts` avec cette persona + ce périmètre élargi (stationnement **et** entrée carrossable, plus seulement "stationnement"), liste d'exclusion alignée mot pour mot sur la demande (maison, pelouses, arbres, trottoirs publics, rue — "entrées piétonnes/allées" retiré de l'ancienne liste d'exclusion, une entrée carrossable doit désormais être détectée). Structure conservée (repérage du terrain d'abord, contour à N points, évaluation qualité, JSON only) — voir `memory/memory.md` pour le détail complet du changement et la nuance sur le `type` client toujours figé à `'stationnement'`. `tsc -b`/`npm run lint` propres. **Aucun changement de schéma cette fois** (toujours `contour`), donc pas de fichier client touché — seul `index.ts` a changé. Pas encore déployé par l'utilisateur avec cette version, pas encore vérifié en conditions réelles, pas encore commité.

## Tâche 13 — Test imagerie satellite MapTiler en remplacement de Mapbox satellite-v9 (branche `test-maptiler-satellite`, depuis `optimisation-outil-de-mesure`)
- [x] 2026-07-17 — L'utilisateur voulait d'abord tester l'imagerie Google Maps (clé Google Cloud fournie dans `.input/googlecloud`) comme source satellite alternative pour améliorer la détection Gemini — bloqué : `curl` direct confirme la clé valide mais "Maps Static API" non activée sur le projet Google Cloud (HTTP 403). Plutôt que d'attendre l'activation, test de MapTiler à la place (clé `BI53wMV7NZe8vk0DLbTn` fournie par l'utilisateur, testée par `curl` : tuiles satellite `api.maptiler.com/tiles/satellite-v2/{z}/{x}/{y}.jpg` répondent HTTP 200, `maxzoom: 22`). Remplacement direct (décision validée avec l'utilisateur, pas de bascule A/B à maintenir en code) : nouveau `src/lib/maptilerClient.ts` (miroir de `mapboxClient.ts`) + `useMapboxMap.ts` construit désormais un style Mapbox-Style-Spec inline (source raster MapTiler) au lieu de `mapbox://styles/mapbox/satellite-v9` quand `VITE_MAPTILER_TOKEN` est configuré (repli sur `satellite-v9` sinon, pour ne pas casser la carte sur un poste sans ce token). Le moteur Mapbox GL JS (dessin, géocodage, capture) reste inchangé — seule l'imagerie de fond change de fournisseur. `.env`/`.env.example` : ajout `VITE_MAPTILER_TOKEN`. `tsc -b`/`npm run lint` propres.
  **Vérification Playwright réussie** (nouveau serveur dev sur le port 3012 avec la variable chargée, port 3011 laissé intact pour ne pas perturber une session existante) : imagerie MapTiler s'affiche correctement sur Localiser ET Délimiter, requêtes réseau confirment 100% des tuiles satellite venant de `api.maptiler.com` (zéro requête raster vers `api.mapbox.com`, géocodage Mapbox intact comme attendu), zéro erreur console. Détection automatique Gemini testée : pipeline capture→Edge Function→Gemini fonctionne toujours de bout en bout (pas d'erreur réseau/HTTP).
  **Point à surveiller (pas un bug d'intégration)** : sur l'adresse de test utilisée (Trois-Rivières, zone rurale/exurbaine), l'imagerie MapTiler au zoom 20 est apparue **floue/pixélisée**, et Gemini a lui-même jugé l'image "insuffisante" pour cette raison (résolution trop basse pour distinguer les contours). `maxzoom: 22` dans le `tiles.json` de MapTiler ne garantit donc pas une résolution native fine partout — probablement une limite d'imagerie MapTiler propre à cette zone géographique précise, pas un problème de code. Données de test nettoyées après vérification.
  **2026-07-17 (conclusion, même jour) — rejeté** : l'utilisateur a confirmé que l'imagerie MapTiler était **encore pire** que Mapbox sur ses propres essais, pas seulement sur l'adresse rurale testée par Playwright. Décision : revenir à Mapbox. Changements de cette tâche entièrement défaits (aucun n'avait été commité) : `useMapboxMap.ts` restauré à l'état d'avant (style par défaut `mapbox://styles/mapbox/satellite-v9`, plus de logique MapTiler), `src/lib/maptilerClient.ts` supprimé, `VITE_MAPTILER_TOKEN` retiré de `.env`/`.env.example`. `tsc -b`/`npm run lint` propres après revert. **MapTiler à ne pas retenter comme source d'imagerie pour ce module sans nouvelle information** (voir `memory/memory.md`, section "Essayé et rejeté").

## Tâche 7 — Numéro civique manquant + téléphone obligatoire fiche Client (branche `tache6-fix-adresse-autocomplete`)
- [x] 2026-07-15 — `.input/tache7.md`. Trois correctifs sur la fiche "Nouveau client" :
  1. Le dropdown qui ne se refermait pas (tâche 6) est confirmé résolu, aucune action.
  2. Numéro civique manquant après sélection d'une suggestion Mapbox : l'API Mapbox Geocoding sépare le numéro civique (`feature.address`, ex. `"960"`) du nom de rue (`feature.text`, ex. `"Rue Labelle"`) pour les features de type `address` — `parseFeature()` dans `AddressAutocomplete.tsx` (composant générique, `src/components/ui/`) ne lisait que `feature.text`. Corrigé : `adresse = feature.address ? \`${feature.address} ${feature.text}\` : feature.text`. Vérifié par sonde réelle sur l'API Mapbox (curl) confirmant la séparation `text`/`address`, puis en navigateur (Playwright, compte admin, port 3011) : "960 Rue Labelle" → Adresse "960 Rue Labelle" / Ville "Saint-Jérôme" / Code postal "J7Z 5N1", conforme à l'exemple attendu de `.input/tache7.md`.
  3. Téléphone rendu obligatoire : `telephone: z.string().optional()` → `z.string().min(1, 'Le téléphone est requis')` dans `client.schema.ts`. Vérifié en navigateur : soumission sans téléphone bloquée, message "Le téléphone est requis" affiché.
  4. Champ "Province" caché du formulaire mais présent en base : ajout d'une colonne `province text not null default 'Qc'` sur `public.clients` via migration `supabase/migrations/20260715010000_clients_province.sql` (RECA n'opère qu'au Québec — valeur fixe au niveau DB, aucun champ de formulaire ni type applicatif touché, cohérent avec la portée minimale demandée). **Migration confirmée appliquée en live (2026-07-15)** — sonde REST directe (`GET /rest/v1/clients?select=id,province`) retourne 200.
  `tsc --noEmit` propre. Aucune donnée de test laissée en base (formulaire jamais soumis avec succès pendant les tests — le test téléphone s'arrête à l'erreur de validation). Pas encore commité.

## Tâche 11 — Montant des versements sur le contrat + prix/échéancier déplacés à l'étape 1 (branche `tache6-fix-adresse-autocomplete`)
- [x] 2026-07-16 — `.input/tache11.md`. Trois demandes sur le Wizard Contrats/prévisualisation, clarifiées avec l'utilisateur avant implémentation (3 questions posées) :
  1. **Montant du versement affiché sur le contrat** : `DocumentSummaryPayment.tsx` (prévisualisation "Contrat enregistré !") affichait "50%"/"$" brut pour chaque échéance (Premier/Second versement) — remplacé par le montant réel en dollars (décision utilisateur : remplace le %, ne l'affiche plus en plus). Même remplacement appliqué à `WizardStepValidation.tsx` (étape Révision du Wizard) pour cohérence, même s'il n'était pas explicitement nommé par le ticket. Nouvelle fonction pure partagée `computeInstallmentAmount` (`utils/paymentPresets.ts`, déplacée depuis `contracts.service.ts` où elle était privée et dupliquait la même formule que la génération de factures) — résout `%` × `contract.prix` (arrondi à 2 décimales) ou renvoie `montant` fixe tel quel.
  2. **Prix déplacé** de l'étape "Modalités de paiement" (page 3) vers l'étape "Client & Propriété" (page 1, `WizardStepClient.tsx`).
  3. **Échéancier simplifié et déplacé** : les 2 boutons preset (Annuel/Bi-paiement, renommés depuis "Paiement annuel"/"Deux versements") migrent aussi vers la page 1, avec surlignage du preset actif (`detectPreset`, nouveau — déduit le preset depuis les descriptions `modalitesPaiement` courantes, pas de nouveau champ de formulaire dédié). Décision utilisateur : le tableau détaillé d'édition manuelle par échéance (`PaymentScheduleBuilder.tsx`) est **retiré complètement** — l'échéancier n'est plus modifiable par contrat une fois le preset choisi. L'étape "Modalités de paiement" (page 3, renommée **"Paiement"** dans `STEP_LABELS`) ne garde donc que Mode de paiement + Notes + avertissements (`computeSoftWarnings`).
  4. **Date du 2e versement (Bi-paiement)** : n'est plus calculée à +3 mois de la date de début (`addMonths`, retiré) — nouveau champ `dateDeuxiemeVersement` (date fixe) ajouté à `ContractWizardDefaults`/`contractWizardDefaultsSchema`/`ContractWizardDefaultsForm.tsx` (page admin `/contracts/parametres`), même pattern jsonb `settings.contract_wizard_defaults` que les 8 valeurs existantes — **aucune migration SQL nécessaire** (merge `{...DEFAULT, ...row}` déjà en place, valeur par défaut `2027-02-01` servie tant que l'admin n'a pas encore sauvegardé le nouveau champ).
  5. **Gating de l'étape 1** (décision utilisateur, 3e question) : `STEP_COMPLETION.client` (`useContractWizardState.ts`) exige désormais client **+ prix + échéancier** avant de pouvoir avancer (avant : client seul suffisait). `STEP_COMPLETION.terms` simplifié à `Boolean(modePaiement)` (prix/échéancier ne sont plus vérifiés ici, déjà garantis par l'étape 1).
  `WizardStepClient.tsx`/`MobileContractWizard.tsx`/`ContractWizard.tsx` mis à jour pour propager `control`/`register`/`errors`/`setValue` (nouveaux props de `WizardStepClient`, absents avant cette tâche).
  `tsc -b`/`npm run lint` propres. Vérifié en navigateur (Playwright, compte admin, port 3011) de bout en bout : page Paramètres du Wizard affiche le nouveau champ "Date du 2e versement (Bi-paiement)" (valeur par défaut 2027-02-01 visible sans sauvegarde) ; étape 1 du Wizard affiche Prix + Échéancier (Annuel/Bi-paiement) sous le sélecteur de client ; bouton "Suivant" confirmé désactivé tant que client+prix+échéancier ne sont pas tous renseignés, puis activé ; étape Révision affiche "Premier versement — 500,00 $"/"Second versement — 500,00 $" (montant, plus de %) avec la date fixe des paramètres (2027-02-01) ; contrat créé de bout en bout (prix 799,99 $, Bi-paiement) → page "Contrat enregistré !" confirme "PREMIER VERSEMENT 400,00 $ (2026-11-01)"/"SECOND VERSEMENT 400,00 $ (2027-02-01)" dans la section Paiement, plus aucune trace de "%". Aucune erreur console sur tout le parcours. 2 clients de test (Tache11 TestMontant, Tache11 Preview) + 1 contrat (CTR-000027) + 2 factures associées (FAC-000046/047) supprimés (soft-delete `deleted_at`) via l'API REST après vérification, confirmé absents des listes actives. Pas encore commité (accumulé avec tâches 6-10 sur la branche `tache6-fix-adresse-autocomplete`, toujours pas committée).

## Tâche 8 — Page "Contrat enregistré !" + prévisualisation HTML du futur PDF (branche `tache6-fix-adresse-autocomplete`)
- [x] 2026-07-15 — `.input/tache8.md` + mockup `.input/contract_design.png`. Le clic "Créer" (dernière étape du Wizard Contrats) ne retourne plus vers `/contracts/:id` avec un toast — il navigue vers une nouvelle page dédiée `/contracts/:id/created` (`ContractCreatedPage.tsx`) : icône de succès animée (`motion/react`, fade+y 250ms), 2 colonnes (prévisualisation HTML du futur contrat PDF à gauche, "Informations du contrat" à droite), 3 boutons (Fermer → `/contracts` ; Voir le contrat (PDF) et Envoyer le contrat → placeholders, toast "arrive au prochain sprint", aucune génération PDF/envoi réel ce sprint).
  - **Transport des données** : `navigate(path, { state })` depuis `useContractWizardState.handleCreate` (`{contract, client: selectedClient, zones: values.zones}`) — zéro fetch réseau sur le chemin nominal, le contrat fraîchement créé porte déjà tout (services/clauses/seuil/heure/dépôt/paiement, voir `toWizardRowInput`). Filet de sécurité si `location.state` absent (refresh/navigation directe) : refetch `useContract`/`useClient`, `zones: []` en dégradé (documenté par commentaire dans `ContractCreatedPage.tsx`).
  - **Nouveau dossier `src/features/contracts/components/contract-document/`** (9 fichiers) : prévisualisation HTML "façon future page PDF", composants **purs** (props uniquement, aucun hook/router à l'intérieur) pour rester réutilisables tel quel par un futur moteur PDF — `types.ts` (`ContractDocumentData`), `DocumentSectionHeader.tsx` (primitif `plain`/`navy`, les 2 seuls traitements d'en-tête du mockup), `DocumentHeader.tsx`, `DocumentServicesModalites.tsx`, `DocumentClientZones.tsx`, `DocumentClausesList.tsx` (8 clauses déjà persistées sur `contract.*`, pas de re-génération), `DocumentSummaryPayment.tsx`, `DocumentSignatures.tsx` (décoratif, pas de vraie capture de signature), `DocumentFooterBar.tsx`, `ContractDocumentPreview.tsx` (orchestrateur).
  - **`ContractSummaryStats.tsx`** (nouveau, `components/`) : petites cartes label/valeur pour les 8 stats de la colonne droite — `StatCard` (`src/components/ui/StatCard.tsx`) essayé en premier mais son style `text-h1` est pensé pour des KPI numériques courts, rend mal pour un nom/une adresse (testé en navigateur) → cartes custom à la place. `StatCard` reste donc **toujours non branché à une vraie page**.
  - **Piège CSS découvert et corrigé pendant la vérification** : `zoom` (utilisé pour la mise à ~70% de la prévisualisation) interagit mal avec le calcul des tracks CSS Grid — l'enfant zoomé continuait de "pousser" la largeur de sa colonne, débordant sur la colonne de droite. Remplacé par une mise en page sans scale littéral (le document prend simplement la largeur de sa colonne, `min-w-0` pour empêcher tout débordement de grille). De même, `lg:`/lignes de rupture internes au document basées sur `lg:` (1024px, viewport) créaient un chevauchement car la colonne réelle du document fait ~700px sur desktop — corrigé en utilisant `sm:` (640px) pour les grilles internes du document (couvre à la fois la colonne desktop ~700px et la tablette empilée pleine largeur, tout en repassant à 1 colonne en vrai mobile <640px). **Piège générique à retenir : les préfixes responsive Tailwind réagissent à la largeur du VIEWPORT, jamais à celle d'une colonne parente — un composant destiné à vivre dans une colonne plus étroite que le viewport doit choisir un seuil assez bas pour rester cohérent, pas le seuil "naturel" de la page qui le contient.**
  - **`getNextPaymentEntry` extrait en utilitaire pur** (`utils/paymentPresets.ts`) — calcul "prochaine échéance" déjà présent une fois (`ContractSummaryPanel.tsx`), réutilisé 2 fois de plus par cette tâche (`ContractSummaryStats.tsx`, `DocumentSummaryPayment.tsx`), `ContractSummaryPanel.tsx` mis à jour pour l'utiliser aussi (au lieu de dupliquer une 3e fois).
  - **Toasts de succès retirés** de `useCreateContractWithInvoices.ts` (le brief interdit explicitement le toast au succès, remplacé par la nouvelle page) — le toast d'échec partiel (facturation) est conservé.
  - **Site web/RBQ du pied de page** : tranché avec l'utilisateur — site web statique `groupereca.ca` codé en dur (vraie valeur, aucun champ `Settings` pour ça), n° de licence RBQ retiré entièrement (pas de vraie valeur disponible, pas de valeur fictive affichée).
  `tsc -b`/`npm run lint` propres. Vérifié en navigateur (Playwright, compte admin, port 3011, 3 largeurs 1440/834/390px, avec autorisation explicite de l'utilisateur pour créer des données de test réelles) : parcours complet du Wizard (client créé via adresse Mapbox réelle "960 Rue Labelle" → suggestion sélectionnée) → "Créer" → atterrit sur `/contracts/:id/created`, aucun toast, animation jouée, aucune erreur console ; prévisualisation fidèle au mockup (héro navy, services/modalités, client/zones, clauses, récap/paiement/citation, signatures, pied de page) ; les 3 boutons fonctionnent (Fermer → liste, les 2 placeholders → toast "Cette fonctionnalité arrive au prochain sprint.") ; responsive confirmé aux 3 largeurs (2 colonnes → empilé → 1 colonne mobile, boutons pleine largeur) ; chemin de secours (URL directe sans `location.state`) testé, refetch fonctionne, dégradation "Aucune zone tracée" gracieuse, pas de crash. Toutes les données de test (4 clients, 3 contrats) supprimées après vérification, listes revérifiées vides. Pas encore commité.

## Tâche 9 — Prévisualisation du contrat en pleine largeur (branche `tache6-fix-adresse-autocomplete`)
- [x] 2026-07-16 — `.input/tache9.md`. `ContractCreatedPage.tsx` (page "Contrat enregistré !", tâche 8) était scindée en 2 colonnes (`lg:grid-cols-[1fr_380px]`) : prévisualisation HTML à gauche, panneau "Informations du contrat" (`ContractSummaryStats.tsx`) à droite. Retiré sur demande explicite de l'utilisateur — la colonne droite disparaît complètement, la prévisualisation prend seule toute la largeur du conteneur de page (`max-w-[1100px]` inchangé). `ContractSummaryStats.tsx` supprimé (plus aucun appelant après ce retrait, confirmé par `grep`).
  `tsc -b`/`npm run lint` propres. Vérifié en navigateur (Playwright, compte admin, port 3011, contrat existant CTR-000005) : plus de colonne de droite, prévisualisation pleine largeur (grille interne Services/Modalités/Client sur 3 colonnes au lieu d'être compressée dans ~700px), reste du document (clauses, signatures, pied de page) et les 3 boutons d'action toujours corrects en bas de page, aucune erreur console, non-régression mobile (390px, empilement déjà géré par les classes existantes du document). Aucune donnée de test créée (contrat existant réutilisé pour la vérification). Pas encore commité.

## Tâche 10 — Clause Annulation retirée, taxes affichées, lien de paiement (branche `tache6-fix-adresse-autocomplete`)
- [x] 2026-07-16 — `.input/tache10.md`. Trois ajustements sur la prévisualisation HTML du contrat (`ContractDocumentPreview`, `ContractCreatedPage.tsx`) :
  1. `DocumentClausesList.tsx` : entrée "Annulation / résolution" (`contract.clauseAnnulation`) retirée du tableau `clauses` affiché — portée limitée à l'affichage, `generateClauses.ts`/colonne `clause_annulation` inchangés (même pattern "retiré de l'UI, gardé en base" que tâche 5/7).
  2. `DocumentSummaryPayment.tsx` (reçoit désormais aussi `settings` en plus de `contract`) + `ContractDocumentPreview.tsx` (prop ajoutée) : carte "Paiement" affiche maintenant Sous-total / TPS (taux réel `settings.taxes.tps`) / TVQ (taux réel `settings.taxes.tvq`) / Total, calculés sur `contract.prix` — remplace l'ancienne ligne unique "Prix total". Utilise les taux **configurables** de Paramètres (`settings.taxes`, en points de %), pas les constantes `TPS_RATE`/`TVQ_RATE` codées en dur dans `contracts.service.ts` (incohérence pré-existante entre les deux, non traitée ici — hors scope).
  3. `DocumentFooterBar.tsx` : ligne "Payer le contrat en ligne groupereca.ca/paiement" ajoutée au-dessus du bandeau contact existant (texte simple, pas de vrai lien — aucune fonctionnalité de paiement réelle ce sprint).
  `tsc -b`/`npm run lint` propres. Vérifié en navigateur (Playwright, compte admin, port 3011, contrat existant CTR-000023, prix 799,99 $) : clause Annulation absente de la liste des clauses, carte Paiement affiche Sous-total 799,99 $ / TPS (5%) 40,00 $ / TVQ (9.975%) 79,80 $ / Total 919,79 $ (cohérent avec les taux réels configurés en Paramètres), ligne "Payer le contrat en ligne groupereca.ca/paiement" visible en bas du document, aucune erreur console, non-régression mobile (390px, capture pleine page). Aucune donnée de test créée (contrat existant réutilisé). Pas encore commité.

## Sprint 013 — Design System, socle visuel Phase 1 (branche `sprint013-design-system`, depuis `sprint012-mobile-contrats`)
- [x] 2026-07-14 — `.input/sprint013.md`. Socle visuel commun (tokens + primitives `src/components/ui/`) dérivé de 3 maquettes (`.input/ui_dashboard.png`/`ui_mobile.png`/`ui_add_contract.png`), appliqué sur une seule page pilote (Dashboard). Ajout des tokens `--shadow-card`/`--shadow-floating` (absents avant, le vrai manque puisque rayons/badges satisfaisaient déjà la cible) ; `Card.tsx` perd sa bordure dure au profit de l'ombre douce + gagne `variant="clickable"`/`chevron` additifs ; `Badge.tsx` gagne `size="sm"|"md"` (logique couleur extraite dans `statusColors.ts` partagé) ; `Button.tsx` gagne `fullWidth` ; `Table.tsx` aligné sur le nouveau style Card (2 classes, fonctionnellement inchangé). 3 nouvelles primitives génériques : `StatCard.tsx`, `ListRow.tsx`, `Avatar.tsx`. `OperationsCenterPage.tsx` (dashboard, était un stub vide) réécrit avec données réelles (`useContracts`/`useClients`/`useInvoices`) : 3 StatCard + activité récente (5 contrats les plus récents) en ListRow.
  `tsc -b`/`npm run lint`/`npm run build` propres. Vérifié en navigateur (Playwright, compte admin, port 3011, 390/768/1280px) : Dashboard pilote conforme, non-régression Leads/Contrats (liste/détail desktop+mobile)/Dropdown/Modal/Toast. Décisions de portée consignées dans `memory/memory.md` pour sprint014 (Card clickable/chevron et Avatar construits mais non branchés hors pilote). Aucune donnée de test créée. Pas encore commité.

## Système d'index de fichiers (méthode "Signa" / réduction de tokens)
- [x] 2026-07-13 — Création de `memory/file-index.md` (table de référence module → fichiers réels, pour les 11 dossiers `src/features/*` + section "Partagé/transverse" : `src/components/layout`, `src/components/ui`, `src/hooks`, `src/layouts`, `src/routes`, `src/lib`) suite à `.input/tache2.md` (note méthode "Signa" sur la réduction de coûts en tokens Claude Code). `CLAUDE.md` mis à jour : section 4 étendue (4e fichier `memory/` documenté + intégré au protocole début/fin de tâche), nouvelle section 6 "Règles pour limiter l'exploration du repo" (ne pas parcourir tout le repo si le module est déjà cartographié, consulter l'index en premier réflexe, exception ponctuelle si fichier absent de l'index mais mise à jour obligatoire ensuite). Aucun changement de code applicatif — tâche 100% documentation/mémoire. Point noté : le module "Équipements" a pour dossier réel `src/features/equipments/` (pluriel), pas `equipment` — à corriger si un futur index/doc utilise le mauvais nom.

## Tâche 14 — Occlusion/confiance dans le prompt Gemini + sélecteur de modèle IA aux paramètres du Wizard (branche `optimisation-outil-de-mesure`)
- [x] 2026-07-17 — Retour utilisateur, 2 parties : (1) nouveau texte de prompt à refléter (« Tu es un estimateur en déneigement ayant 20 ans d'expérience au Québec. Si certaines parties du stationnement sont masquées par des véhicules, des arbres ou des ombres, estime raisonnablement leur prolongement en te basant sur la géométrie habituelle des entrées résidentielles. Retourne un polygone qui représente la surface normalement déneigée. Si tu n'es pas certain, indique un niveau de confiance. ») ; (2) ajout aux paramètres du module Contrats (`ContractWizardSettingsPage.tsx`) d'un sélecteur de modèle IA pour la détection automatique — Gemini 2.5 Flash (actif par défaut, celui déjà utilisé) et Gemini 2.5 Pro en 2e option.
  **Partie 1 (prompt)** : `SATELLITE_ANALYSIS_SYSTEM_PROMPT` (`supabase/functions/analyze-satellite-image/index.ts`) enrichi — persona "20 ans d'expérience", nouvelle étape 3 sur l'estimation raisonnable du prolongement d'une surface partiellement masquée (véhicule/arbre/ombre) en se basant sur la géométrie habituelle des entrées résidentielles, avec un garde-fou explicite (ne pas halluciner l'existence même d'une surface, seulement son contour sous occlusion). A nécessité un (petit) changement de schéma pour porter le niveau de confiance demandé : chaque zone gagne `confiance: 'haute'|'moyenne'|'faible'` (`GEMINI_RESPONSE_SCHEMA` + Zod + `SatelliteZoneDetection`). Côté client, `useDelineateState.ts` compte les zones à confiance non-haute et l'indique dans le toast de succès ("dont N à confirmer manuellement — possible occlusion") ; la confiance n'est pas persistée en base (pas demandé, aurait nécessité une migration `contract_zones`).
  **Partie 2 (sélecteur de modèle)** : nouveau champ `aiModel: 'flash'|'pro'` dans `ContractWizardDefaults` (défaut `'flash'`, stocké dans le même jsonb `settings.contract_wizard_defaults` — aucune migration nécessaire), exposé dans `ContractWizardDefaultsForm.tsx` via un nouveau `Select` ("Gemini 2.5 Flash"/"Gemini 2.5 Pro", `AI_MODEL_OPTIONS` dans `wizardOptions.ts`). Volontairement un identifiant abstrait (`'flash'|'pro'`) plutôt que le nom de modèle Google exact stocké tel quel : l'Edge Function mappe vers un alias `-latest` (`GEMINI_MODEL_BY_OPTION`, `gemini-flash-latest`/`gemini-pro-latest`) — même leçon que la tâche 1 (`gemini-2.5-flash` avait renvoyé 404 "no longer available to new users" malgré son apparition dans `models.list`). **`gemini-pro-latest` n'a jamais été testé réellement** (pas de clé Gemini disponible dans ce sandbox pour un `curl` de vérification, contrairement à MapTiler/Google Maps testés directement) — **à vérifier au premier essai réel de l'option "Pro" par l'utilisateur : si 404/erreur, chercher le bon alias Google pour Gemini Pro (`models.list` avec la vraie clé) et corriger `GEMINI_MODEL_BY_OPTION`.** `useDelineateState.ts` lit `useContractWizardDefaults()` pour transmettre `aiModel` à `analyzeSatelliteImage(storagePath, aiModel)` → Edge Function.
  `tsc -b`/`npm run lint`/`npm run build` propres. **Pas encore vérifié en conditions réelles** (nécessite un nouveau `supabase functions deploy analyze-satellite-image` par l'utilisateur, même contrainte que toutes les tâches touchant cette Edge Function). Pas encore commité.
  **2026-07-17 (suite, diagnostic post-déploiement)** : l'utilisateur a redéployé et rapporté un échec avec les 2 modèles. Diagnostic par Playwright (compte admin, port 3011, vraie clé Gemini déployée) : `POST .../analyze-satellite-image` renvoie 502 dans les 2 cas, mais avec des codes Gemini **différents** — `{"error":"Gemini API error: 503"}` pour Flash (service Gemini surchargé), `{"error":"Gemini API error: 429"}` pour Pro (quota dépassé). **Ni 404** — donc les deux alias `gemini-flash-latest`/`gemini-pro-latest` semblent bien valides, ce n'est probablement pas un bug de mapping de modèle mais une limite d'API externe (quota du tier de la clé `GEMINI_API_KEY`, ou surcharge transitoire du côté Google) — à confirmer/ajuster côté Google AI Studio/Cloud Console (quota, facturation) si ça persiste après un nouvel essai.
  **Amélioration apportée suite à ce diagnostic** (le toast générique "Impossible d'analyser l'image satellite." masquait ces causes très différentes) : `satelliteAnalysis.service.ts` extrait maintenant le vrai message d'erreur du corps de la réponse (`FunctionsHttpError.context.json()`) au lieu de toujours lever une erreur générique ; l'Edge Function renvoie un message distinct selon le code Gemini (`describeGeminiError()` — 429 "Quota Gemini dépassé", 503 "Service Gemini temporairement surchargé", sinon le code brut) ; `useDelineateState.ts` affiche ce message précis dans le toast au lieu du texte générique. `tsc -b`/`npm run lint` propres après ce correctif. **Nécessite un nouveau déploiement de l'Edge Function pour être actif**, pas encore revérifié après ce correctif, pas encore commité.
  **Observation secondaire hors périmètre notée par l'agent de test** (à investiguer plus tard si confirmé, pas traité ici) : à l'étape "Client & Propriété" du Wizard, cliquer "Suivant" sans passer par "Outil de mesure" semble sauter directement à l'étape "Paiement" en contournant "Analyse & Zones" — reproduit de façon systématique sur une session fraîche selon l'agent, non vérifié en détail par moi.

## Tâche 2 (nom de fichier réutilisé une 2e fois, contenu inédit) — Sélecteur Fournisseur IA (Google/TokenRouter) + implémentation TokenRouter, branche `optimisation-outil-de-mesure`
- [x] 2026-07-17 — `.input/tache2.md` (clé TokenRouter dans `.input/tokenrouter`). Prolonge directement la tâche 14 (sélecteur "Modèle IA" Flash/Pro) : les paramètres du Wizard (`ContractWizardSettingsPage.tsx` → `ContractWizardDefaultsForm.tsx`) gagnent un sélecteur **Fournisseur IA** (Google/TokenRouter) placé avant le sélecteur **Modèle**, ce dernier devenant dépendant du fournisseur (cascading select, `useWatch({ control, name: 'aiProvider' })` + un `useEffect` qui réinitialise `aiModel` au 1er modèle du nouveau fournisseur dès que la valeur courante n'appartient plus à sa liste — pattern à réutiliser pour tout futur sélecteur en cascade de ce genre dans le projet, aucun précédent exact avant celui-ci). `AiProvider = 'google'|'tokenrouter'` (nouveau) + `AiModel` étendu aux 3 identifiants TokenRouter (`contractWizardDefaults.types.ts`), `AI_MODEL_OPTIONS` (liste plate) remplacé par `AI_MODEL_OPTIONS_BY_PROVIDER` (`wizardOptions.ts`), schéma Zod avec un `.refine()` croisé (le modèle doit appartenir à la liste du fournisseur choisi, même esprit que le `.refine()` de `client.schema.ts`). Aucune migration SQL (nouveau champ `aiProvider` dans le même jsonb `settings.contract_wizard_defaults`, pattern déjà établi tâches 5/11/14).
  **Edge Function `analyze-satellite-image`** : branche sur `aiProvider` (défaut `'google'` si absent, rétrocompatible) — chemin Google inchangé (`GEMINI_API_KEY`, alias `-latest`, `?key=` en query string) ; nouveau chemin TokenRouter (`TOKENROUTER_API_KEY`, nouveau secret Supabase requis) qui pointe vers `https://api.tokenrouter.com/v1beta/models/{aiModel}:generateContent` avec `Authorization: Bearer` — `aiModel` transmis tel quel (pas de table d'alias comme pour Google). Le corps de requête `generateContent` (system_instruction/contents/generationConfig) est désormais construit par une fonction pure partagée (`buildGenerateContentRequestBody`) réutilisée par les 2 fournisseurs, la forme étant identique (TokenRouter est un miroir documenté de l'API Gemini).
  **Vérification par curl direct (clé réelle `.input/tokenrouter`, avant tout déploiement)**, confirmant/infirmant le risque signalé au moment du plan : `google/gemini-3.5-flash` accepte parfaitement `responseSchema`/`responseMimeType: application/json` (JSON structuré retourné correctement, testé avec un schéma factice) — **c'est le seul des 3 modèles TokenRouter demandés qui convient à cette fonctionnalité**. Les 2 modèles `-image` (`gemini-3.1-flash-lite-image`, `gemini-2.5-flash-image`) sont des modèles de **génération** d'image et rejettent la même requête avec `400 "The request is not supported by this model."` — confirmé, pas juste soupçonné. Les 3 restent proposés dans le sélecteur tels que demandés par l'utilisateur (pas retirés), mais seul le 3ᵉ fonctionnera réellement pour la détection automatique du stationnement — à garder en tête si l'utilisateur signale un échec après avoir choisi l'un des 2 modèles image.
  `tsc -b`/`npm run lint`/`npm run build` propres. **Vérifié en navigateur** (Playwright, compte admin, port 3011, `/contracts/parametres`) : Fournisseur IA affiche Google par défaut avec Flash/Pro ; bascule vers TokenRouter change bien la liste de modèles vers les 3 options TokenRouter (auto-sélectionne la 1ère, `google/gemini-3.1-flash-lite-image`) sans état incohérent ; sélection manuelle de `Gemini 3.5 Flash` + "Enregistrer" persiste réellement (sonde par rechargement de page : `aiProvider=tokenrouter`/`aiModel=google/gemini-3.5-flash` confirmés après reload) ; remis à Google/Flash (état par défaut d'avant la tâche) après vérification, sauvegarde confirmée par toast et rechargement. Zéro erreur console sur tout le parcours.
  **Déploiement requis avant test réel du chemin TokenRouter en conditions live** (même contrainte que toutes les tâches touchant cette Edge Function, hors de portée de ce sandbox) : l'utilisateur doit exécuter `supabase secrets set TOKENROUTER_API_KEY=sk-...` (valeur dans `.input/tokenrouter`) puis `supabase functions deploy analyze-satellite-image`. Le chemin Google n'a pas changé de comportement (non-régression attendue, pas re-testée en conditions live dans cette tâche). Pas encore commité (accumulé avec tâches 1/12/14 sur la branche `optimisation-outil-de-mesure`, aucune commitée).
  **2026-07-17 (suite, même jour) — test réel de bout en bout après déploiement par l'utilisateur (`google/gemini-3.1-flash-lite-image` uniquement, sur demande explicite)** : paramètres basculés sur TokenRouter/Gemini 3.1 Flash Lite (Image), un client de test créé avec une vraie adresse (960 Rue Labelle), Wizard jusqu'à Délimiter, capture satellite réelle, clic "Détection automatique". **Résultat conforme à la prédiction du test curl pré-déploiement** : l'Edge Function déployée répond `502` avec `{"error":"TokenRouter API error: 400"}`, le toast client affiche ce message exact (pas de générique), aucun crash, le tracé manuel reste disponible — confirme que (1) le déploiement + secret `TOKENROUTER_API_KEY` fonctionnent correctement de bout en bout, (2) le pipeline d'erreur (Edge Function → `satelliteAnalysis.service.ts` → toast) fonctionne pour TokenRouter comme pour Google, (3) `gemini-3.1-flash-lite-image` est bien confirmé **non fonctionnel pour cette fonctionnalité en conditions réelles**, pas seulement par curl. Client de test supprimé après vérification (confirmé absent de la recherche), paramètres remis à Google/Flash.
  **2026-07-17 (suite, même jour) — `google/gemini-3.5-flash` testé en conditions réelles sur demande explicite, résultat positif** : mêmes paramètres/parcours que le test Lite, avec le modèle `google/gemini-3.5-flash`. **Fonctionne de bout en bout** : Edge Function répond `200` avec une détection réelle (1 zone stationnement, contour à 4 points, `confiance: "moyenne"`, `qualite_image: "moyenne"` avec raison — occlusion arbres/ombres), zone ajoutée sur la carte (polygone orange bien positionné, 27.05 m²) et dans le panneau "Zones tracées", toast correct ("1 zone de stationnement suggérée, dont 1 à confirmer manuellement…"), zéro erreur console. **Point notable : nettement plus lent que Gemini direct — ~18s de bout en bout** (le premier essai avec un délai fixe de 10s a capturé le bouton encore en état de chargement, spinner visible, aucune réponse encore reçue ; il a fallu repasser sur un polling actif jusqu'à 60s pour capter la vraie réponse). À anticiper si l'utilisateur ou un futur test suppose un délai fixe court pour ce chemin. 2 clients de test créés pendant cette passe (un 1er tronqué par le délai de 10s trop court, jamais nettoyé avant relance ; le 2e complet) tous les deux supprimés après vérification (recherche confirmée vide). Paramètres remis à Google/Flash. **Les 3 modèles TokenRouter du sélecteur sont maintenant tous vérifiés en conditions réelles : `gemini-3.5-flash` fonctionne (lent), `gemini-3.1-flash-lite-image`/`gemini-2.5-flash-image` échouent systématiquement (modèles image, `400`).**

## Tâche 3 (nom de fichier réutilisé une 3e fois) — Ajout de `openai/gpt-5.4-mini` aux modèles TokenRouter, branche `optimisation-outil-de-mesure`
- [x] 2026-07-17 — `.input/tache3.md`. Ajoute `openai/gpt-5.4-mini` au sélecteur "Modèle" TokenRouter (tâche 2). Différence structurelle clé : ce modèle est appelé via l'**endpoint OpenAI-compatible** `/v1/chat/completions` de TokenRouter, pas l'endpoint Gemini-natif `/v1beta/models/{model}:generateContent` utilisé par les 3 modèles précédents. `AiModel` étendu, `AI_MODEL_OPTIONS_BY_PROVIDER.tokenrouter` gagne l'option ("GPT-5.4 Mini"). Edge Function `analyze-satellite-image` gagne un 2e style d'API (`isOpenAiCompatible`, dispatché sur le préfixe `openai/` du modèle) : URL `https://api.tokenrouter.com/v1/chat/completions`, corps `messages`/`response_format.json_schema` (nouveau `buildOpenAiChatCompletionsBody`/`OPENAI_RESPONSE_SCHEMA` — équivalent JSON Schema strict de `GEMINI_RESPONSE_SCHEMA`, types minuscules + `additionalProperties: false` + tous les champs dans `required` y compris `raison_qualite` nullable, contrainte du mode `strict` d'OpenAI), réponse parsée depuis `choices[0].message.content` (pas `candidates[0].content.parts[0].text`). Même secret `TOKENROUTER_API_KEY` (déjà ajouté tâche 2), pas de nouveau secret. Aucune migration SQL.
  **Vérifié par 4 tests curl directs avant implémentation** (clé réelle `.input/tokenrouter`) : texte simple ✓, JSON schema strict ✓, vision (`image_url` data URI) ✓, puis **test complet avec le vrai `SATELLITE_ANALYSIS_SYSTEM_PROMPT` + le schéma exact sur une vraie capture contenant de l'imagerie satellite** → `200`, JSON strictement conforme, 3 zones détectées avec contours plausibles. **Contrairement aux 2 modèles image de la tâche 2, `gpt-5.4-mini` est confirmé pleinement viable pour cette fonctionnalité.**
  `tsc -b`/`npm run lint`/`npm run build` propres (pas de Deno CLI dans ce sandbox pour un typecheck isolé de l'Edge Function — relecture manuelle du fichier à la place, comme pour toute tâche précédente touchant ce fichier). **Vérifié en navigateur (partiel — sélecteur seulement)** : `/contracts/parametres`, TokenRouter → "GPT-5.4 Mini" apparaît en 4e option, sélection + sauvegarde + rechargement confirment la persistance, zéro erreur console. Réglages remis à Google/Flash après vérification.
  **Test réel de détection via l'Edge Function pas encore possible** : le code déployé sur Supabase est toujours celui de la tâche 2 (ignore le style OpenAI) — nécessite un nouveau `supabase functions deploy analyze-satellite-image` par l'utilisateur avant de pouvoir tester "Détection automatique" avec ce modèle en conditions réelles, même contrainte que toutes les tâches précédentes touchant ce fichier. Détail complet du plan : `memory/plans.md`. Pas encore commité (accumulé avec tâches 1/2/12/14 sur la branche `optimisation-outil-de-mesure`).

## Tâche 4 (nom de fichier réutilisé une 7e fois, contenu inédit) — Refonte UX "Analyse & Zones" carte plein écran (branche `optimisation-outil-de-mesure`)
- [x] 2026-07-17 — `.input/tache4.md`. Refonte purement visuelle de l'étape "Analyse & Zones" (sous-étapes Localiser/Délimiter) du Wizard Contrats **Desktop uniquement** : la carte devient l'élément dominant de l'écran (bords à bords, jusqu'au footer), le panneau d'adresse (Localiser) et le panneau "Zones tracées" (Délimiter) deviennent des panneaux flottants par-dessus la carte au lieu d'une grille 30%/70%, et une nouvelle toolbar verticale flottante (Recentrer/Nouvelle zone/Modifier/Supprimer/Détection auto) remplace l'ancienne barre horizontale `ZoneToolbar.tsx` (supprimée, code mort). **Décision de portée validée pendant l'exploration (pas posée à l'utilisateur, déduite de `memory/`) : le Mobile n'est pas touché** — la version Mobile de cette étape (sprint012 Phase D, 2026-07-13) a déjà exactement cette architecture (carte plein écran + `BottomSheet` + toolbar flottante), la section "Responsive" du ticket décrit pour mobile ce qui existe déjà.
  Zéro changement fonctionnel (contrainte explicite du ticket) : Mapbox/Turf/calculs de surface/détection IA/tracé/édition/suppression de zone strictement inchangés — uniquement layout/positionnement. `WizardLayout.tsx` gagne une prop `fullBleedContent?` (nouveau mécanisme générique, réutilisable par tout futur Wizard) qui retire son padding/scroll par défaut ; `ContractWizard.tsx` l'active pour `activeStepId === 'property'` ; `WizardStepProperty.tsx` réintroduit le padding localement (bandeau de sous-étapes + wrapper padding pour la sous-étape Valider, qui reste une pile de `Card` classique — pas de carte à maximiser). `PropertySubStepLocate.tsx`/`PropertySubStepDelineate.tsx` branchent conditionnellement : layout flottant `relative`/`absolute` uniquement quand la carte est réellement disponible (`showMap`/`mapReady`) ; le repli manuel (token absent/carte indisponible) garde l'ancien layout en grille, inchangé. `Recentrer` câblé pour la première fois côté Desktop dans Délimiter (un vrai manque avant cette tâche — seul le Mobile l'avait). Nouveau `ZoneToolbarFloatingDesktop.tsx` autonome (ne réutilise pas `components/mobile/ZoneToolbarFloating.tsx`, léger doublon du sous-composant bouton icône assumé pour ne prendre aucun risque de régression sur le Mobile déjà vérifié bout-en-bout).
  `tsc -b`/`npm run lint`/`npm run build` propres. Vérifié en navigateur (Playwright, compte admin, port 3011, token Mapbox réel, 1400px) : Localiser affiche la carte plein cadre avec le panneau adresse flottant en haut à gauche (badge "Propriété localisée", Recentrer fonctionnel) ; Délimiter affiche la toolbar verticale flottante (Recentrer/Nouvelle zone/Modifier/Supprimer/Détection auto tous fonctionnels) et le panneau "Zones tracées" flottant en bas à droite, tracé d'un polygone réel confirmé (101.33 m², nommage puis affichage dans le panneau) ; footer bas (Annuler/Retour/Capturer/Suivant) inchangé et fonctionnel ; création réelle d'un contrat de bout en bout (en sautant Analyse & Zones, non-régression tâche 5) confirmée ; non-régression Mobile (390px, capture Locate+Délimiter identiques à avant, `BottomSheet`/toolbar horizontale intacts). Zéro erreur console sur tous les parcours. 3 clients de test (Tache4 FloatTest/EndToEnd/MobileCheck) + 1 contrat (CTR-000033) supprimés via l'UI après vérification (le contrat, orphelin après suppression du client comme déjà documenté pour ce cas, a nécessité une suppression séparée depuis sa fiche détail), listes Clients/Contrats revérifiées vides de toute trace de test. Pas encore commité (accumulé avec tâches 1/2/3/12/14 sur la branche `optimisation-outil-de-mesure`, aucune commitée).

## Tâche 5 (2e occurrence, nom de fichier réutilisé, contenu inédit) — Prix/Échéancier repartis vers l'étape Paiement (branche `optimisation-outil-de-mesure`)
- [x] 2026-07-17 — `.input/tache5.md` (fichier minuscule, 1 ligne : « Déplacer les champs Prix total et Échéancier dans l'étape Paiement »). Déplacement inverse exact de la tâche 11 (2026-07-16), qui avait mis Prix + Échéancier (sélecteur Annuel/Bi-paiement) dans l'étape "Client & Propriété" (`WizardStepClient.tsx`). Cette tâche les rapatrie dans l'étape "Paiement" (`WizardStepTerms.tsx`, en tête, avant Mode de paiement/Notes) :
  - `WizardStepClient.tsx` redevenu minimal (`ClientSearchPicker`+`AddressPreviewCard` seulement) — props `control`/`register`/`errors`/`setValue` retirées (plus aucun besoin, elles n'existaient d'ailleurs pas avant la tâche 11). `ContractWizard.tsx`/`MobileContractWizard.tsx` mis à jour pour ne plus les passer.
  - `WizardStepTerms.tsx` regagne `setValue` (le prop/type existait déjà dans les deux points d'appel depuis la tâche 11, jamais retiré même si inutilisé jusqu'ici — aucun changement de signature d'appel nécessaire côté `ContractWizard.tsx`/`MobileContractWizard.tsx`) + les blocs Prix (`Input`)/Échéancier (2 `Button` Annuel/Bi-paiement, `detectPreset`/`buildPaymentSchedule`) copiés tels quels depuis l'ancien `WizardStepClient.tsx`.
  - `useContractWizardState.ts` : `STEP_COMPLETION.client` revient à `Boolean(selectedClient)` seul ; `STEP_COMPLETION.terms` regagne `Boolean(prix) && modalitesPaiement.length > 0` en plus de `Boolean(modePaiement)`.
  `tsc -b`/`npm run lint` propres. Vérifié en navigateur (Playwright, compte admin, port 3011) : étape "Client & Propriété" sans aucun champ Prix/Échéancier, "Suivant" actif dès le client sélectionné (avant : bloqué tant que prix+échéancier absents) ; étape "Paiement" affiche Prix/Échéancier/Mode de paiement/Notes dans cet ordre, "Suivant" bloqué tant que les 3 (prix/échéancier/mode) ne sont pas renseignés puis débloqué ; création réelle d'un contrat de bout en bout (1234,56 $, Bi-paiement, échéance 2026-11-01 confirmée dans le récapitulatif et après création) ; zéro erreur console. 1 client + 1 contrat de test supprimés via l'UI après vérification. Pas encore commité (accumulé avec tâches 1/2/3/4/12/14 sur la branche `optimisation-outil-de-mesure`, aucune commitée).

## Tâches hors-module
- [x] 2026-07-11 — Ajout de la section "Système de mémoire" au `CLAUDE.md` racine (créé, n'existait pas avant).
- [x] 2026-07-11 — Renommage `mempry/` → `memory/` et initialisation des 3 fichiers avec le contexte réel du projet.
- [x] 2026-07-12 — Rework module Contrats : fiche de création complète (clauses éditables, recherche/création client inline, échéancier de paiement → génération de factures). Migration `20260712000000_contracts_clauses_and_schedule.sql` appliquée par l'utilisateur à la base live. Test de bout en bout réel confirmé : contrat créé (statut Actif), 2 modalités 50/50 → 2 factures générées automatiquement (574,88 $ chacune, statut Brouillon), client créé et bien lié. Les 3 critères de vérification de `.input/tache2.md` sont validés. Données de test nettoyées (client/contrat/factures du test final supprimés ; 2 clients "Jean Test"/"Jean Test2" d'un test précédent laissés à l'utilisateur). Pas encore commité. Détail complet du plan : `/root/.claude/plans/robust-fluttering-dahl.md`.
