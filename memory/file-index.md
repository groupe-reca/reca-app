# Index de fichiers par module

Table de référence : quels fichiers appartiennent à quel module. À consulter EN PREMIER pour toute tâche touchant un module existant, avant toute exploration/grep du repo (voir CLAUDE.md §6).

Convention de chaque module : `types/`, `schemas/`, `services/`, `hooks/`, `components/`, `pages/` (tous les dossiers ne sont pas toujours présents).

## auth
- types: `src/features/auth/types/auth.types.ts`
- schemas: `src/features/auth/schemas/login.schema.ts`
- services: `src/features/auth/services/auth.service.ts`
- hooks: `src/features/auth/hooks/useLogin.ts`, `useLogout.ts`, `useSession.ts`
- components: `src/features/auth/components/LoginForm.tsx`, `RequireAuth.tsx`
- pages: `src/features/auth/pages/LoginPage.tsx`

## leads
- types: `src/features/leads/types/lead.types.ts`
- schemas: `src/features/leads/schemas/lead.schema.ts`
- services: `src/features/leads/services/leads.service.ts`
- hooks: `src/features/leads/hooks/leadKeys.ts`, `useCreateLead.ts`, `useDeleteLead.ts`, `useLeads.ts`, `useLead.ts`, `useScheduleReminder.ts`, `useUpdateLeadStatus.ts`, `useUpdateLead.ts`
- components: `src/features/leads/components/LeadFormModal.tsx`, `LeadForm.tsx`, `LeadReminderCard.tsx`, `LeadStatusBadge.tsx`, `LeadTable.tsx`
- pages: `src/features/leads/pages/LeadCreatePage.tsx`, `LeadDetailPage.tsx`, `LeadsListPage.tsx`

## quotes
- types: `src/features/quotes/types/quote.types.ts`
- schemas: `src/features/quotes/schemas/quote.schema.ts`
- services: `src/features/quotes/services/quotes.service.ts`
- hooks: `src/features/quotes/hooks/quoteKeys.ts`, `useConvertQuoteToClient.ts`, `useCreateQuote.ts`, `useDeleteQuote.ts`, `useQuotes.ts`, `useQuote.ts`, `useUpdateQuoteStatus.ts`, `useUpdateQuote.ts`
- components: `src/features/quotes/components/QuoteFormModal.tsx`, `QuoteForm.tsx`, `QuoteStatusBadge.tsx`, `QuoteTable.tsx`
- pages: `src/features/quotes/pages/QuoteCreatePage.tsx`, `QuoteDetailPage.tsx`, `QuotesListPage.tsx`

## clients
- types: `src/features/clients/types/client.types.ts`
- schemas: `src/features/clients/schemas/client.schema.ts` (tâche 5 — `CLIENT_TYPES`/`ClientType` enum `residentiel`|`commercial` remplace le `typeClient` free-text, `entreprise` requis si commercial via `.refine()` ; tâche 7 — `telephone` obligatoire, plus `.optional()`)
- services: `src/features/clients/services/clients.service.ts`
- hooks: `src/features/clients/hooks/clientKeys.ts`, `useClients.ts`, `useClient.ts`, `useCreateClient.ts`, `useDeleteClient.ts`, `useUpdateClient.ts`
- components: `src/features/clients/components/ClientFormModal.tsx`, `ClientForm.tsx` (tâche 5 — sélecteur Résidentiel/Commercial en haut, Entreprise conditionnelle, adresse via `AddressAutocomplete`, latitude/longitude/notes gardés en `defaultValues` mais plus rendus), `ClientSearchPicker.tsx` (tâche 5 — badge type + adresse/téléphone formatés dans la vue "client sélectionné", réutilisé tel quel par le Wizard Contrats), `ClientTable.tsx`
- pages: `src/features/clients/pages/ClientCreatePage.tsx`, `ClientDetailPage.tsx`, `ClientsListPage.tsx`

## contracts
- types: `src/features/contracts/types/contract.types.ts` (tâche 5 — `ObligationsAnswers` réduit à 4 champs : `seuilDeclenchementCm`/`heurePremierPassage`/`depotNeige`/`permisMunicipalObtenu`, `plafond_saisonnier_cm`/`plafondSaisonnierCm` retirés ; `MODE_CONCLUSION`/`DEPOT_NEIGE` inchangés ; **tâche 6 — `PRIX_TAXES_MODES`/`PrixTaxesMode` ajoutés (base commune, `contractWizardDefaults.types.ts` importe depuis ici) ; `ContractRow.prix_taxes`/`Contract.prixTaxes` ajoutés**), `contractWizardDefaults.types.ts` (tâche 5, étendu tâche 11 — `ContractWizardDefaults`/`DEFAULT_CONTRACT_WIZARD_DEFAULTS`, paramètres par défaut du Wizard, gagne `dateDeuxiemeVersement` : date fixe du 2e versement Bi-paiement, remplace le calcul +3 mois ; tâche 14 — `AiModel = 'flash'|'pro'`, champ `aiModel` ajouté, défaut `'flash'` ; tâche 2 — `AiProvider = 'google'|'tokenrouter'` ajouté, champ `aiProvider` ajouté à `ContractWizardDefaults` (défaut `'google'`), `AiModel` étendu aux 3 identifiants TokenRouter ; tâche 3 — `AiModel` gagne `'openai/gpt-5.4-mini'`, 1er modèle TokenRouter de style OpenAI-compatible ; **tâche 6 — `prixTaxes: PrixTaxesMode` ajouté, défaut `'avant_taxes'`**), `satelliteAnalysis.types.ts` (nouveau, tâche 1 — `SatelliteAnalysisResult`/`SatelliteZoneDetection`, miroir TS de la réponse déjà validée par l'Edge Function `analyze-satellite-image`, pas de revalidation Zod côté client ; tâche 14 — `SatelliteZoneDetection` gagne `confiance: 'haute'|'moyenne'|'faible'`)
- schemas: `src/features/contracts/schemas/contractCreation.schema.ts` (tâche 5 — `contractFieldsShape` réduit à `adresseGeocodee/latitude/longitude/zones/photos/prix/modePaiement/modalitesPaiement/notes` ; `obligationsAnswersSchema`/`serviceEntrySchema` retirés, plus aucun champ `type`/`modeConclusion`/`saison`/dates/`renouvellement`/`services`/`obligations` — tous dérivés du client ou des paramètres par défaut), `contract.schema.ts` (formulaire d'édition post-création, indépendant, non touché par la tâche 5), `contractWizardDefaults.schema.ts` (tâche 5, étendu tâche 11 — `dateDeuxiemeVersement` requis ; tâche 14 — `aiModel` ajouté ; tâche 2 — `aiProvider` ajouté, `aiModel` desserré en `z.string()` (couvre les identifiants TokenRouter) + `.refine()` croisé vérifiant que `aiModel` appartient à la liste `AI_MODEL_OPTIONS_BY_PROVIDER[aiProvider]` ; tâche 6 — `prixTaxes` ajouté)
- services: `src/features/contracts/services/contracts.service.ts` (tâche 5 — `fetchContractWizardSettings` remplace `fetchAssurancePoliceNo`, lit aussi `contract_wizard_defaults` ; `toWizardRowInput`/`upsertContractWithZones`/`createContractWithZones`/`saveContractDraft` prennent un `clientTypeLabel` en paramètre et construisent `services`/`obligations`/`saison`/dates entièrement depuis les défauts, plus depuis le formulaire ; tâche 11 — `computeInstallmentAmount` déplacé vers `utils/paymentPresets.ts`, importé plutôt que défini localement ; **tâche 6 — nouvelle fonction pure `computeInstallmentTaxes(amount, prixTaxes)`, branche le calcul Sous-total/TPS/TVQ/Total selon `contract.prixTaxes` (avant/après taxes), utilisée par `generateInvoicesFromSchedule` ; `toWizardRowInput` écrit `prix_taxes: defaults.prixTaxes` ; `mapContract` lit `row.prix_taxes`**), `geocoding.service.ts`, `contractWizardSettings.service.ts` (nouveau, tâche 5 — `getContractWizardDefaults`/`updateContractWizardDefaults`, interroge `settings` directement sans dépendre de `features/settings/`), `satelliteAnalysis.service.ts` (nouveau, tâche 1 — `analyzeSatelliteImage(storagePath)`, appelle l'Edge Function `analyze-satellite-image` via `supabase.functions.invoke` ; tâche 14 — prend aussi `aiModel: AiModel`, transmis dans le corps de la requête ; tâche 2 — prend aussi `aiProvider: AiProvider`)
- hooks: `src/features/contracts/hooks/contractKeys.ts` (tâche 5 — `wizardDefaults()` ajouté), `useClientContracts.ts`, `useContracts.ts`, `useContract.ts`, `useCreateContractWithInvoices.ts` (tâche 5 — accepte `clientTypeLabel`), `useSaveContractDraft.ts` (tâche 5 — accepte `clientTypeLabel`), `useContractWizardDefaults.ts`/`useUpdateContractWizardDefaults.ts` (nouveaux, tâche 5), `useContractDraft.ts` (sprint014 — `useQueries` contrat+zones+photos pour la reprise), `useDeleteContract.ts`, `useUpdateContractStatus.ts`, `useUpdateContract.ts`, `usePropertyCapture.ts`, `usePropertyPhotos.ts` (sprint014 — upload photos), `useZoneTypeSelection.ts` (sprint009), `useDelineateState.ts` (sprint012 ; tâche 1 — accepte `viewport`/`onAddZones`, expose `isAnalyzing`/`handleAutoDetect` : détection automatique du stationnement via Gemini, réaligne la caméra sur `viewport`, déprojette les contours reçus puis ajoute toutes les zones **en un seul appel** à `onAddZones` — jamais en bouclant sur `onAddZone`, qui perdrait tout sauf la dernière zone, fermeture React figée oblige ; tâche 14 — lit `useContractWizardDefaults()` pour transmettre `aiModel` à `analyzeSatelliteImage`, toast de succès mentionne le nombre de zones à confiance non-haute ; tâche 7 — `onNavChange` transmet aussi `immersive: mapReady`, consommé uniquement côté Desktop ; **tâche 8 — accepte aussi `contractId`/`boundary`/`onCaptured`, instancie `usePropertyCapture` en interne, nouveau `handleContinueWithCapture` (remplace `onContinue` direct dans `onNavChange.onNext`) : recapture la carte avec les zones visibles en quittant Délimiter, réassigne `imageStoragePath` de toutes les zones à cette nouvelle capture**), `usePropertyStepState.ts` (sprint012, étendu sprint014 avec `photos`/`addPhoto`/`removePhoto` ; tâche 12 — `viewport`/`handleCaptured` : persiste le cadrage capturé entre sous-étapes ; tâche 1 — `addZones(zones[])` : ajout groupé en une seule mise à jour d'état, à utiliser pour tout futur ajout de plusieurs zones à la fois ; tâche 2 — `useDelineateState.ts` transmet aussi `wizardDefaults?.aiProvider` à `analyzeSatelliteImage`)
- components: `ContractFormModal.tsx`, `ContractForm.tsx`, `ContractStatusBadge.tsx`, `ContractTable.tsx`, `TextareaField.tsx`, `ContractWizardDefaultsForm.tsx` (tâche 11 — gagne le champ "Date du 2e versement (Bi-paiement)" ; tâche 14 — gagne le `Select` "Modèle IA (détection automatique du stationnement)", Flash/Pro ; tâche 2 — sélecteur "Modèle" devient un sélecteur en **cascade** sous un nouveau `Select` "Fournisseur IA" (Google/TokenRouter, `AI_PROVIDER_OPTIONS`) — liste de modèles dérivée de `AI_MODEL_OPTIONS_BY_PROVIDER[aiProvider]` via `useWatch({ control, name: 'aiProvider' })`, `aiModel` réinitialisé au 1er choix valide par un `useEffect` dès qu'il sort de la liste courante ; premier sélecteur en cascade du projet, pattern réutilisable ; **tâche 6 — gagne le `Select` "Le prix du contrat est" (`PRIX_TAXES_OPTIONS`, avant/après taxes)**). **`ContractBasicInfoFields.tsx` supprimé (tâche 5)** — plus aucun champ à porter, tous dérivés/déplacés. **`ContractSummaryStats.tsx` supprimé (tâche 9)** — colonne droite "Informations du contrat" retirée de `ContractCreatedPage`, code mort après ce retrait. **`PaymentScheduleBuilder.tsx` supprimé (tâche 11)** — l'échéancier n'est plus modifiable échéance par échéance, uniquement via le sélecteur Annuel/Bi-paiement de `WizardStepClient.tsx`.
- components/contract-document (nouveau, tâche 8 (2026-07-15) — prévisualisation HTML "façon future page PDF" affichée sur `ContractCreatedPage`, composants **purs**, aucun hook/router à l'intérieur, réutilisables tels quels par un futur moteur PDF) : `types.ts` (`ContractDocumentData` ; **tâche 8 (2026-07-17, 2e occurrence) — gagne `imageUrl: string | null`**), `DocumentSectionHeader.tsx` (primitif d'en-tête `plain`/`navy`), `DocumentHeader.tsx`, `DocumentServicesModalites.tsx`, `DocumentClientZones.tsx`, `DocumentSummaryPayment.tsx` (tâche 10 — reçoit aussi `settings`, affiche Sous-total/TPS/TVQ/Total calculés depuis `settings.taxes` ; tâche 11 — montant de chaque versement affiché via `computeInstallmentAmount` au lieu du `%` brut ; **tâche 6 — Sous-total/Total branchés sur `contract.prixTaxes` (avant/après taxes), même logique que `computeInstallmentTaxes` de `contracts.service.ts`**), `DocumentSignatures.tsx`, `DocumentFooterBar.tsx` (tâche 10 — ligne "Payer le contrat en ligne groupereca.ca/paiement" ajoutée, texte simple sans lien réel), `ContractDocumentPreview.tsx` (orchestrateur racine, tâche 10 — passe `settings` à `DocumentSummaryPayment` ; **tâche 8 (2e occurrence) — `DocumentClausesList` remplacé par `DocumentSatelliteZones`**), `DocumentSatelliteZones.tsx` (nouveau, **tâche 8 (2e occurrence)** — photo satellite avec zones tracées visibles dessus + légende (réutilise `PolygonCard.tsx` de `components/wizard/`), remplace `DocumentClausesList.tsx` **supprimé**). Grilles internes en `sm:` (640px, jamais `lg:`) — voir note piège dans `memory/memory.md`.
- components/mobile: `src/features/contracts/components/mobile/MobileContractLayout.tsx`, `MobileWizardStepProperty.tsx` (**tâche 8 — passe `contractId`/`onCaptured` à `MobilePropertySubStepDelineate`**), `MobilePropertySubStepLocate.tsx`, `MobilePropertySubStepDelineate.tsx` (tâche 1 — transmet `viewport`/`isAnalyzing`/`handleAutoDetect` à `ZoneToolbarFloating` ; **tâche 8 — accepte `contractId`/`onCaptured`, transmis à `useDelineateState` (recapture avec zones en quittant Délimiter, identique Desktop)**), `PropertyInfoSheet.tsx`, `PropertyZonesSheet.tsx`, `ZoneToolbarFloating.tsx` (tâche 1 — bouton "Détection automatique", icône `Loader2` animée pendant l'analyse), `ZoneDetailSheet.tsx` (tous sprint012 sauf mention contraire)
- components/wizard: `src/features/contracts/components/wizard/ContractWizard.tsx` (tâche 5 (2026-07-15) — `openPropertyAnalysis` passé à `WizardStepClient`, plus de rendu `services` ; tâche 11 — `control`/`register`/`errors`/`setValue` aussi passés à `WizardStepClient` ; tâche 5 (2026-07-17, 2e occurrence) — ces 4 props retirées de `WizardStepClient` (prix/échéancier repartis vers `WizardStepTerms`) ; **tâche 7** — calcule `immersive` depuis `propertyNav.immersive`, appelle `useDesktopImmersive(immersive)`, passe `hideProgress`/`headerActions` conditionnels à `WizardLayout`), `useContractWizardState.ts` (**vit ici, pas dans `hooks/`** — tâche 5 (2026-07-15) : `STEP_ORDER` 4 étapes `['client','property','terms','review']`, `propertyAnalysisRequested`/`openPropertyAnalysis`/`nextSkippingProperty` pour l'étape "Analyse & Zones" optionnelle, `clientTypeLabel` dérivé de `selectedClient.typeClient` ; tâche 11 — `STEP_COMPLETION.client` exige aussi `prix`+`modalitesPaiement`, `STEP_LABELS.terms` renommé "Paiement" ; tâche 5 (2026-07-17, 2e occurrence du nom) — `STEP_COMPLETION.client` revient à `Boolean(selectedClient)` seul, `STEP_COMPLETION.terms` regagne `prix`+`modalitesPaiement` en plus de `modePaiement`), `AddressPreviewCard.tsx` (tâche 5 — restylée "Adresse Validée" verte + bouton "Outil de mesure" `onOpenMeasurementTool`, plus de titre "Adresse" rouge), `ContractSummaryPanel.tsx` (tâche 5 — `typeLabel` dérivé de `client.typeClient` au lieu d'un `useWatch('type')`), `PolygonEditor.tsx` (tâche 12 — branche le mode `draw_polygon` custom `createDrawPolygonModeWithAllVertices` ; tâche 1 — gagne `addSuggestedZone(geojson, zoneId, type)`, ajoute directement une zone suggérée par Gemini sans passer par la fenêtre de nommage), `PropertyMap.tsx` (tâche 12 — prop `zoom` optionnel, plus de `19.5` codé en dur), `PropertyBoundaryLayer.tsx`, `PropertyMaskLayer.tsx`, `MapViewportController.tsx` (tâche 12 — prop `autoFit` : désactive `fitBounds()` quand un cadrage capturé existe déjà), `PropertyMapStage.tsx` (tâche 12 — prop `initialViewport`, calcule `autoFit` en interne), `PropertyInfoPanel.tsx`, `PropertyInfoContent.tsx`, `PropertyZonesPanel.tsx`, `PropertyZonesContent.tsx`, `PropertySubStepDelineate.tsx` (tâche 12 — reçoit/transmet `initialViewport` ; tâche 1 — transmet `viewport`/`isAnalyzing`/`handleAutoDetect` à la toolbar ; **tâche 4 — Desktop seulement, refonte carte plein écran** : quand `mapReady`, bascule sur un layout `relative` plein bord — `PropertyZonesPanel` devient flottant `absolute bottom-4 right-4`, nouvelle `ZoneToolbarFloatingDesktop` flottante `absolute top-4 left-4`, `Recentrer` câblé via `onRecenterReady` (absent avant côté Desktop) ; conserve intact l'ancien layout en grille 30/70 quand la carte est indisponible (repli manuel) ; **tâche 8 — accepte `contractId`/`onCaptured`, transmis à `useDelineateState`**), `PropertySubStepLocate.tsx` (tâche 12 — reçoit/transmet `initialViewport`, `onCaptured` remonte aussi le `viewport` ; **tâche 4** — même bascule : `PropertyInfoPanel` flottant `absolute top-4 left-4` quand `showMap`, layout grille conservé pour le repli manuel ; **tâche 7** — `onNavChange` transmet aussi `immersive: showMap`), `PropertySubStepValidate.tsx`, `SurfaceSummary.tsx`, `WizardStepClient.tsx` (tâche 5 (2026-07-15) — réduit à `ClientSearchPicker`+`AddressPreviewCard` ; tâche 11 — regagne `prix` (Input) + un sélecteur d'échéancier Annuel/Bi-paiement (2 `Button`, surlignage via `detectPreset`), props `control`/`register`/`errors`/`setValue` ; **tâche 5 (2026-07-17, 2e occurrence du nom) — redevenu minimal**, `prix`/échéancier repartis vers `WizardStepTerms.tsx`, props `control`/`register`/`errors`/`setValue` retirées), `WizardStepTerms.tsx` (tâche 5 (2026-07-15) — bloc Obligations retiré entièrement, dateDebut`/`modeConclusion` lus via `useContractWizardDefaults()` au lieu du form ; tâche 11 — `prix`/échéancier repartis vers `WizardStepClient.tsx`, `PaymentScheduleBuilder` retiré, ne garde plus que Mode de paiement + Notes + warnings ; **tâche 5 (2026-07-17, 2e occurrence du nom) — regagne `prix`(Input)+échéancier (2 `Button`) en tête, avant Mode de paiement**, prop `setValue` désormais utilisée), `WizardStepProperty.tsx` (consomme `usePropertyStepState` ; **tâche 4** — bandeau de sous-étapes gagne son propre padding, sous-étape `validate` enveloppée d'un padding+scroll local — nécessaire depuis que `WizardLayout` ne paddage plus ce contenu, voir `fullBleedContent` ; **tâche 7** — `PropertyNav` gagne `immersive?: boolean`, bandeau de sous-étapes masqué entièrement (`subStep === 'validate' || mapUnavailable`) ; **tâche 8** — passe `contractId`/`onCaptured={handleCaptured}` à `PropertySubStepDelineate`), `WizardStepValidation.tsx` (tâche 5 — `type`/`services`/`obligations` ne sont plus des `useWatch` du form, lus depuis `client.typeClient` + `useContractWizardDefaults()` ; tâche 11 — échéances affichées via `computeInstallmentAmount` (montant $) au lieu du pourcentage brut), `ZoneToolbarFloatingDesktop.tsx` (nouveau, tâche 4 — toolbar verticale flottante Desktop, remplace `ZoneToolbar.tsx` **supprimé** (barre horizontale, code mort après ce remplacement) ; composant autonome, ne réutilise pas `components/mobile/ZoneToolbarFloating.tsx` — duplication délibérée d'un petit bouton icône pour ne prendre aucun risque sur le Mobile déjà vérifié), `ZoneTypeSelector.tsx`, `ZoneNamingModal.tsx`, `PolygonCard.tsx`, `PolygonList.tsx`, `ZoneAreaSummary.tsx`. **`WizardStepServices.tsx` supprimé (tâche 5, étape retirée)**.
- components/wizard/mobile: `src/features/contracts/components/wizard/mobile/MobileContractWizard.tsx` (tâche 5 — plus de case `services`, `onOpenMeasurementTool` passé à `WizardStepClient`)
- constants: `src/features/contracts/constants/wizardOptions.ts` (`SERVICE_OPTIONS`/`SEUIL_DECLENCHEMENT_OPTIONS`/`DEPOT_NEIGE_OPTIONS`/`MODE_CONCLUSION_OPTIONS`/`MODE_CONCLUSION_LABELS` désormais consommés par `ContractWizardDefaultsForm.tsx` en plus du Wizard ; `ZONE_TYPE_OPTIONS`/`ZONE_TYPE_COLORS` ; tâche 14 — `AI_MODEL_OPTIONS` ajouté ; tâche 2 — `AI_MODEL_OPTIONS` remplacé par `AI_MODEL_OPTIONS_BY_PROVIDER: Record<AiProvider, {value,label}[]>` (Google: Flash/Pro inchangés, TokenRouter: 3 nouvelles entrées), `AI_PROVIDER_OPTIONS` ajouté ; tâche 3 — `AI_MODEL_OPTIONS_BY_PROVIDER.tokenrouter` gagne `openai/gpt-5.4-mini` ("GPT-5.4 Mini"), 4e entrée), `zoneDrawStyles.ts` (tâche 12 — 2 couches `gl-draw-historical-vertex-outer/inner` pour les points décoratifs de `drawPolygonAllVertices.ts`), `contractStatusColors.ts`. **Tâche 6** — `PRIX_TAXES_OPTIONS` ajouté (avant/après taxes).
- pages: `src/features/contracts/pages/ContractDetailPage.tsx` (dispatcher), `ContractsListPage.tsx` (dispatcher), `ContractWizardPage.tsx` (dispatcher), `ContractWizardSettingsPage.tsx` (nouveau, tâche 5 — page admin-only des paramètres par défaut, route `contracts/parametres`), `ContractCreatedPage.tsx` (nouveau, tâche 8 (2026-07-15) — page "Contrat enregistré !", route `contracts/:id/created`, reçoit `contract`/`client`/`zones` via `location.state` avec filet de secours `useContract`/`useClient` ; **tâche 8 (2026-07-17, 2e occurrence) — calcule `imageUrl` (URL signée depuis `zones[0]?.imageStoragePath`, `useEffect`+`useState`), transmis à `ContractDocumentPreview`**)
- pages/desktop: `src/features/contracts/pages/desktop/DesktopContractsListPage.tsx` (tâche 5 — icône ⚙️ admin-only vers `/contracts/parametres`), `DesktopContractDetailPage.tsx` (tâche 5 — ligne "Plafond saisonnier" retirée, colonne supprimée)
- pages/mobile: `src/features/contracts/pages/mobile/MobileContractsListPage.tsx` (tâche 5 — icône ⚙️ admin-only), `MobileContractDetailPage.tsx` (tâche 5 — même retrait "Plafond saisonnier")
- utils: `src/features/contracts/utils/generateClauses.ts` (tâche 5 — `obligationsClient` devient un texte fixe (`OBLIGATIONS_CLIENT_BASE`, plus de sentence-builder Q&R puisque balises/entrée libre/animaux/portail/autres particularités n'existent plus) — **à faire relire par un juriste avant production**), `computeSoftWarnings.ts`, `draftMapping.ts` (tâche 5 — `contractToFormValues` réduit aux champs restants du form, `DEFAULT_OBLIGATIONS` retiré), `paymentPresets.ts` (tâche 5 — `'mensuel'`/`buildMonthlySchedule` retirés, `PaymentPresetId` = `'annuel'|'deux_versements'` ; tâche 8 — `getNextPaymentEntry` ajouté, réutilisé par `ContractSummaryPanel.tsx`/`DocumentSummaryPayment.tsx` ; tâche 11 — labels renommés `Annuel`/`Bi-paiement`, `buildPaymentSchedule` prend désormais `dateDeuxiemeVersement` (date fixe des paramètres) au lieu de calculer +3 mois, `detectPreset` (surlignage du sélecteur actif) et `computeInstallmentAmount` (montant $ réel d'une échéance, déplacé depuis `contracts.service.ts`, partagé par la génération de factures et les prévisualisations) ajoutés), `propertyBoundary.ts`, `drawPolygonAllVertices.ts` (nouveau, tâche 12 — `createDrawPolygonModeWithAllVertices()` : mode `draw_polygon` Mapbox GL Draw custom, affiche un point à chaque sommet déjà commis pendant le tracé), `satelliteZoneProjection.ts` (nouveau, tâche 1 — `boundingBoxToPolygon(bbox, map)` : convertit une bounding box Gemini (0-1000) en polygone GeoJSON via `map.unproject()`, en tenant compte de `devicePixelRatio`)

## invoices
- types: `src/features/invoices/types/invoice.types.ts`
- schemas: `src/features/invoices/schemas/invoice.schema.ts`
- services: `src/features/invoices/services/invoices.service.ts`
- hooks: `src/features/invoices/hooks/invoiceKeys.ts`, `useClientInvoices.ts`, `useContractInvoices.ts`, `useCreateInvoice.ts`, `useDeleteInvoice.ts`, `useInvoices.ts`, `useInvoice.ts`, `useUpdateInvoiceStatus.ts`, `useUpdateInvoice.ts`
- components: `src/features/invoices/components/InvoiceFormModal.tsx`, `InvoiceForm.tsx`, `InvoiceStatusBadge.tsx`, `InvoiceTable.tsx`
- pages: `src/features/invoices/pages/InvoiceCreatePage.tsx`, `InvoiceDetailPage.tsx`, `InvoicesListPage.tsx`

## payments
- types: `src/features/payments/types/payment.types.ts`
- schemas: `src/features/payments/schemas/payment.schema.ts`
- services: `src/features/payments/services/payments.service.ts`
- hooks: `src/features/payments/hooks/paymentKeys.ts`, `useCreatePayment.ts`, `useDeletePayment.ts`, `useInvoicePayments.ts`, `usePayments.ts`
- components: `src/features/payments/components/PaymentFormModal.tsx`, `PaymentForm.tsx`, `PaymentTable.tsx`
- pages: `src/features/payments/pages/PaymentsListPage.tsx`

## equipments
*(dossier réel `src/features/equipments/`, pluriel — pas `equipment`)*
- types: `src/features/equipments/types/equipment.types.ts`
- schemas: `src/features/equipments/schemas/equipment.schema.ts`
- services: `src/features/equipments/services/equipments.service.ts`
- hooks: `src/features/equipments/hooks/equipmentKeys.ts`, `useCreateEquipment.ts`, `useUpdateEquipmentStatus.ts`, `useEquipment.ts`, `useEquipments.ts`, `useUpdateEquipment.ts`, `useDeleteEquipment.ts`
- components: `src/features/equipments/components/EquipmentFormModal.tsx`, `EquipmentForm.tsx`, `EquipmentTable.tsx`, `EquipmentStatusBadge.tsx`
- pages: `src/features/equipments/pages/EquipmentCreatePage.tsx`, `EquipmentsListPage.tsx`, `EquipmentDetailPage.tsx`

## employees
- types: `src/features/employees/types/employee.types.ts`
- schemas: `src/features/employees/schemas/employee.schema.ts`
- services: `src/features/employees/services/employeeAccount.service.ts`, `employeeEquipment.service.ts`, `employees.service.ts`
- hooks: `src/features/employees/hooks/employeeKeys.ts`, `useAssignEquipment.ts`, `useCreateEmployee.ts`, `useDeleteEmployee.ts`, `useEmployeeAccount.ts`, `useEmployeeEquipment.ts`, `useEmployees.ts`, `useEmployee.ts`, `usePromoteEmployeeAccount.ts`, `useUnassignEquipment.ts`, `useUpdateEmployee.ts`
- components: `src/features/employees/components/EmployeeFormModal.tsx`, `EmployeeForm.tsx`, `EmployeeTable.tsx`
- pages: `src/features/employees/pages/EmployeeCreatePage.tsx`, `EmployeeDetailPage.tsx`, `EmployeesListPage.tsx`

## routes
- types: `src/features/routes/types/route.types.ts`
- schemas: `src/features/routes/schemas/route.schema.ts`
- services: `src/features/routes/services/routeAssignments.service.ts`, `routeClients.service.ts`, `routes.service.ts`
- hooks: `src/features/routes/hooks/routeKeys.ts`, `useAddRouteClient.ts`, `useCreateRouteAssignment.ts`, `useCreateRoute.ts`, `useDeleteRouteAssignment.ts`, `useDeleteRoute.ts`, `useRemoveRouteClient.ts`, `useReorderRouteClient.ts`, `useRouteAssignments.ts`, `useRouteClients.ts`, `useRoutes.ts`, `useRoute.ts`, `useUpdateAssignmentStatus.ts`, `useUpdateRouteStatus.ts`, `useUpdateRoute.ts`
- components: `src/features/routes/components/RouteFormModal.tsx`, `RouteForm.tsx`, `RouteStatusBadge.tsx`, `RouteTable.tsx`
- pages: `src/features/routes/pages/RouteCreatePage.tsx`, `RouteDetailPage.tsx`, `RoutesListPage.tsx`

## settings
- types: `src/features/settings/types/settings.types.ts` (sprint014 — `assurancePoliceNo`/`assurance_police_no`)
- schemas: `src/features/settings/schemas/settings.schema.ts` (sprint014 — `assurancePoliceNo`)
- services: `src/features/settings/services/accounts.service.ts`, `settings.service.ts` (sprint014 — mapping `assurance_police_no`)
- hooks: `src/features/settings/hooks/settingsKeys.ts`, `useAccounts.ts`, `useSettings.ts`, `useUpdateAccountActive.ts`, `useUpdateAccountRole.ts`, `useUpdateModules.ts`, `useUpdateSettings.ts`
- components: `src/features/settings/components/AccountsTable.tsx`, `CompanySettingsForm.tsx` (sprint014 — champ "N° de police d'assurance"), `ModulesTable.tsx`
- pages: `src/features/settings/pages/SettingsPage.tsx`

## Partagé / transverse
- `src/components/layout/` : `ModuleContainer.tsx`, `PageLayout.tsx`, `PageTabs.tsx`, `StickyPageFooter.tsx`, `StickyPageHeader.tsx`, `WizardFooter.tsx` (sprint014 — `onDraft`/`draftDisabled` retirés, brouillon déplacé vers `WizardLayout.headerActions`), `WizardLayout.tsx` (sprint014 — `headerActions?`/`sidePanel?` optionnels ajoutés ; tâche 4 — `fullBleedContent?` optionnel, retire le padding/`overflow-y-auto` du conteneur de contenu pour une étape qui gère elle-même son padding/scroll interne, utilisé par l'étape Property du Wizard Contrats ; tâche 7 — `hideProgress?` optionnel, masque entièrement `WizardProgress`), `WizardProgress.tsx`
- `src/components/layout/mobile/` (sprint012 — Wizard mobile générique, réutilisable hors Contrats) : `FloatingActionBar.tsx` (sprint014 — `onDraft` rendu sur chaque étape, pas seulement la dernière), `MobileFooterInsetContext.ts` (hauteur du footer courant, lue par `BottomSheet` pour ne jamais se positionner par-dessus), `MobileStepLayout.tsx`, `MobileWizard.tsx`
- `src/components/ui/` : `AddressAutocomplete.tsx` (tâche 5 — champ adresse avec suggestions Mapbox à la frappe, dégrade en texte libre si `isMapboxConfigured` est faux, réutilisable partout où un champ adresse existe ; tâche 7 — `parseFeature()` combine `feature.address` (numéro civique) + `feature.text` (nom de rue), Mapbox les renvoie séparément pour les features `address`), `Avatar.tsx` (nouveau, sprint013 — initiales/photo, construit mais non branché ce sprint), `Badge.tsx` (sprint013 — `size` sm/md, couleurs déplacées vers `statusColors.ts`), `BottomSheet.tsx` (sprint012 — feuille du bas glissable/redimensionnable, coexiste avec `Modal.tsx`), `Button.tsx` (sprint013 — `fullWidth` additif, non branché ce sprint), `Card.tsx` (sprint013 — ombre au lieu de bordure dure, `variant="clickable"`/`chevron` additifs), `Dropdown.tsx`, `Input.tsx`, `ListRow.tsx` (nouveau, sprint013 — icône+titre+sous-titre+valeur/chevron, générique), `Modal.tsx`, `QueryState.tsx`, `Select.tsx`, `StatCard.tsx` (nouveau, sprint013 — carte KPI générique), `statusColors.ts` (nouveau, sprint013 — `StatusColor`/`STATUS_BG_CLASSES`/`STATUS_TEXT_CLASSES` partagés), `Table.tsx` (sprint013 — classes alignées sur le nouveau style Card), `Toaster.tsx`, `Tooltip.tsx`, `useTableState.ts`
- `src/hooks/` : `useBodyScrollLock.ts`, `useBreakpoint.ts`, `useDeviceTier.ts` (nouveau, sprint012 — `'mobile'|'desktop'`, wrapper sémantique au-dessus de `useBreakpoint`), `useElementSize.ts`, `useFocusTrap.ts`, `useMapboxMap.ts`
- `src/components/map/` (repéré tâche 13, absent de l'index avant — à traiter comme transverse) : `MapCanvas.tsx` (wrapper bas-niveau générique autour de `useMapboxMap`, sans connaissance du domaine Contrats, réutilisé par `PropertyMap.tsx`)
- `src/layouts/` : `AppLayout.tsx` (sprint012 — devient un dispatcher Desktop/Mobile), `Breadcrumb.tsx` (desktop uniquement ; tâche 7 — lit `useDesktopImmersiveValue()`, retourne `null` en mode plein écran), `DesktopAppShell.tsx` (nouveau, sprint012 — ex-contenu d'`AppLayout.tsx` ; tâche 7 — enveloppé dans `DesktopChromeProvider`, `<main>` extrait dans un petit composant interne `DesktopMain` qui bascule son padding en mode immersif), `DesktopChromeContext.tsx` (nouveau, tâche 7 — `DesktopChromeProvider`, composant Provider, monté une fois par `DesktopAppShell`), `useDesktopChrome.ts` (nouveau, tâche 7 — hooks-only, séparé du Provider pour Fast Refresh, même pattern que `useMobileHeaderActions.ts` : `useDesktopImmersive(immersive)`/`useDesktopImmersiveValue()`, réutilisable par tout futur mode plein écran Desktop), `MobileAppShell.tsx` (nouveau, sprint012), `MobileBottomNavigation.tsx` (nouveau, sprint012), `MobileHeader.tsx` (nouveau, sprint012), `MobileHeaderActionsContext.tsx` (nouveau, sprint012 — composant Provider), `navItems.ts` (nouveau, sprint012 — `NAV_ITEMS`/`useVisibleNavItems` extraits de `Sidebar.tsx`, partagés), `PublicLayout.tsx`, `Sidebar.tsx` (desktop/tablette uniquement, comportement inchangé), `useMobileHeaderActions.ts` (nouveau, sprint012 — hooks, séparé du Provider pour Fast Refresh)
- `src/routes/` : `RequireModule.tsx`, `router.tsx`
- `src/lib/` : `format.ts`, `mapboxClient.ts`, `queryClient.ts`, `storage.ts`, `supabaseClient.ts`, `supabaseCrud.ts`
- `supabase/functions/` (nouveau, tâche 1 — première Edge Function du repo) : `analyze-satellite-image/index.ts` (Deno — `corsHeaders` explicites + handler `OPTIONS` requis pour tout appel navigateur, télécharge l'image capturée depuis le bucket privé `contract-captures` avec `SUPABASE_SERVICE_ROLE_KEY` auto-injecté, appelle le fournisseur IA choisi avec sa clé (secret Supabase, jamais côté client), valide la réponse avec Zod, consommée par `src/features/contracts/services/satelliteAnalysis.service.ts` ; tâche 14 — modèle Gemini choisi via `aiModel` reçu dans le corps de requête (`'flash'|'pro'`, défaut `'flash'`), mappé via `GEMINI_MODEL_BY_OPTION` vers un alias `-latest` (jamais un nom de modèle figé type `gemini-2.5-flash`, déjà renvoyé 404 "no longer available to new users" malgré son apparition dans `models.list`) ; réponse par zone enrichie de `confiance: 'haute'|'moyenne'|'faible'`. **`gemini-pro-latest` jamais testé réellement, faute de clé disponible dans ce sandbox** (voir `memory/memory.md`, tâche 14). **Tâche 2** — branche désormais sur `aiProvider` (`'google'`|`'tokenrouter'`, défaut `'google'`) : chemin Google inchangé, nouveau chemin TokenRouter (secret `TOKENROUTER_API_KEY`, URL `https://api.tokenrouter.com/v1beta/models/{aiModel}:generateContent`, `Authorization: Bearer` au lieu du `?key=`) — `aiModel` transmis tel quel pour TokenRouter (pas d'alias). Corps de requête `generateContent` factorisé dans `buildGenerateContentRequestBody()`, partagé par les 2 fournisseurs. **Confirmé par curl direct avec la vraie clé TokenRouter** : seul `google/gemini-3.5-flash` supporte `responseSchema`/JSON structuré ; les 2 modèles `-image` (`gemini-3.1-flash-lite-image`, `gemini-2.5-flash-image`) sont des modèles de génération d'image et renvoient `400` sur ce type de requête — voir `memory/memory.md`, tâche 2. **Tâche 3** — le chemin TokenRouter se scinde en 2 styles d'API selon `aiModel.startsWith('openai/')` (`isOpenAiCompatible`) : `google/...` inchangé (Gemini `generateContent`) ; `openai/...` (ex. `openai/gpt-5.4-mini`) route vers `https://api.tokenrouter.com/v1/chat/completions` (nouveau `buildOpenAiChatCompletionsBody`/`OPENAI_RESPONSE_SCHEMA` — JSON Schema strict standard, distinct de `GEMINI_RESPONSE_SCHEMA`), réponse parsée depuis `choices[0].message.content`. Même secret `TOKENROUTER_API_KEY`, pas de nouveau secret. **Confirmé viable par 4 tests curl directs** (texte, JSON schema strict, vision, test complet avec le vrai prompt+schéma sur une image satellite réelle) — voir `memory/memory.md`, tâche 3. Déploiement + secrets hors de portée de ce sandbox (pas de token CLI Supabase authentifié) — toujours à refaire manuellement par l'utilisateur après toute future modification de ce fichier (y compris `supabase secrets set TOKENROUTER_API_KEY=...` avant le premier essai du chemin TokenRouter, et un nouveau déploiement après la tâche 3 avant que `openai/gpt-5.4-mini` soit testable en conditions réelles).

## Maintenance
Cet index doit être mis à jour dans le protocole de fin de tâche (comme `tasks.md`/`memory.md`) dès qu'un fichier est ajouté, supprimé ou déplacé dans un module.
