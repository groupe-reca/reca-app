# Index de fichiers par module

Table de référence : quels fichiers appartiennent à quel module. À consulter EN PREMIER pour toute tâche touchant un module existant, avant toute exploration/grep du repo (voir CLAUDE.md §6).

Convention de chaque module : `types/`, `schemas/`, `services/`, `hooks/`, `components/`, `pages/` (tous les dossiers ne sont pas toujours présents).

## auth
- types: `src/features/auth/types/auth.types.ts` (Optimisation UI v3 — `Theme = 'clair'|'sombre'` ajouté, `User.theme` ajouté)
- schemas: `src/features/auth/schemas/login.schema.ts`
- services: `src/features/auth/services/auth.service.ts` (Optimisation UI v3 — `mapUser` sélectionne/retourne aussi `theme`)
- hooks: `src/features/auth/hooks/useLogin.ts`, `useLogout.ts`, `useSession.ts`
- components: `src/features/auth/components/LoginForm.tsx`, `RequireAuth.tsx`
- pages: `src/features/auth/pages/LoginPage.tsx`

## leads
- types: `src/features/leads/types/lead.types.ts`
- schemas: `src/features/leads/schemas/lead.schema.ts`
- services: `src/features/leads/services/leads.service.ts`
- hooks: `src/features/leads/hooks/leadKeys.ts`, `useCreateLead.ts`, `useDeleteLead.ts`, `useLeads.ts`, `useLead.ts`, `useScheduleReminder.ts`, `useUpdateLeadStatus.ts`, `useUpdateLead.ts`
- components: `src/features/leads/components/LeadFormModal.tsx`, `LeadForm.tsx`, `LeadReminderCard.tsx`, `LeadStatusBadge.tsx`, `LeadTable.tsx`
- pages: `src/features/leads/pages/LeadCreatePage.tsx`, `LeadDetailPage.tsx`, `LeadsListPage.tsx`

## quotes
- types: `src/features/quotes/types/quote.types.ts`
- schemas: `src/features/quotes/schemas/quote.schema.ts`
- services: `src/features/quotes/services/quotes.service.ts`
- hooks: `src/features/quotes/hooks/quoteKeys.ts`, `useConvertQuoteToClient.ts`, `useCreateQuote.ts`, `useDeleteQuote.ts`, `useQuotes.ts`, `useQuote.ts`, `useUpdateQuoteStatus.ts`, `useUpdateQuote.ts`
- components: `src/features/quotes/components/QuoteFormModal.tsx`, `QuoteForm.tsx`, `QuoteStatusBadge.tsx`, `QuoteTable.tsx`
- pages: `src/features/quotes/pages/QuoteCreatePage.tsx`, `QuoteDetailPage.tsx`, `QuotesListPage.tsx`

## clients
- types: `src/features/clients/types/client.types.ts` (**refonte fiche Client — `CLIENT_STATUSES`/`ClientStatus` (`actif`|`inactif`), `CLIENT_LANGUES`/`ClientLangue` (`francais`|`anglais`), `ClientEditorRef` ajoutés ; `Client` gagne `statut`/`langue`/`updatedAt`/`updatedBy` (ce dernier calculé via jointure `users`, jamais stocké)**), `clientNote.types.ts` (nouveau — `ClientNoteAuthorRef`/`ClientNoteRow`/`ClientNote`, miroir exact de `contractNote.types.ts`)
- schemas: `src/features/clients/schemas/client.schema.ts` (tâche 5 — `CLIENT_TYPES`/`ClientType` enum `residentiel`|`commercial` remplace le `typeClient` free-text, `entreprise` requis si commercial via `.refine()` ; tâche 7 — `telephone` obligatoire, plus `.optional()` ; **refonte fiche Client — `statut`/`langue` ajoutés, requis**), `clientNote.schema.ts` (nouveau — `clientNoteSchema`, miroir de `contractNote.schema.ts`)
- services: `src/features/clients/services/clients.service.ts` (**refonte fiche Client — `getClient(id)` n'utilise plus `clientsCrud.getById` : requête dédiée `SELECT_WITH_EDITOR` (`*, updated_by_user:users(id, nom)`) pour "Dernière modification", avec repli automatique sur un `select('*')` simple si l'embed échoue (FK pas encore appliquée) — ne jamais faire dépendre une requête déjà en prod d'un nouvel embed sans ce repli, voir `memory/memory.md` ; `mapClient()`/`toRowInput()` étendus statut/langue/updatedAt/updatedBy ; `listClients`/`createClient`/`updateClient`/`softDeleteClient` inchangés, toujours via `clientsCrud` générique**), `clientNotes.service.ts` (nouveau — `listClientNotes`/`createClientNote`/`updateClientNote`/`softDeleteClientNote`, ces 2 dernières absentes du gabarit `contractNotes.service.ts`, pour le menu "…" Modifier/Supprimer de chaque note)
- hooks: `src/features/clients/hooks/clientKeys.ts` (**`notes(clientId)` ajouté**), `useClients.ts`, `useClient.ts`, `useCreateClient.ts`, `useDeleteClient.ts`, `useUpdateClient.ts`, `useClientNotes.ts`/`useAddClientNote.ts`/`useUpdateClientNote.ts`/`useDeleteClientNote.ts` (nouveaux, miroir des hooks de notes de contrat)
- components: `src/features/clients/components/ClientFormModal.tsx`, `ClientForm.tsx` (tâche 5 — sélecteur Résidentiel/Commercial en haut, Entreprise conditionnelle, adresse via `AddressAutocomplete`, latitude/longitude/notes gardés en `defaultValues` mais plus rendus ; **refonte fiche Client — gagne 2 `Select` Statut/Langue**), `ClientSearchPicker.tsx` (tâche 5 — badge type + adresse/téléphone formatés dans la vue "client sélectionné", réutilisé tel quel par le Wizard Contrats ; **correctif iOS Safari — boutons de résultats gagnent `onMouseDown preventDefault()` pour éviter la course blur/click**), `ClientTable.tsx`
- components/detail (nouveau, refonte fiche Client — reproduction de maquette mobile fournie, composants purs partagés à l'identique par `DesktopClientDetailPage.tsx`/`MobileClientDetailPage.tsx`, même pattern que `contracts/components/detail/`) : `ClientDetailHeader.tsx` (Modifier/Supprimer, Supprimer stylé contour rouge comme "Annuler le contrat"), `ClientContactCard.tsx` (coordonnées + `StaticMapThumbnail`), `ClientInfoCard.tsx` (Type/GPS/Statut/Langue/Créé le/Dernière modification — grille `grid-cols-2` **sans** `sm:`, la maquette montre 2 colonnes même en mobile étroit, contrairement à `ContractInfoStrip.tsx`), `ClientContractsCard.tsx` (tronqué à 2 contrats + "Voir tous les contrats (N)", pattern `showAll` local), `ClientNotesCard.tsx` (**ajout inline** champ+bouton, diffère volontairement du gabarit Contrats qui utilise une modale) + `ClientNoteTimelineItem.tsx` (avatar coloré via `getAvatarColor`, menu "…" Modifier/Supprimer) + `ClientEditNoteModal.tsx`, `ClientInvoicesCard.tsx` (absente de la maquette, conservée pour ne pas régresser la fonctionnalité Factures déjà existante)
- pages: `src/features/clients/pages/ClientCreatePage.tsx`, `ClientsListPage.tsx`, `ClientDetailPage.tsx` (**refonte fiche Client — devient un dispatcher `useDeviceTier()` pur, même pattern que `ContractDetailPage.tsx`**)
- pages/desktop (nouveau) : `DesktopClientDetailPage.tsx`
- pages/mobile (nouveau) : `MobileClientDetailPage.tsx` (pas de composant de layout partagé type `MobileContractLayout` — `<main>` de `MobileAppShell` n'a aucun padding propre, la page s'enveloppe elle-même dans `flex flex-col gap-4 p-4`)

## contracts
- types: `src/features/contracts/types/contract.types.ts` (**Optimisation UI v3 — `ContractStatus` gagne `brouillon`/`a_signer` ; `en_attente` relibellé "Signature en attente" (valeur DB inchangée) ; `ContractClientRef` gagne `telephone` ; `Contract` gagne `hasOverdueInvoice: boolean` (calculé, jamais stocké, voir `contracts.service.ts`)** ; **tâche 9 — `ContractClientRef` gagne `courriel`/`adresse`/`ville`/`codePostal` ; `ContractRow`/`Contract` gagnent `obstaclesConnus`/`messageOperateur`/`consignesSpeciales` (3 nouvelles colonnes, migration `20260718020000_contracts_operator_info.sql`)**, tâche 5 — `ObligationsAnswers` réduit à 4 champs : `seuilDeclenchementCm`/`heurePremierPassage`/`depotNeige`/`permisMunicipalObtenu`, `plafond_saisonnier_cm`/`plafondSaisonnierCm` retirés ; `MODE_CONCLUSION`/`DEPOT_NEIGE` inchangés ; **tâche 6 — `PRIX_TAXES_MODES`/`PrixTaxesMode` ajoutés (base commune, `contractWizardDefaults.types.ts` importe depuis ici) ; `ContractRow.prix_taxes`/`Contract.prixTaxes` ajoutés**), `contractWizardDefaults.types.ts` (tâche 5, étendu tâche 11 — `ContractWizardDefaults`/`DEFAULT_CONTRACT_WIZARD_DEFAULTS`, paramètres par défaut du Wizard, gagne `dateDeuxiemeVersement` : date fixe du 2e versement Bi-paiement, remplace le calcul +3 mois ; tâche 14 — `AiModel = 'flash'|'pro'`, champ `aiModel` ajouté, défaut `'flash'` ; tâche 2 — `AiProvider = 'google'|'tokenrouter'` ajouté, champ `aiProvider` ajouté à `ContractWizardDefaults` (défaut `'google'`), `AiModel` étendu aux 3 identifiants TokenRouter ; tâche 3 — `AiModel` gagne `'openai/gpt-5.4-mini'`, 1er modèle TokenRouter de style OpenAI-compatible ; **tâche 6 — `prixTaxes: PrixTaxesMode` ajouté, défaut `'avant_taxes'`**), `satelliteAnalysis.types.ts` (nouveau, tâche 1 — `SatelliteAnalysisResult`/`SatelliteZoneDetection`, miroir TS de la réponse déjà validée par l'Edge Function `analyze-satellite-image`, pas de revalidation Zod côté client ; tâche 14 — `SatelliteZoneDetection` gagne `confiance: 'haute'|'moyenne'|'faible'`), `contractNote.types.ts` (nouveau, tâche 9 — `ContractNoteRow`/`ContractNote`/`ContractNoteAuthorRef`, table `contract_notes` — migration `20260718030000_contract_notes.sql`), `contractEvent.types.ts` (nouveau, modernisation fiche contrat v2 — `ContractEventType`/`CONTRACT_EVENT_TYPE_LABELS`/`ContractEventRow`/`ContractEvent`/`ContractEventActorRef`, table `contract_events` — migration `20260718050000_contract_events.sql`, journal léger "Historique du contrat")
- schemas: `src/features/contracts/schemas/contractCreation.schema.ts` (tâche 5 — `contractFieldsShape` réduit à `adresseGeocodee/latitude/longitude/zones/photos/prix/modePaiement/modalitesPaiement/notes` ; `obligationsAnswersSchema`/`serviceEntrySchema` retirés, plus aucun champ `type`/`modeConclusion`/`saison`/dates/`renouvellement`/`services`/`obligations` — tous dérivés du client ou des paramètres par défaut), `contract.schema.ts` (formulaire d'édition post-création, indépendant, non touché par la tâche 5), `contractWizardDefaults.schema.ts` (tâche 5, étendu tâche 11 — `dateDeuxiemeVersement` requis ; tâche 14 — `aiModel` ajouté ; tâche 2 — `aiProvider` ajouté, `aiModel` desserré en `z.string()` (couvre les identifiants TokenRouter) + `.refine()` croisé vérifiant que `aiModel` appartient à la liste `AI_MODEL_OPTIONS_BY_PROVIDER[aiProvider]` ; tâche 6 — `prixTaxes` ajouté), `contractNote.schema.ts` (nouveau, tâche 9 — `contractNoteSchema`, 1 champ `message`), `contractOperatorInfo.schema.ts` (tâche 9 — `contractOperatorInfoFieldSchema`, générique ; **modernisation fiche contrat v2 — `ContractOperatorInfoField` perd `'notes'`, retiré de `ContractOperatorInfoCard.tsx`, ne reste que obstacles/message opérateur/consignes**)
- services: `src/features/contracts/services/contracts.service.ts` (**Optimisation UI v3 — `SELECT_WITH_CLIENT` sélectionne aussi `telephone` du client ; `listContracts()` utilise `SELECT_LIST_WITH_INVOICES` (jointure `invoices(statut)`) et calcule `hasOverdueInvoice` par contrat (pas stocké) ; `mapContract` retourne `hasOverdueInvoice: false` par défaut, seul `listContracts` le recalcule ; `saveContractDraft` écrit désormais le statut `brouillon` (avant : détournait `en_attente`, faute de statut dédié)**, tâche 5 — `fetchContractWizardSettings` remplace `fetchAssurancePoliceNo`, lit aussi `contract_wizard_defaults` ; `toWizardRowInput`/`upsertContractWithZones`/`createContractWithZones`/`saveContractDraft` prennent un `clientTypeLabel` en paramètre et construisent `services`/`obligations`/`saison`/dates entièrement depuis les défauts, plus depuis le formulaire ; tâche 11 — `computeInstallmentAmount` déplacé vers `utils/paymentPresets.ts`, importé plutôt que défini localement ; **tâche 6 — nouvelle fonction pure `computeInstallmentTaxes(amount, prixTaxes)`, branche le calcul Sous-total/TPS/TVQ/Total selon `contract.prixTaxes` (avant/après taxes), utilisée par `generateInvoicesFromSchedule` ; `toWizardRowInput` écrit `prix_taxes: defaults.prixTaxes` ; `mapContract` lit `row.prix_taxes`**), `geocoding.service.ts`, `contractWizardSettings.service.ts` (nouveau, tâche 5 — `getContractWizardDefaults`/`updateContractWizardDefaults`, interroge `settings` directement sans dépendre de `features/settings/`), `satelliteAnalysis.service.ts` (nouveau, tâche 1 — `analyzeSatelliteImage(storagePath)`, appelle l'Edge Function `analyze-satellite-image` via `supabase.functions.invoke` ; tâche 14 — prend aussi `aiModel: AiModel`, transmis dans le corps de la requête ; tâche 2 — prend aussi `aiProvider: AiProvider`). **Tâche 9** — `SELECT_WITH_CLIENT` étend le join client (`courriel`/`adresse`/`ville`/`code_postal`), nouveau mapper explicite `mapContractClient()` (avant : passthrough direct) ; `mapContract()` lit aussi les 3 champs opérateur ; nouvelles fonctions `updateContractOperatorInfo(id, patch)` et `mapZoneRowToFormValues(row)` (pont `ContractZoneRow` DB → `ContractZoneFormValues`, jusqu'ici seul le brouillon du Wizard produisait cette forme). Nouveau fichier `contractNotes.service.ts` (tâche 9 — `listContractNotes`/`createContractNote`, table `contract_notes`). **Modernisation fiche contrat v2** — nouveau fichier `contractEvents.service.ts` (`listContractEvents`/`createContractEvent`, table `contract_events`) ; `contracts.service.ts` importe `createContractEvent` et journalise réellement : `upsertContractWithZones` écrit `'contrat_cree'` à la 1ère insertion ou `'statut_modifie'` (payload `{statut:'actif'}`) si un brouillon existant est finalisé ; `updateContractStatus` écrit `'statut_modifie'` à chaque changement de statut (point d'intégration unique, couvre "Annuler le contrat" et le menu "…").
- hooks: `src/features/contracts/hooks/useContractsListFilters.ts` (nouveau, Optimisation UI v3 — recherche+filtre par statut de la page liste, état local pur, `CONTRACT_STATUS_FILTER_OPTIONS` exporté), `contractKeys.ts` (tâche 5 — `wizardDefaults()` ajouté ; tâche 9 — `notes(contractId)` ajouté ; **modernisation fiche contrat v2 — `events(contractId)` ajouté**), `useContractZones.ts` (nouveau, tâche 9 — `useQuery` sur `listContractZones`, n'existait pas hors état de brouillon du Wizard), `useContractNotes.ts`/`useAddContractNote.ts` (nouveaux, tâche 9), `useContractEvents.ts`/`useLogContractEvent.ts` (nouveaux, modernisation fiche contrat v2 — `useLogContractEvent` best-effort, appelé par `ContractFormModal.tsx` (signature) et les 2 pages détail (placeholders courriel/PDF), jamais bloquant pour l'action principale), `useUpdateContractOperatorInfo.ts` (nouveau, tâche 9 — exporte aussi le type `OperatorInfoPatch`, réutilisé par `ContractOperatorInfoEditModal.tsx`), `useSignedCaptureUrl.ts` (nouveau, tâche 9 — URL signée `contract-captures`, extrait du pattern déjà présent dans `ContractCreatedPage.tsx`, utilisé par `ContractMapCard.tsx`), `useClientContracts.ts`, `useContracts.ts`, `useContract.ts`, `useCreateContractWithInvoices.ts` (tâche 5 — accepte `clientTypeLabel`), `useSaveContractDraft.ts` (tâche 5 — accepte `clientTypeLabel`), `useContractWizardDefaults.ts`/`useUpdateContractWizardDefaults.ts` (nouveaux, tâche 5), `useContractDraft.ts` (sprint014 — `useQueries` contrat+zones+photos pour la reprise), `useDeleteContract.ts`, `useUpdateContractStatus.ts` (**modernisation fiche contrat v2 — journalise `'statut_modifie'` via `contracts.service.ts`, voir plus haut**), `useUpdateContract.ts`, `usePropertyCapture.ts`, `usePropertyPhotos.ts` (sprint014 — upload photos), `useZoneTypeSelection.ts` (sprint009), `useDelineateState.ts` (sprint012 ; tâche 1 — accepte `viewport`/`onAddZones`, expose `isAnalyzing`/`handleAutoDetect` : détection automatique du stationnement via Gemini, réaligne la caméra sur `viewport`, déprojette les contours reçus puis ajoute toutes les zones **en un seul appel** à `onAddZones` — jamais en bouclant sur `onAddZone`, qui perdrait tout sauf la dernière zone, fermeture React figée oblige ; tâche 14 — lit `useContractWizardDefaults()` pour transmettre `aiModel` à `analyzeSatelliteImage`, toast de succès mentionne le nombre de zones à confiance non-haute ; tâche 7 — `onNavChange` transmet aussi `immersive: mapReady`, consommé uniquement côté Desktop ; **tâche 8 — accepte aussi `contractId`/`boundary`/`onCaptured`, instancie `usePropertyCapture` en interne, nouveau `handleContinueWithCapture` (remplace `onContinue` direct dans `onNavChange.onNext`) : recapture la carte avec les zones visibles en quittant Délimiter, réassigne `imageStoragePath` de toutes les zones à cette nouvelle capture**), `usePropertyStepState.ts` (sprint012, étendu sprint014 avec `photos`/`addPhoto`/`removePhoto` ; tâche 12 — `viewport`/`handleCaptured` : persiste le cadrage capturé entre sous-étapes ; tâche 1 — `addZones(zones[])` : ajout groupé en une seule mise à jour d'état, à utiliser pour tout futur ajout de plusieurs zones à la fois ; tâche 2 — `useDelineateState.ts` transmet aussi `wizardDefaults?.aiProvider` à `analyzeSatelliteImage`)
- components: `ContractCard.tsx`/`ContractsStatsRow.tsx`/`ContractsListContent.tsx` (nouveaux, Optimisation UI v3 — nouvelle page liste "cartes" : `ContractsListContent` = `QueryState` + `ContractsStatsRow` + recherche + `FilterChips` (`components/ui/`) + liste de `ContractCard`, partagé par `DesktopContractsListPage`/`MobileContractsListPage`), `ContractFormModal.tsx`, `ContractForm.tsx`, `ContractStatusBadge.tsx`, `TextareaField.tsx`, `ContractWizardDefaultsForm.tsx` (tâche 11 — gagne le champ "Date du 2e versement (Bi-paiement)" ; tâche 14 — gagne le `Select` "Modèle IA (détection automatique du stationnement)", Flash/Pro ; tâche 2 — sélecteur "Modèle" devient un sélecteur en **cascade** sous un nouveau `Select` "Fournisseur IA" (Google/TokenRouter, `AI_PROVIDER_OPTIONS`) — liste de modèles dérivée de `AI_MODEL_OPTIONS_BY_PROVIDER[aiProvider]` via `useWatch({ control, name: 'aiProvider' })`, `aiModel` réinitialisé au 1er choix valide par un `useEffect` dès qu'il sort de la liste courante ; premier sélecteur en cascade du projet, pattern réutilisable ; **tâche 6 — gagne le `Select` "Le prix du contrat est" (`PRIX_TAXES_OPTIONS`, avant/après taxes)**). **`ContractBasicInfoFields.tsx` supprimé (tâche 5)** — plus aucun champ à porter, tous dérivés/déplacés. **`ContractSummaryStats.tsx` supprimé (tâche 9)** — colonne droite "Informations du contrat" retirée de `ContractCreatedPage`, code mort après ce retrait. **`PaymentScheduleBuilder.tsx` supprimé (tâche 11)** — l'échéancier n'est plus modifiable échéance par échéance, uniquement via le sélecteur Annuel/Bi-paiement de `WizardStepClient.tsx`. **`ContractTable.tsx` supprimé (Optimisation UI v3)** — remplacé par `ContractsListContent.tsx`, plus aucun usage après la réécriture des 2 pages liste.
- components/detail (tâche 9 — refonte complète de la fiche contrat ; **modernisation fiche contrat v2 (2026-07-18) — restyle complet façon `.input/design-v2.png`**, composants purs/auto-suffisants partagés à l'identique par `DesktopContractDetailPage.tsx`/`MobileContractDetailPage.tsx`, seule la composition grille/pile change) : `ContractDetailHeader.tsx` (bloc d'actions Modifier/Envoyer par courriel (**v2 — `Button variant="info"`, bleu**)/Télécharger PDF/Annuler le contrat + menu `Dropdown` "…"), `ContractInfoStrip.tsx` (**v2 — devient "Informations générales", liste verticale compacte 1 colonne (Prix/Signature/Début/Fin/Renouvellement/Mode de paiement/Type/Statut), 1 des 3 colonnes de la 1ère rangée au lieu d'un bandeau pleine largeur**), `ContractClientCard.tsx` (**v2 — liens tél/courriel en `text-reca-info` (bleu), CTA bas de carte devient un `Button` "Ouvrir la fiche client" au lieu d'un `ListRow`**), `ContractMapCard.tsx` (**v2 — absorbe visuellement `ContractZonesStatRow` (rendu à l'intérieur), icône d'agrandissement dans l'en-tête, devient la 3e colonne de la 1ère rangée**), `ContractZonesStatRow.tsx` (**v2 — prop renommée `onEditZones`→`onViewTrace`, bouton "Voir le tracé complet" déclenche le lightbox de `ContractMapCard` au lieu d'un toast placeholder ; n'est plus importé par les pages, seulement par `ContractMapCard`**), `ContractOperatorInfoCard.tsx` + `ContractOperatorInfoEditModal.tsx` (**v2 — devient "Informations terrain pour l'opérateur", 3 colonnes : Obstacles connus/Consignes spéciales en listes à puces (texte libre `.split('\n')`) + encart ambre "ATTENTION" pour le message opérateur ; le 4e champ `notes` ("Autres détails") est retiré de cette carte (reste éditable via "Modifier"/`ContractForm.tsx`), `ContractOperatorInfoField` perd `'notes'`**), `ContractClausesCard.tsx` (**v2 — devient une checklist ✓ dynamique (1 ligne par service actif de `contract.services` + "Responsabilité limitée au contrat" fixe), le texte légal complet déplacé dans la nouvelle `ContractClausesModal.tsx`, remplace l'ancien expand/collapse inline**), `ContractPaymentsCard.tsx` (**v2 — tableau de versements inline (mêmes colonnes que l'ex-modale) + ligne Total, plus de résumé compact/bouton modale**), `ContractNotesCard.tsx` + `ContractNoteTimelineItem.tsx` + `ContractAddNoteModal.tsx` (timeline "Notes au dossier", table `contract_notes`, inchangés par la v2), `ContractHistoryCard.tsx` + `ContractHistoryModal.tsx` + `ContractEventTimelineItem.tsx` (**nouveaux, v2 — "Historique du contrat"**, table `contract_events`, auto-suffisant comme `ContractNotesCard`). **`ContractPaymentsHistoryModal.tsx` supprimé (v2)** — remplacé par le tableau inline de `ContractPaymentsCard.tsx`, plus aucun appelant.
- components/contract-document (nouveau, tâche 8 (2026-07-15) — prévisualisation HTML "façon future page PDF" affichée sur `ContractCreatedPage`, composants **purs**, aucun hook/router à l'intérieur, réutilisables tels quels par un futur moteur PDF) : `types.ts` (`ContractDocumentData` ; **tâche 8 (2026-07-17, 2e occurrence) — gagne `imageUrl: string | null`**), `DocumentSectionHeader.tsx` (primitif d'en-tête `plain`/`navy`), `DocumentHeader.tsx`, `DocumentServicesModalites.tsx`, `DocumentClientZones.tsx`, `DocumentSummaryPayment.tsx` (tâche 10 — reçoit aussi `settings`, affiche Sous-total/TPS/TVQ/Total calculés depuis `settings.taxes` ; tâche 11 — montant de chaque versement affiché via `computeInstallmentAmount` au lieu du `%` brut ; **tâche 6 — Sous-total/Total branchés sur `contract.prixTaxes` (avant/après taxes), même logique que `computeInstallmentTaxes` de `contracts.service.ts`**), `DocumentSignatures.tsx`, `DocumentFooterBar.tsx` (tâche 10 — ligne "Payer le contrat en ligne groupereca.ca/paiement" ajoutée, texte simple sans lien réel), `ContractDocumentPreview.tsx` (orchestrateur racine, tâche 10 — passe `settings` à `DocumentSummaryPayment` ; **tâche 8 (2e occurrence) — `DocumentClausesList` remplacé par `DocumentSatelliteZones`**), `DocumentSatelliteZones.tsx` (nouveau, **tâche 8 (2e occurrence)** — photo satellite avec zones tracées visibles dessus + légende (réutilise `PolygonCard.tsx` de `components/wizard/`), remplace `DocumentClausesList.tsx` **supprimé**). Grilles internes en `sm:` (640px, jamais `lg:`) — voir note piège dans `memory/memory.md`.
- components/mobile: `src/features/contracts/components/mobile/MobileContractLayout.tsx`, `MobileWizardStepProperty.tsx` (**tâche 8 — passe `contractId`/`onCaptured` à `MobilePropertySubStepDelineate`**), `MobilePropertySubStepLocate.tsx`, `MobilePropertySubStepDelineate.tsx` (tâche 1 — transmet `viewport`/`isAnalyzing`/`handleAutoDetect` à `ZoneToolbarFloating` ; **tâche 8 — accepte `contractId`/`onCaptured`, transmis à `useDelineateState` (recapture avec zones en quittant Délimiter, identique Desktop)**), `PropertyInfoSheet.tsx`, `PropertyZonesSheet.tsx`, `ZoneToolbarFloating.tsx` (tâche 1 — bouton "Détection automatique", icône `Loader2` animée pendant l'analyse), `ZoneDetailSheet.tsx` (tous sprint012 sauf mention contraire)
- components/wizard: `src/features/contracts/components/wizard/ContractWizard.tsx` (tâche 5 (2026-07-15) — `openPropertyAnalysis` passé à `WizardStepClient`, plus de rendu `services` ; tâche 11 — `control`/`register`/`errors`/`setValue` aussi passés à `WizardStepClient` ; tâche 5 (2026-07-17, 2e occurrence) — ces 4 props retirées de `WizardStepClient` (prix/échéancier repartis vers `WizardStepTerms`) ; **tâche 7** — calcule `immersive` depuis `propertyNav.immersive`, appelle `useDesktopImmersive(immersive)`, passe `hideProgress`/`headerActions` conditionnels à `WizardLayout`), `useContractWizardState.ts` (**vit ici, pas dans `hooks/`** — tâche 5 (2026-07-15) : `STEP_ORDER` 4 étapes `['client','property','terms','review']`, `propertyAnalysisRequested`/`openPropertyAnalysis`/`nextSkippingProperty` pour l'étape "Analyse & Zones" optionnelle, `clientTypeLabel` dérivé de `selectedClient.typeClient` ; tâche 11 — `STEP_COMPLETION.client` exige aussi `prix`+`modalitesPaiement`, `STEP_LABELS.terms` renommé "Paiement" ; tâche 5 (2026-07-17, 2e occurrence du nom) — `STEP_COMPLETION.client` revient à `Boolean(selectedClient)` seul, `STEP_COMPLETION.terms` regagne `prix`+`modalitesPaiement` en plus de `modePaiement`), `AddressPreviewCard.tsx` (tâche 5 — restylée "Adresse Validée" verte + bouton "Outil de mesure" `onOpenMeasurementTool`, plus de titre "Adresse" rouge), `ContractSummaryPanel.tsx` (tâche 5 — `typeLabel` dérivé de `client.typeClient` au lieu d'un `useWatch('type')`), `PolygonEditor.tsx` (tâche 12 — branche le mode `draw_polygon` custom `createDrawPolygonModeWithAllVertices` ; tâche 1 — gagne `addSuggestedZone(geojson, zoneId, type)`, ajoute directement une zone suggérée par Gemini sans passer par la fenêtre de nommage), `PropertyMap.tsx` (tâche 12 — prop `zoom` optionnel, plus de `19.5` codé en dur), `PropertyBoundaryLayer.tsx`, `PropertyMaskLayer.tsx`, `MapViewportController.tsx` (tâche 12 — prop `autoFit` : désactive `fitBounds()` quand un cadrage capturé existe déjà), `PropertyMapStage.tsx` (tâche 12 — prop `initialViewport`, calcule `autoFit` en interne), `PropertyInfoPanel.tsx`, `PropertyInfoContent.tsx`, `PropertyZonesPanel.tsx`, `PropertyZonesContent.tsx`, `PropertySubStepDelineate.tsx` (tâche 12 — reçoit/transmet `initialViewport` ; tâche 1 — transmet `viewport`/`isAnalyzing`/`handleAutoDetect` à la toolbar ; **tâche 4 — Desktop seulement, refonte carte plein écran** : quand `mapReady`, bascule sur un layout `relative` plein bord — `PropertyZonesPanel` devient flottant `absolute bottom-4 right-4`, nouvelle `ZoneToolbarFloatingDesktop` flottante `absolute top-4 left-4`, `Recentrer` câblé via `onRecenterReady` (absent avant côté Desktop) ; conserve intact l'ancien layout en grille 30/70 quand la carte est indisponible (repli manuel) ; **tâche 8 — accepte `contractId`/`onCaptured`, transmis à `useDelineateState`**), `PropertySubStepLocate.tsx` (tâche 12 — reçoit/transmet `initialViewport`, `onCaptured` remonte aussi le `viewport` ; **tâche 4** — même bascule : `PropertyInfoPanel` flottant `absolute top-4 left-4` quand `showMap`, layout grille conservé pour le repli manuel ; **tâche 7** — `onNavChange` transmet aussi `immersive: showMap`), `PropertySubStepValidate.tsx`, `SurfaceSummary.tsx`, `WizardStepClient.tsx` (tâche 5 (2026-07-15) — réduit à `ClientSearchPicker`+`AddressPreviewCard` ; tâche 11 — regagne `prix` (Input) + un sélecteur d'échéancier Annuel/Bi-paiement (2 `Button`, surlignage via `detectPreset`), props `control`/`register`/`errors`/`setValue` ; **tâche 5 (2026-07-17, 2e occurrence du nom) — redevenu minimal**, `prix`/échéancier repartis vers `WizardStepTerms.tsx`, props `control`/`register`/`errors`/`setValue` retirées), `WizardStepTerms.tsx` (tâche 5 (2026-07-15) — bloc Obligations retiré entièrement, dateDebut`/`modeConclusion` lus via `useContractWizardDefaults()` au lieu du form ; tâche 11 — `prix`/échéancier repartis vers `WizardStepClient.tsx`, `PaymentScheduleBuilder` retiré, ne garde plus que Mode de paiement + Notes + warnings ; **tâche 5 (2026-07-17, 2e occurrence du nom) — regagne `prix`(Input)+échéancier (2 `Button`) en tête, avant Mode de paiement**, prop `setValue` désormais utilisée), `WizardStepProperty.tsx` (consomme `usePropertyStepState` ; **tâche 4** — bandeau de sous-étapes gagne son propre padding, sous-étape `validate` enveloppée d'un padding+scroll local — nécessaire depuis que `WizardLayout` ne paddage plus ce contenu, voir `fullBleedContent` ; **tâche 7** — `PropertyNav` gagne `immersive?: boolean`, bandeau de sous-étapes masqué entièrement (`subStep === 'validate' || mapUnavailable`) ; **tâche 8** — passe `contractId`/`onCaptured={handleCaptured}` à `PropertySubStepDelineate`), `WizardStepValidation.tsx` (tâche 5 — `type`/`services`/`obligations` ne sont plus des `useWatch` du form, lus depuis `client.typeClient` + `useContractWizardDefaults()` ; tâche 11 — échéances affichées via `computeInstallmentAmount` (montant $) au lieu du pourcentage brut), `ZoneToolbarFloatingDesktop.tsx` (nouveau, tâche 4 — toolbar verticale flottante Desktop, remplace `ZoneToolbar.tsx` **supprimé** (barre horizontale, code mort après ce remplacement) ; composant autonome, ne réutilise pas `components/mobile/ZoneToolbarFloating.tsx` — duplication délibérée d'un petit bouton icône pour ne prendre aucun risque sur le Mobile déjà vérifié), `ZoneTypeSelector.tsx`, `ZoneNamingModal.tsx`, `PolygonCard.tsx`, `PolygonList.tsx`, `ZoneAreaSummary.tsx`. **`WizardStepServices.tsx` supprimé (tâche 5, étape retirée)**.
- components/wizard/mobile: `src/features/contracts/components/wizard/mobile/MobileContractWizard.tsx` (tâche 5 — plus de case `services`, `onOpenMeasurementTool` passé à `WizardStepClient`)
- constants: `src/features/contracts/constants/wizardOptions.ts` (`SERVICE_OPTIONS`/`SEUIL_DECLENCHEMENT_OPTIONS`/`DEPOT_NEIGE_OPTIONS`/`MODE_CONCLUSION_OPTIONS`/`MODE_CONCLUSION_LABELS` désormais consommés par `ContractWizardDefaultsForm.tsx` en plus du Wizard ; `ZONE_TYPE_OPTIONS`/`ZONE_TYPE_COLORS` ; tâche 14 — `AI_MODEL_OPTIONS` ajouté ; tâche 2 — `AI_MODEL_OPTIONS` remplacé par `AI_MODEL_OPTIONS_BY_PROVIDER: Record<AiProvider, {value,label}[]>` (Google: Flash/Pro inchangés, TokenRouter: 3 nouvelles entrées), `AI_PROVIDER_OPTIONS` ajouté ; tâche 3 — `AI_MODEL_OPTIONS_BY_PROVIDER.tokenrouter` gagne `openai/gpt-5.4-mini` ("GPT-5.4 Mini"), 4e entrée), `zoneDrawStyles.ts` (tâche 12 — 2 couches `gl-draw-historical-vertex-outer/inner` pour les points décoratifs de `drawPolygonAllVertices.ts`), `contractStatusColors.ts` (**Optimisation UI v3 — `brouillon: 'gray'`, `a_signer: 'purple'`, `en_attente: 'yellow'`, `expire: 'red'` (avant : `gray`), `annule: 'gray'` (avant : `red`)**). **Tâche 6** — `PRIX_TAXES_OPTIONS` ajouté (avant/après taxes).
- pages: `src/features/contracts/pages/ContractDetailPage.tsx` (dispatcher), `ContractsListPage.tsx` (dispatcher), `ContractWizardPage.tsx` (dispatcher), `ContractWizardSettingsPage.tsx` (nouveau, tâche 5 — page admin-only des paramètres par défaut, route `contracts/parametres`), `ContractCreatedPage.tsx` (nouveau, tâche 8 (2026-07-15) — page "Contrat enregistré !", route `contracts/:id/created`, reçoit `contract`/`client`/`zones` via `location.state` avec filet de secours `useContract`/`useClient` ; **tâche 8 (2026-07-17, 2e occurrence) — calcule `imageUrl` (URL signée depuis `zones[0]?.imageStoragePath`, `useEffect`+`useState`), transmis à `ContractDocumentPreview`**)
- pages/desktop: `src/features/contracts/pages/desktop/DesktopContractsListPage.tsx` (tâche 5 — icône ⚙️ admin-only vers `/contracts/parametres` ; **Optimisation UI v3 — corps délégué à `ContractsListContent`**), `DesktopContractDetailPage.tsx` (**tâche 9 — réécriture complète**, corps délégué aux composants `components/detail/`, grille `lg:grid-cols-3`, ne fetch que ce qui est partagé par ≥2 cartes : `useContract`/`useContractZones`/`useContractInvoices`/`usePaymentsByContract`)
- pages/mobile: `src/features/contracts/pages/mobile/MobileContractsListPage.tsx` (tâche 5 — icône ⚙️ admin-only ; **Optimisation UI v3 — corps délégué à `ContractsListContent`**), `MobileContractDetailPage.tsx` (**tâche 9 — réécriture complète**, mêmes composants `components/detail/` que Desktop empilés en 1 colonne, carte satellite juste après l'en-tête ; actions désormais inline via `ContractDetailHeader` — plus de `headerActions` dans `MobileContractLayout`)
- pdf (nouveau, branche `module-creation-pdf`) : `src/features/contracts/pdf/ContractPdfDocument.tsx` (**2 `<Page>` explicites** — page 1 = infos du contrat, page 2 = tracé de propriété, omise si aucune zone/image, voir `memory/memory.md`), `generateContractPdf.tsx` (orchestrateur, atteint uniquement via `import()` dynamique depuis les pages ; suite — exporte aussi `buildContractPdfBlob()`, construction du `Blob` sans télécharger, réutilisée par l'envoi par courriel), `sections/PdfContractHeader.tsx`, `PdfContractDetails.tsx` (services+modalités fusionnés, services en liste inline compacte), `PdfClientCard.tsx` (client seul, sans les zones — couvertes en page 2), `PdfSatelliteZones.tsx` (contenu entier de la page 2 : en-tête + image + tableau des zones), `PdfSignatures.tsx`, `PdfSummaryPayment.tsx` (récapitulatif+paiement fusionnés, échéancier en tableau compact) — mire `contract-document/` en primitives react-pdf, réutilise `computeInstallmentAmount`/`getNextPaymentEntry` de `paymentPresets.ts`
- utils: `src/features/contracts/utils/generateClauses.ts` (tâche 5 — `obligationsClient` devient un texte fixe (`OBLIGATIONS_CLIENT_BASE`, plus de sentence-builder Q&R puisque balises/entrée libre/animaux/portail/autres particularités n'existent plus) — **à faire relire par un juriste avant production**), `computeSoftWarnings.ts`, `draftMapping.ts` (tâche 5 — `contractToFormValues` réduit aux champs restants du form, `DEFAULT_OBLIGATIONS` retiré), `paymentPresets.ts` (tâche 5 — `'mensuel'`/`buildMonthlySchedule` retirés, `PaymentPresetId` = `'annuel'|'deux_versements'` ; tâche 8 — `getNextPaymentEntry` ajouté, réutilisé par `ContractSummaryPanel.tsx`/`DocumentSummaryPayment.tsx` ; tâche 11 — labels renommés `Annuel`/`Bi-paiement`, `buildPaymentSchedule` prend désormais `dateDeuxiemeVersement` (date fixe des paramètres) au lieu de calculer +3 mois, `detectPreset` (surlignage du sélecteur actif) et `computeInstallmentAmount` (montant $ réel d'une échéance, déplacé depuis `contracts.service.ts`, partagé par la génération de factures et les prévisualisations) ajoutés), `propertyBoundary.ts`, `drawPolygonAllVertices.ts` (nouveau, tâche 12 — `createDrawPolygonModeWithAllVertices()` : mode `draw_polygon` Mapbox GL Draw custom, affiche un point à chaque sommet déjà commis pendant le tracé), `satelliteZoneProjection.ts` (nouveau, tâche 1 — `boundingBoxToPolygon(bbox, map)` : convertit une bounding box Gemini (0-1000) en polygone GeoJSON via `map.unproject()`, en tenant compte de `devicePixelRatio`)

## invoices
- types: `src/features/invoices/types/invoice.types.ts`
- schemas: `src/features/invoices/schemas/invoice.schema.ts`
- services: `src/features/invoices/services/invoices.service.ts`
- hooks: `src/features/invoices/hooks/invoiceKeys.ts`, `useClientInvoices.ts`, `useContractInvoices.ts`, `useCreateInvoice.ts`, `useDeleteInvoice.ts`, `useInvoices.ts`, `useInvoice.ts`, `useUpdateInvoiceStatus.ts`, `useUpdateInvoice.ts`
- components: `src/features/invoices/components/InvoiceFormModal.tsx`, `InvoiceForm.tsx`, `InvoiceStatusBadge.tsx`, `InvoiceTable.tsx` (liste uniquement — la fiche détail utilise désormais `components/detail/`, pas ce tableau)
- components/detail (nouveau, branche `optimisation-ui` — même pattern que `contracts/components/detail/`/`clients/components/detail/`, composants purs partagés desktop/mobile) : `InvoiceDetailHeader.tsx` (Modifier/Envoyer par courriel[bleu, placeholder]/Télécharger PDF[placeholder]/Annuler la facture + menu "…" statut arbitraire+suppression), `InvoiceSummaryCard.tsx` (Date/Sous-total/TPS/TVQ/Total/Solde/Statut, liste verticale), `InvoiceClientContractCard.tsx` (`ListRow` client + `ListRow` contrat), `InvoicePaymentsCard.tsx` (bouton "Enregistrer un paiement" ouvrant `PaymentFormModal` inchangé + tableau `Table` générique, remplace l'ancienne liste `<div>` brute).
- pages: `src/features/invoices/pages/InvoiceCreatePage.tsx`, `InvoicesListPage.tsx`, `InvoiceDetailPage.tsx` (**branche `optimisation-ui` — devient un dispatcher `useDeviceTier()` pur**). Breadcrumb `invoices/:id` renommé `'Détail'` → `'Détail facture'` dans `router.tsx`.
- pages/desktop (nouveau) : `DesktopInvoiceDetailPage.tsx`
- pages/mobile (nouveau) : `MobileInvoiceDetailPage.tsx` (pas de composant de layout partagé, même raison que `MobileClientDetailPage.tsx`)
- pdf (nouveau, branche `module-creation-pdf`) : `src/features/invoices/pdf/types.ts` (`InvoicePdfData` — `Invoice.client`/`Invoice.contract` ne sont que des refs légères, ce type porte les objets `Client`/`Contract` complets récupérés séparément), `InvoicePdfDocument.tsx`, `generateInvoicePdf.tsx` (orchestrateur, `import()` dynamique ; suite — exporte aussi `buildInvoicePdfBlob()`, même raison que `buildContractPdfBlob`), `sections/PdfInvoiceHeader.tsx`, `PdfBillTo.tsx`, `PdfContractRef.tsx` (rendue seulement si un contrat est lié), `PdfInvoiceSummaryTable.tsx`, `PdfPaymentsTable.tsx` (aucun aperçu HTML n'existait avant, structure dérivée de `InvoiceSummaryCard`/`InvoiceClientContractCard`/`InvoicePaymentsCard`)

## payments
- types: `src/features/payments/types/payment.types.ts`
- schemas: `src/features/payments/schemas/payment.schema.ts`
- services: `src/features/payments/services/payments.service.ts` (**tâche 9 (contrats) — `listPaymentsByContract(contratId)` ajouté : passe par `invoices.contrat_id` puis `payments.facture_id in (...)`, les paiements ne sont liés qu'à une facture, jamais directement à un contrat**)
- hooks: `src/features/payments/hooks/paymentKeys.ts` (**tâche 9 — `byContract` ajouté**), `useCreatePayment.ts`, `useDeletePayment.ts`, `useInvoicePayments.ts`, `usePayments.ts`, `usePaymentsByContract.ts` (nouveau, tâche 9 — consommé par `ContractPaymentsCard.tsx`/`ContractPaymentsHistoryModal.tsx` du module Contrats), `usePaymentsListFilters.ts` (nouveau, branche `optimisation-ui` — recherche+filtre par méthode de la page liste, même pattern que `useContractsListFilters.ts`, `PAYMENT_METHOD_FILTER_OPTIONS` exporté)
- components: `src/features/payments/components/PaymentFormModal.tsx`, `PaymentForm.tsx` (inchangés, toujours utilisés depuis `InvoicePaymentsCard.tsx`), `PaymentsStatsRow.tsx`/`PaymentCard.tsx`/`PaymentsListContent.tsx` (nouveaux, branche `optimisation-ui` — restyle "cartes" de la page liste, même langage que `ContractsStatsRow.tsx`/`ContractCard.tsx`/`ContractsListContent.tsx` ; `PaymentCard` navigue vers la facture liée au clic, aucune fiche paiement dédiée n'existe). **`PaymentTable.tsx` supprimé** (code mort, remplacé par `PaymentsListContent.tsx`, plus aucun appelant).
- pages: `src/features/payments/pages/PaymentsListPage.tsx` (branche `optimisation-ui` — corps délégué à `PaymentsListContent`, reste une page **unique**, pas de split desktop/mobile — aucune "fiche paiement" détail n'existe, voir `memory/memory.md`)

## equipments
*(dossier réel `src/features/equipments/`, pluriel — pas `equipment`)*
- types: `src/features/equipments/types/equipment.types.ts`
- schemas: `src/features/equipments/schemas/equipment.schema.ts`
- services: `src/features/equipments/services/equipments.service.ts`
- hooks: `src/features/equipments/hooks/equipmentKeys.ts`, `useCreateEquipment.ts`, `useUpdateEquipmentStatus.ts`, `useEquipment.ts`, `useEquipments.ts`, `useUpdateEquipment.ts`, `useDeleteEquipment.ts`
- components: `src/features/equipments/components/EquipmentFormModal.tsx`, `EquipmentForm.tsx`, `EquipmentTable.tsx`, `EquipmentStatusBadge.tsx`
- pages: `src/features/equipments/pages/EquipmentCreatePage.tsx`, `EquipmentsListPage.tsx`, `EquipmentDetailPage.tsx`

## employees
- types: `src/features/employees/types/employee.types.ts`
- schemas: `src/features/employees/schemas/employee.schema.ts`
- services: `src/features/employees/services/employeeAccount.service.ts`, `employeeEquipment.service.ts`, `employees.service.ts`
- hooks: `src/features/employees/hooks/employeeKeys.ts`, `useAssignEquipment.ts`, `useCreateEmployee.ts`, `useDeleteEmployee.ts`, `useEmployeeAccount.ts`, `useEmployeeEquipment.ts`, `useEmployees.ts`, `useEmployee.ts`, `usePromoteEmployeeAccount.ts`, `useUnassignEquipment.ts`, `useUpdateEmployee.ts`
- components: `src/features/employees/components/EmployeeFormModal.tsx`, `EmployeeForm.tsx`, `EmployeeTable.tsx`
- pages: `src/features/employees/pages/EmployeeCreatePage.tsx`, `EmployeeDetailPage.tsx`, `EmployeesListPage.tsx`

## routes
*(module V2, refonte complète 2026-07-22 — basé sur les contrats, pas les clients ; l'ancien `route_clients`/`route_assignments` a été entièrement abandonné)*
- types: `src/features/routes/types/route.types.ts` (`Route`/`RouteSummary`, `ROUTE_COLOR_PRESETS`), `routeContract.types.ts` (`RouteContract`), `unassignedContract.types.ts` (`UnassignedContract`), `routeMapPoint.types.ts` (`RouteMapPoint`/`RouteMapData`)
- schemas: `src/features/routes/schemas/route.schema.ts` (`routeSchema`, réutilisé création+Paramètres), `routeRename.schema.ts` (renommage inline ✏️)
- services: `src/features/routes/services/routes.service.ts` (`listRoutesSummary`/`getRoute`/`createRoute`/`updateRoute`/`renameRoute`/`deleteRoute`), `routeContracts.service.ts` (`assignContractToRoute` partagé par quick-assign/Ajouter/Transférer, `removeContractFromRoute`, `reorderRouteContract` → RPC), `unassignedContracts.service.ts` (`listUnassignedContracts(season)`, anti-join côté client), `routesMap.service.ts` (`getRoutesMapData`, calcule un point par contrat via `utils/contractPoint.ts`)
- hooks: `src/features/routes/hooks/routeKeys.ts` + `useRoutes.ts`/`useRoute.ts`/`useCreateRoute.ts`/`useUpdateRoute.ts`/`useRenameRoute.ts`/`useDeleteRoute.ts`/`useRouteContracts.ts`/`useAssignContractToRoute.ts`/`useRemoveContractFromRoute.ts`/`useReorderRouteContract.ts`/`useUnassignedContracts.ts` (dépend de `useContractWizardDefaults()` du module Contrats pour la "saison courante")/`useRoutesMapData.ts`
- utils: `src/features/routes/utils/contractPoint.ts` (`computeContractPoint` — centroïde `@turf/centroid` de la plus grande zone, repli adresse géocodée), `mapBounds.ts` (`boundsFromPoints`)
- components: `RouteColorPicker.tsx`, `RouteForm.tsx`, `RouteSettingsModal.tsx`, `RouteCard.tsx`, `RoutesTabs.tsx` (barre d'onglets locale, ne pas confondre avec `components/layout/PageTabs.tsx` partagé — padding différent, voir `memory/memory.md`)
- components/quickAssign: `UnassignedContractCard.tsx`, `QuickAssignControl.tsx` (boutons numérotés ≤5 routes / `<Select>` au-delà)
- components/detail: `RouteDetailHeader.tsx` (✏️ renommage inline), `RouteActionBar.tsx`, `RouteContractCard.tsx` (⬆⬇➡🗑), `RouteContractsList.tsx`, `AddContractToRouteModal.tsx`, `TransferContractModal.tsx`
- components/map: `RoutesMapView.tsx` (marqueurs `mapboxgl.Marker` colorés par route + popup, pas de source/layer GeoJSON — volume de points faible), `RouteSelector.tsx`
- pages: `RoutesShellPage.tsx` (coquille 3 onglets, `<Outlet/>`), `RoutesListTabPage.tsx`, `ContratsTabPage.tsx`, `CarteTabPage.tsx`, `RouteCreatePage.tsx`, `RouteDetailPage.tsx` (fiche Route — pas de split desktop/mobile ce sprint, voir `memory/memory.md`)
- Migrations : `supabase/migrations/20260722000000_contract_status_suspendu.sql` (statut `suspendu` sur `contracts`), `20260722010000_drop_routes_v1.sql`, `20260722020000_routes_v2.sql` (tables `routes`+`route_contracts`, policies SELECT élargies admin — voir piège RLS dans `memory/memory.md`), `20260722030000_reorder_route_contract.sql` (RPC)

## missions
*(fusionné dans `main` le 2026-07-23 avec `module-routes-v2`, dont il dépend — voir `memory/memory.md` pour le conflit avec l'ex-module Interventions retiré de `main` à cette occasion)*
- types: `src/features/missions/types/mission.types.ts` (`MissionStatus`, `Mission`/`MissionSummary` avec compteurs d'items par statut), `missionItem.types.ts` (`MissionItemStatus`, `MissionItemSummary` joint contrat/client), `missionEvent.types.ts` (mirror `contractEvent.types.ts`), `missionNote.types.ts` (mirror `clientNote.types.ts`), `missionMapPoint.types.ts` (mirror `routeMapPoint.types.ts`)
- schemas: `src/features/missions/schemas/mission.schema.ts`, `missionNote.schema.ts` (mirror `clientNote.schema.ts`)
- services: `src/features/missions/services/missions.service.ts` (`listMissionsSummary`/`getMission`/`createMission`/`updateMissionStatus` — journalise l'événement adéquat de façon atomique, mirror `updateContractStatus`), `missionItems.service.ts` (`listMissionItems`/`updateMissionItemStatus`/`copyActiveContractsToMission` — la copie automatique des contrats actifs d'une Route à la création), `missionsMap.service.ts` (mirror `routesMap.service.ts`, coloré par statut de MissionItem), `missionEvents.service.ts` (mirror `contractEvents.service.ts`), `missionNotes.service.ts` (mirror `clientNotes.service.ts`, CRUD complet)
- hooks: `src/features/missions/hooks/missionKeys.ts`, `useMissions.ts`, `useMission.ts`, `useCreateMission.ts` (orchestre création + copie des contrats + événement, séquentiel), `useUpdateMissionStatus.ts`, `useMissionItems.ts`, `useUpdateMissionItemStatus.ts` (pas de toast succès, évite le spam sur clics répétés), `useMissionsMapData.ts`, `useMissionEvents.ts`, `useLogMissionEvent.ts` (journal best-effort, utilisé par les boutons Pause/Reprise), `useMissionNotes.ts`/`useAddMissionNote.ts`/`useUpdateMissionNote.ts`/`useDeleteMissionNote.ts` (mirror des 4 hooks Clients), `useMissionsListFilters.ts` (filtre multi-dimension Date/Statut/Route/Opérateur/Équipement + recherche, 100% local)
- constants: `src/features/missions/constants/missionStatusColors.ts`, `missionItemStatusColors.ts` (source unique de couleur pour le badge ET les marqueurs de la carte)
- components: `src/features/missions/components/MissionCard.tsx` (carte liste), `MissionStatusBadge.tsx`, `MissionForm.tsx` (page Nouvelle Mission), `MissionsListContent.tsx` (recherche + filtres + cartes, mirror `ContractsListContent.tsx`)
- components/detail: `MissionDetailHeader.tsx` (badge + boutons Débuter/Pause/Reprendre/Fermer/Annuler selon statut), `MissionInfoCard.tsx`, `MissionProgressCard.tsx`, `MissionItemCard.tsx`/`MissionItemStatusBadge.tsx`/`MissionItemsList.tsx`, `MissionCloseGuardBanner.tsx` (gate de fermeture + "Fermer quand même" admin-only), `MissionNotesCard.tsx`/`MissionNoteTimelineItem.tsx`/`MissionEditNoteModal.tsx` (mirror Clients), `MissionHistoryCard.tsx`/`MissionHistoryModal.tsx`/`MissionEventTimelineItem.tsx` (mirror Contrats)
- components/map: `MissionMapView.tsx` (mirror `RoutesMapView.tsx` du module Routes, coloré par statut de MissionItem ; réutilise `computeContractPoint`/`boundsFromPoints` de `src/features/routes/utils/` par import cross-feature, pas de duplication)
- pages: `src/features/missions/pages/MissionsListPage.tsx`, `MissionCreatePage.tsx`, `MissionDetailPage.tsx` — **implémentation unique responsive, pas de split Desktop/Mobile** (suit le pattern de `module-routes-v2`, pas celui des modules plus anciens, voir `memory/memory.md`)
- Migration : `supabase/migrations/20260723000000_missions.sql` (4 tables : `missions`/`mission_items`/`mission_events`/`mission_notes`, pas encore appliquée)

## settings
- types: `src/features/settings/types/settings.types.ts` (sprint014 — `assurancePoliceNo`/`assurance_police_no` ; module Missions — `ModuleKey` gagne `'missions'`, 10e clé)
- schemas: `src/features/settings/schemas/settings.schema.ts` (sprint014 — `assurancePoliceNo`)
- services: `src/features/settings/services/accounts.service.ts`, `settings.service.ts` (sprint014 — mapping `assurance_police_no` ; module Missions — `DEFAULT_MODULES.missions: true` ajouté), `preferences.service.ts` (nouveau, Optimisation UI v3 — `updateOwnTheme(theme)`, appelle la RPC `update_own_theme`)
- hooks: `src/features/settings/hooks/settingsKeys.ts`, `useAccounts.ts`, `useSettings.ts`, `useUpdateAccountActive.ts`, `useUpdateAccountRole.ts`, `useUpdateModules.ts`, `useUpdateSettings.ts`, `useUpdateTheme.ts` (nouveau, Optimisation UI v3 — mutation `updateOwnTheme`, invalide `sessionQueryKey`)
- components: `src/features/settings/components/AccountsTable.tsx`, `CompanySettingsForm.tsx` (sprint014 — champ "N° de police d'assurance"), `ModulesTable.tsx` (**`MODULE_LABELS`/`MODULE_ORDER` codés en dur, PAS dynamiques — module Missions ajouté aux deux, `MODULE_ORDER` n'est pas vérifié par TypeScript donc un oubli y serait silencieux, à toujours vérifier manuellement pour tout futur module**), `ThemeToggle.tsx` (nouveau, Optimisation UI v3 — pilules Sombre/Clair, consomme `useTheme()`)
- pages: `src/features/settings/pages/SettingsPage.tsx` (Optimisation UI v3 — nouvelle `Card` "Préférences" avec `ThemeToggle`)

## Partagé / transverse
- `src/components/layout/` : `ModuleContainer.tsx`, `PageLayout.tsx`, `PageTabs.tsx`, `StickyPageFooter.tsx`, `StickyPageHeader.tsx`, `WizardFooter.tsx` (sprint014 — `onDraft`/`draftDisabled` retirés, brouillon déplacé vers `WizardLayout.headerActions`), `WizardLayout.tsx` (sprint014 — `headerActions?`/`sidePanel?` optionnels ajoutés ; tâche 4 — `fullBleedContent?` optionnel, retire le padding/`overflow-y-auto` du conteneur de contenu pour une étape qui gère elle-même son padding/scroll interne, utilisé par l'étape Property du Wizard Contrats ; tâche 7 — `hideProgress?` optionnel, masque entièrement `WizardProgress`), `WizardProgress.tsx`
- `src/components/layout/mobile/` (sprint012 — Wizard mobile générique, réutilisable hors Contrats) : `FloatingActionBar.tsx` (sprint014 — `onDraft` rendu sur chaque étape, pas seulement la dernière), `MobileFooterInsetContext.ts` (hauteur du footer courant, lue par `BottomSheet` pour ne jamais se positionner par-dessus), `MobileStepLayout.tsx`, `MobileWizard.tsx`
- `src/components/ui/` : `FilterChips.tsx` (nouveau, Optimisation UI v3 — premier composant "chip" du repo, pilules cliquables `{id,label,count?}`, générique/transverse, utilisé par la page liste Contrats), `AddressAutocomplete.tsx` (tâche 5 — champ adresse avec suggestions Mapbox à la frappe, dégrade en texte libre si `isMapboxConfigured` est faux, réutilisable partout où un champ adresse existe ; tâche 7 — `parseFeature()` combine `feature.address` (numéro civique) + `feature.text` (nom de rue), Mapbox les renvoie séparément pour les features `address`), `Avatar.tsx` (nouveau, sprint013 — initiales/photo, construit mais non branché ce sprint ; **refonte fiche Client — gagne un prop optionnel `color?: StatusColor` (additif, défaut `bg-reca-gray-light` inchangé), utilisé par `ClientNoteTimelineItem.tsx` via `src/lib/avatarColor.ts`, PAS rétroactivement par `ContractNoteTimelineItem.tsx` (hors scope)**), `Badge.tsx` (sprint013 — `size` sm/md, couleurs déplacées vers `statusColors.ts`), `BottomSheet.tsx` (sprint012 — feuille du bas glissable/redimensionnable, coexiste avec `Modal.tsx`), `Button.tsx` (sprint013 — `fullWidth` additif, non branché ce sprint ; **modernisation fiche contrat v2 — variant `info` ajouté, `bg-reca-info` (token existant, jamais utilisé comme bouton avant), pour tout CTA bleu "secondaire d'interface" distinct du rouge RECA**), `Card.tsx` (sprint013 — ombre au lieu de bordure dure, `variant="clickable"`/`chevron` additifs), `Dropdown.tsx` (**tâche 9 — gagne un prop optionnel `className` (additif, défaut `''`), fusionné sur le wrapper `inline-block`, permet à un déclencheur `Dropdown` de s'étirer `w-full` dans une pile de boutons mobile**), `Input.tsx`, `ListRow.tsx` (nouveau, sprint013 — icône+titre+sous-titre+valeur/chevron, générique), `Modal.tsx`, `QueryState.tsx` (**tâche 9 — état d'erreur gagne des variantes `dark:` (`border-red-500/30`/`bg-red-500/15`/`text-red-400`), oubliées lors du passage thème sombre d'Optimisation UI v3**), `Select.tsx`, `StatCard.tsx` (nouveau, sprint013 — carte KPI générique), `StaticMapThumbnail.tsx` (nouveau, refonte fiche Client — vignette de carte statique via l'API Mapbox Static Images, style `dark-v11`+pin, repli icône si Mapbox non configuré ou lat/lng absents, générique/transverse), `statusColors.ts` (sprint013 — `StatusColor`/`STATUS_BG_CLASSES`/`STATUS_TEXT_CLASSES` partagés ; **Optimisation UI v3 — gagne `purple`/`yellow`, chaque couleur gagne une variante `dark:` explicite (seul point du repo à utiliser `dark:` directement, car ces classes référencent des utilitaires Tailwind littéraux type `bg-green-50`, pas les tokens `reca-*` réactifs au thème)**), `Table.tsx` (sprint013 — classes alignées sur le nouveau style Card), `Toaster.tsx`, `Tooltip.tsx`, `useTableState.ts`
- `src/hooks/` : `useTheme.ts` (nouveau, Optimisation UI v3 — lit `session.user.theme` (défaut `'sombre'` sans session), applique/retire la classe `dark` sur `<html>`, expose `{theme, setTheme, isUpdating}` ; monté une fois via `ThemeSync` dans `src/app/App.tsx`), `useBodyScrollLock.ts`, `useBreakpoint.ts`, `useDeviceTier.ts` (nouveau, sprint012 — `'mobile'|'desktop'`, wrapper sémantique au-dessus de `useBreakpoint`), `useElementSize.ts`, `useFocusTrap.ts`, `useMapboxMap.ts`
- `src/components/map/` (repéré tâche 13, absent de l'index avant — à traiter comme transverse) : `MapCanvas.tsx` (wrapper bas-niveau générique autour de `useMapboxMap`, sans connaissance du domaine Contrats, réutilisé par `PropertyMap.tsx`)
- `src/components/pdf/` (nouveau, branche `module-creation-pdf` — primitives PDF partagées entre Contrats et Factures, ni l'un ni l'autre ne s'importent directement) : `pdfStyles.ts` (couleurs RECA statiques + `StyleSheet` react-pdf réutilisable), `PdfBrandHeader.tsx` (bandeau héro navy partagé, titre/méta paramétrables), `PdfFooter.tsx` (pied de page + numérotation, `fixed`), `downloadBlob.ts` (déclenche le téléchargement navigateur d'un `Blob`), `getPdfLogoDataUri.ts` (rastérise `logo-sombre.svg` en PNG au runtime via `<canvas>` — ni `logo.jpg` (JPEG progressif) ni un `.svg` brut ne sont utilisables directement par `<Image>` de react-pdf, repli texte si la rasterisation échoue)
- `src/components/email/` (nouveau, branche `module-creation-pdf`, suite — transverse comme `src/components/pdf/`) : `SendEmailModal.tsx` (modale générique RHF+Zod d'envoi de courriel, destinataire/objet/message pré-remplis+éditables, PDF joint généré à la confirmation via une prop `buildAttachmentBlob`, pas à l'ouverture)
- `src/layouts/` : `AppLayout.tsx` (sprint012 — devient un dispatcher Desktop/Mobile), `Breadcrumb.tsx` (desktop uniquement ; tâche 7 — lit `useDesktopImmersiveValue()`, retourne `null` en mode plein écran), `DesktopAppShell.tsx` (nouveau, sprint012 — ex-contenu d'`AppLayout.tsx` ; tâche 7 — enveloppé dans `DesktopChromeProvider`, `<main>` extrait dans un petit composant interne `DesktopMain` qui bascule son padding en mode immersif), `DesktopChromeContext.tsx` (nouveau, tâche 7 — `DesktopChromeProvider`, composant Provider, monté une fois par `DesktopAppShell`), `useDesktopChrome.ts` (nouveau, tâche 7 — hooks-only, séparé du Provider pour Fast Refresh, même pattern que `useMobileHeaderActions.ts` : `useDesktopImmersive(immersive)`/`useDesktopImmersiveValue()`, réutilisable par tout futur mode plein écran Desktop), `MobileAppShell.tsx` (nouveau, sprint012), `MobileBottomNavigation.tsx` (nouveau, sprint012), `MobileHeader.tsx` (nouveau, sprint012), `MobileHeaderActionsContext.tsx` (nouveau, sprint012 — composant Provider), `navItems.ts` (nouveau, sprint012 — `NAV_ITEMS`/`useVisibleNavItems` extraits de `Sidebar.tsx`, partagés ; module Missions — nouvelle entrée `{label:'Missions', to:'/missions', icon: ClipboardList, moduleKey:'missions'}`), `PublicLayout.tsx`, `Sidebar.tsx` (desktop/tablette uniquement, comportement inchangé), `useMobileHeaderActions.ts` (nouveau, sprint012 — hooks, séparé du Provider pour Fast Refresh)
- `src/routes/` : `RequireModule.tsx`, `router.tsx` (refonte fiche Client — breadcrumb de `clients/:id` renommé `'Détail'` → `'Détail client'` ; module Missions — nouveau bloc `RequireModule moduleKey="missions"` avec `missions`/`missions/new`/`missions/:id`)
- `src/lib/` : `email.service.ts` (nouveau, branche `module-creation-pdf`, suite — `sendEmail()`, appelle l'Edge Function `send-email`, convertit le `Blob` PDF en base64 côté client), `format.ts` (tâche 9 — `formatDateTime(iso)` ajouté, `Intl.DateTimeFormat` `dateStyle: 'medium'`+`timeStyle: 'short'`, utilisé par la timeline "Notes au dossier" du module Contrats), `avatarColor.ts` (nouveau, refonte fiche Client — `getAvatarColor(id)`, hash déterministe sur une petite palette `StatusColor`, utilisé par `ClientNoteTimelineItem.tsx`), `mapboxClient.ts`, `queryClient.ts`, `storage.ts`, `supabaseClient.ts`, `supabaseCrud.ts` (`createCrudService<TRow>` générique `select('*')` — pas de jointure possible, toute requête ayant besoin d'un embed doit être écrite à la main, voir `clients.service.ts` `getClient()`)
- Racine app (nouveau dans l'index, repéré Optimisation UI v3 — à traiter comme transverse) : `src/app/App.tsx` (monte `ThemeSync` — composant local qui appelle `useTheme()` et rend `null`, doit être un enfant de `QueryClientProvider`, pas un frère), `src/styles/index.css` (tokens `@theme` Tailwind v4 CSS-first ; `reca-black`/`reca-gray-medium`/`reca-gray-light`/`reca-snow` réactifs au thème via un bloc `.dark { ... }` qui redéfinit la même variable CSS — aucune classe `dark:` requise aux points d'usage ; nouveau token `reca-white` ; `@custom-variant dark (&:where(.dark, .dark *));` ajouté pour les rares vraies classes `dark:` explicites, voir `statusColors.ts`), `index.html` (`<html class="dark">` par défaut, sombre avant même le premier rendu React)
- `supabase/functions/` (nouveau, tâche 1 — première Edge Function du repo) : `send-email/index.ts` (nouveau, branche `module-creation-pdf`, suite, 2e Edge Function du repo — appelle l'API Resend avec `RESEND_API_KEY` (secret serveur), pièce jointe PDF en base64 ; expéditeur fixé à la constante `FROM_ADDRESS` = domaine de test `onboarding@resend.dev` en attendant un domaine vérifié, voir `memory/memory.md`), `analyze-satellite-image/index.ts` (Deno — `corsHeaders` explicites + handler `OPTIONS` requis pour tout appel navigateur, télécharge l'image capturée depuis le bucket privé `contract-captures` avec `SUPABASE_SERVICE_ROLE_KEY` auto-injecté, appelle le fournisseur IA choisi avec sa clé (secret Supabase, jamais côté client), valide la réponse avec Zod, consommée par `src/features/contracts/services/satelliteAnalysis.service.ts` ; tâche 14 — modèle Gemini choisi via `aiModel` reçu dans le corps de requête (`'flash'|'pro'`, défaut `'flash'`), mappé via `GEMINI_MODEL_BY_OPTION` vers un alias `-latest` (jamais un nom de modèle figé type `gemini-2.5-flash`, déjà renvoyé 404 "no longer available to new users" malgré son apparition dans `models.list`) ; réponse par zone enrichie de `confiance: 'haute'|'moyenne'|'faible'`. **`gemini-pro-latest` jamais testé réellement, faute de clé disponible dans ce sandbox** (voir `memory/memory.md`, tâche 14). **Tâche 2** — branche désormais sur `aiProvider` (`'google'`|`'tokenrouter'`, défaut `'google'`) : chemin Google inchangé, nouveau chemin TokenRouter (secret `TOKENROUTER_API_KEY`, URL `https://api.tokenrouter.com/v1beta/models/{aiModel}:generateContent`, `Authorization: Bearer` au lieu du `?key=`) — `aiModel` transmis tel quel pour TokenRouter (pas d'alias). Corps de requête `generateContent` factorisé dans `buildGenerateContentRequestBody()`, partagé par les 2 fournisseurs. **Confirmé par curl direct avec la vraie clé TokenRouter** : seul `google/gemini-3.5-flash` supporte `responseSchema`/JSON structuré ; les 2 modèles `-image` (`gemini-3.1-flash-lite-image`, `gemini-2.5-flash-image`) sont des modèles de génération d'image et renvoient `400` sur ce type de requête — voir `memory/memory.md`, tâche 2. **Tâche 3** — le chemin TokenRouter se scinde en 2 styles d'API selon `aiModel.startsWith('openai/')` (`isOpenAiCompatible`) : `google/...` inchangé (Gemini `generateContent`) ; `openai/...` (ex. `openai/gpt-5.4-mini`) route vers `https://api.tokenrouter.com/v1/chat/completions` (nouveau `buildOpenAiChatCompletionsBody`/`OPENAI_RESPONSE_SCHEMA` — JSON Schema strict standard, distinct de `GEMINI_RESPONSE_SCHEMA`), réponse parsée depuis `choices[0].message.content`. Même secret `TOKENROUTER_API_KEY`, pas de nouveau secret. **Confirmé viable par 4 tests curl directs** (texte, JSON schema strict, vision, test complet avec le vrai prompt+schéma sur une image satellite réelle) — voir `memory/memory.md`, tâche 3. Déploiement + secrets hors de portée de ce sandbox (pas de token CLI Supabase authentifié) — toujours à refaire manuellement par l'utilisateur après toute future modification de ce fichier (y compris `supabase secrets set TOKENROUTER_API_KEY=...` avant le premier essai du chemin TokenRouter, et un nouveau déploiement après la tâche 3 avant que `openai/gpt-5.4-mini` soit testable en conditions réelles).

## Maintenance
Cet index doit être mis à jour dans le protocole de fin de tâche (comme `tasks.md`/`memory.md`) dès qu'un fichier est ajouté, supprimé ou déplacé dans un module.
